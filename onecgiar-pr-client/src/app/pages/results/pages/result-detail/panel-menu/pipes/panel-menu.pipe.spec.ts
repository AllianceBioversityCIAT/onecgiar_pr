import { PanelMenuPipe } from './panel-menu.pipe';

describe('PanelMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new PanelMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
