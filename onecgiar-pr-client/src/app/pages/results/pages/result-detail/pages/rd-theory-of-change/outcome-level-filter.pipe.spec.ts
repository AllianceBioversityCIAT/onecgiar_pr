import { OutcomeLevelFilterPipe } from './outcome-level-filter.pipe';

describe('OutcomeLevelFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OutcomeLevelFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
