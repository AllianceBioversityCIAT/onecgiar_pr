import { FilterTargetsPipe } from './filter-targets.pipe';

describe('FilterTargetsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTargetsPipe();
    expect(pipe).toBeTruthy();
  });
});
