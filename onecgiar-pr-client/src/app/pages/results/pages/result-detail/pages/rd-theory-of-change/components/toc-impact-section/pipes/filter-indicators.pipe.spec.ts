import { FilterIndicatorsPipe } from './filter-indicators.pipe';

describe('FilterIndicatorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIndicatorsPipe();
    expect(pipe).toBeTruthy();
  });
});
