import { InstToInstTypesPipe } from './inst-to-inst-types.pipe';

describe('InstToInstTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new InstToInstTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
