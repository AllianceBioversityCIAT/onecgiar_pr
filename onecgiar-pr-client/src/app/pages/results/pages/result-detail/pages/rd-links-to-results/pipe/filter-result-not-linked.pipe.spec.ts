import { FilterResultNotLinkedPipe } from './filter-result-not-linked.pipe';

describe('FilterResultNotLinkedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterResultNotLinkedPipe();
    expect(pipe).toBeTruthy();
  });
});
