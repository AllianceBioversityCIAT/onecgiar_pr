import { ResultsToUpdateFilterPipe } from './results-to-update-filter.pipe';

describe('ResultsToUpdateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ResultsToUpdateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
