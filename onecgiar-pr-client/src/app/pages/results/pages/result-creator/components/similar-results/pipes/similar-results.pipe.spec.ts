import { SimilarResultsPipe } from './similar-results.pipe';

describe('SimilarResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new SimilarResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
