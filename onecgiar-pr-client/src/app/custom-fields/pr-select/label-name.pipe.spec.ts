import { LabelNamePipe } from './label-name.pipe';

describe('LabelNamePipe', () => {
  it('create an instance', () => {
    const pipe = new LabelNamePipe();
    expect(pipe).toBeTruthy();
  });
});
