import { ListFilterByTextAndAttrPipe } from './list-filter-by-text-and-attr.pipe';

describe('ListFilterByTextAndAttrPipe', () => {
  it('create an instance', () => {
    const pipe = new ListFilterByTextAndAttrPipe();
    expect(pipe).toBeTruthy();
  });
});
