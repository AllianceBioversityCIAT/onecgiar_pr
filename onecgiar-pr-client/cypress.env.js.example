// Cypress environment configuration for local development
// Copy this file to cypress.env.js and update with your test credentials
// This file should be ignored by Git and each developer should have their own copy

export const environment = {
  cypress: {
    // Add your Guest role credentials here for local development
    // Replace with your actual test user credentials
    guestEmail: 'your-test-email@domain.com',
    guestPassword: 'your-test-password'
  }
};
