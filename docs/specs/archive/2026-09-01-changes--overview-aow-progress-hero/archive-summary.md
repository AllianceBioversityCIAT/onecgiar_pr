# Archive Summary — `changes/overview-aow-progress-hero`

| Field | Value |
|---|---|
| Original spec path | `docs/specs/changes/overview-aow-progress-hero/` |
| Archive date | 2026-09-01 |
| Final status | **Done** — T-1..T-6 `[x]` (T-6: 9 PASS / 1 NOT-RUN accepted) |
| Approval mode | pre-approved (owner mandate; YOLO: 1 judgment pass fix-only, ≤1 review round, targeted jest) |
| Branch | `qa-development-2026` (spec branch; pin `master`) |

## Delivered

"Progress by area of work" promoted to the Overview hero: summary rail (SVG ring, mono figures, Complete/In-progress/Not-started splits, Continue-reporting CTA with storage-backed Only-pending), AoW rows sorted remaining-DESC with counts-true segmented bars + Report/open/View-results on the single `openAow` path, outcomes as footer chips + legend, ToC-aware skeletons (no jumping sums), zero-target rule adopted on this surface with `title` disclosure (thin consumers untouched — card 4/badge/hub numbers unchanged).

## Evidence

- `judgment.md` — 11 severe clusters fixed pre-execution (routes-not-signals, glossary partition gap, undecidable coherence gate, pinned-test ownership, mockup arithmetic…), APPROVED.
- `execution.md` — per-task triad audit trail (Implementer sonnet wrapper / Reviewer opus wrapper, author≠auditor); 1 review-round consumed (T-5 zero-total pin, restored with mutation evidence); 1 pre-review Leader adjudication (T-3 ToC-aware loading); 1 live field fix (T-6 responsive tracks). Environment interruptions recorded (2 transient worker network drops resumed in place; dev-DB VPN outage diagnosed to ETIMEDOUT and parked cleanly).
- Gates at close: program-overview 3 suites/156 green (all pinned tests intact), folder green, design-tokens sweep green, lint/tsc/dev-build clean; T-6 live checklist with screenshots (skeleton→final atomic, rail==rows 2/352, three-path navigation, CTA restore, contrast).
- `test-report.md`/`validation-report.md` absent — accepted (owner mandate; MRF precedent).

## Accepted follow-ups

- Complete-AoW live row NOT-RUN (no complete AoW exists on dev at 4.2% max program progress) — behavior pinned by OAH-TEST-4.
- Recorded divergences (by design): Reporting-tab cards include outcome tier + buckets (rail is HLO-tier); thin consumers keep the unfiltered rule (disclosed via title).
