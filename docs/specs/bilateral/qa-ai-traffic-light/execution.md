# Execution Log — QA AI traffic light on "Submit for review" (W3/Bilateral)

## Document Control

| Field | Value |
|---|---|
| Spec path | `docs/specs/bilateral/qa-ai-traffic-light/` |
| Module code | `BIL-QAI` |
| Approval Mode | gated (inherited) |
| Branch | `JuanGuzman-io/feature-p2-3150-bilateral` (== `origin/performance-refactor` at `d3d58a986` on 2026-09-16; worktree `orca/workspaces/onecgiar_pr/hermit`) |
| Target branch | `performance-refactor` |
| Ticket | P2-3698 (sub-task of P2-3150) |
| Leader | Claude Fable 5.1 (T1) · Implementer wrapper `akili-implementer` (sonnet, T2) · Reviewer wrapper `akili-reviewer` (opus, T3) |
| Budget (design.md) | 11 tasks · ~1 500 production LOC + ~1 300 test LOC · 3 review rounds. Tripwire: > 14 tasks, > 2 200 production LOC, > 5 review rounds |
| Created | 2026-09-16 |

### Pre-flight (2026-09-16)

| Check | Result |
|---|---|
| `requirements.md` / `design.md` approved | Yes (Document Control, owner, 2026-09-16) |
| `BIL-QAI-OQ-1` | Resolved → `docs/bilateral-module/integration-contracts.md` (T-1) |
| Contract handoff to Daniela | Already sent 2026-09-16 (vault note → *Handoff del contrato*). T-1 does **not** send Slack; it adds the repo-copy pointer to the vault note |
| Conflicting in-flight specs touching `bilateral-center.service.ts` / `submitForReview` | None in flight. Hits are archived/shipped specs (`archive/2026-09-14-bilateral--manual-create-drawer`, `bugfix/p2-3652-*`, `bilateral/webhook-external-platforms`, `notifications/bilateral-review-decision` → gets the supersession note in T-11) |
| Local environment (`docs/infrastructure.md` §6) | Worktree had no `node_modules` → `npm ci` run in `onecgiar-pr-server` (2026-09-16, exit 0). Client deps pending until the client wave. Node in shell is **v22.23.2** (contract says 20.x; nvm has v20.13.1 — recorded deviation, switch if a native module misbehaves). Docker daemon **off**. |
| Database for T-2 (`migration:generate` / `migration:check`) | **Blocked, probe-confirmed:** no `onecgiar-pr-server/.env` in the worktree; the main checkout `.env` points at a remote `DB_HOST` that is **not reachable** (TCP connect failed — likely VPN). No local MySQL on 3306. Decision pending from the owner at the first gate (connect VPN / copy `.env` into the worktree). T-2 stays `[ ]` until then; memory rule forbids hand-written migrations. |
| CodeGraph | `.codegraph/` exists but neither the `codegraph` CLI nor the MCP tools are available in this session → briefs omit graph lookups; workers explore by file |
| Human gate `T-11(a)` (proxy ceiling ≥ 65 s in TEST) | Owner-only (TEST environment). Must land **before `T-8`**. Flagged at the first gate |
| Skills for Implementers | `nestjs-expert`, `api-design-principles`, `error-handling-patterns`, `tdd`, `angular-developer`, `ui-ux-pro-max`, `cognitive-doc-design` — all present in the session |

### Wave plan

- **Wave 1 (2026-09-16):** `T-1 ∥ T-3` (disjoint files: docs+README+vault vs new `services/quality-assessment/` files; no shared build output). `T-2` held on the DB blocker.
- Next: `T-2` (after DB access) → `T-4 ∥ T-5` → `T-6` → `T-7 ∥ T-8` (T-8 also gated by `T-11(a)`) → `T-9` → `T-10` → `T-11(b)(c)`.

## Task Execution History

### `BIL-QAI-T-1` — Freeze contract v0.1 and document it in the repo

| Field | Value |
|---|---|
| Status | in progress (rework) |
| Date | 2026-09-16 |
| Skills assigned | `api-design-principles`, `cognitive-doc-design` (task list, no deviation) |
| Effort | attempt 1 `medium` → attempt 2 `high` |
| Leader decisions | Slack handoff **not** re-sent: the vault note already records the 2026-09-16 DM to Daniela; the Implementer only appended the repo-copy pointer to the vault note. CodeGraph omitted from the brief (tools unavailable). |

**Attempt 1** — Implementer (sonnet) · files: `docs/bilateral-module/integration-contracts.md` (+148, new `## Quality assessment (outbound)`), `onecgiar-pr-server/README.md` (+4), vault note (+1 bullet under *Handoff del contrato*). Verification: greps for the section heading, `contract_version`, `visibility`, both `BILATERAL_AI_QUALITY_*` keys — all present; disqualifier check inside the new section: only `request_id` (permitted) and the prose stating the prohibition. No `Not Done`.

Reviewer (opus) → **FAIL**. Field-by-field comparison against the vault contract: identical (only the placeholder gloss "…texto del nodo…" translated). One issue:

> 1. **Discovered Issue:** The two new bullets were inserted *between* the three `BULK_HANDOFF_*` keys and the paragraph that describes them, orphaning that paragraph's referent. `onecgiar-pr-server/README.md:45` now reads `Missing any of the three degrades POST /api/bilateral/center/handoff to a 503 … leaving them unset keeps PROD dark by design.` while sitting under a list of **five** bullets — two of which (`BILATERAL_AI_QUALITY_URL`, `BILATERAL_AI_QUALITY_TIMEOUT_MS`) have no 503 behaviour at all and are governed by the opposite rule stated two lines later. The most available reading contradicts `BIL-QAI-R-7`.
> **Violated Rule:** `.agents/reviewer.md` §2 *Stability & Integrity*; `tasks.md` T-1 DoD ("README keys listed" = prose accuracy); `requirements.md` `BIL-QAI-R-7` *Service not configured*.
> **Remediation:** (a) move the two bullets after the line-45 paragraph, or (b) scope the sentence to `BULK_HANDOFF_*`.

ADVISORY (recorded, not gating):
- RELIABILITY: `constraints.timeout_seconds: 60` is pinned while the bound is `BILATERAL_AI_QUALITY_TIMEOUT_MS`; the doc never says the former derives from the latter. → carried as a **forward pointer to `T-4`/`T-5`**: derive `timeout_seconds` from the env value in the builder/client.
- READABILITY: the contract never says when the AI itself should emit `evidence[].verdict = grey` (blocked/unreachable URL per Glossary).
- READABILITY: the bullet "any key ending in `_id`" is absolute and then carved out by `request_id` a paragraph later.
- RISK: "five keys, **in this order**" adds an ordering guarantee the frozen vault contract does not make.

**Attempt 2** — Implementer (sonnet, effort `high`) · file: `onecgiar-pr-server/README.md` only — the two `BILATERAL_AI_QUALITY_*` bullets moved below the "Missing any of the three…" paragraph, directly above the `MICROSERVICE_API_KEY` paragraph (Reviewer option (a)). Verification: `sed -n 36,50p README.md` shows trio → 503 sentence → new bullets → reuse paragraph; `git diff --stat` integration-contracts +148 (unchanged), README +5. No `Not Done`.

Reviewer (opus, same session) → **PASS**. Summary: "The README insertion point was moved so the `Missing any of the three …` paragraph again follows only the three `BULK_HANDOFF_*` keys, restoring its referent without touching any existing line; the contract copy in `integration-contracts.md` remains a faithful field-by-field transcription of vault contract v0.1 and satisfies `BIL-QAI-R-2`, `R-3`, `R-12` and the *Latency window* env keys." Nothing new; attempt-1 advisories stand.

| Field | Value |
|---|---|
| **Final status** | **PASS** (attempt 2 of 3) |
| Requirements covered | `BIL-QAI-R-2` (shape, five keys), `R-3` (evidence fields), `R-12` (optional score), NFR *Latency window* (env keys); `BIL-QAI-OQ-1` closed |
| Final verification | greps per work order green; Reviewer field-by-field match against the vault contract |
| Issues | README placement (attempt 1) — fixed |
| Forward pointers | → `T-4`/`T-5`: derive `constraints.timeout_seconds` from `BILATERAL_AI_QUALITY_TIMEOUT_MS` (advisory RELIABILITY) · → owner at gate: decide whether to drop "in this order" and add the AI-side grey trigger to the contract copy (advisories RISK/READABILITY — out of T-1 scope, not minted as tasks) |
| Commit | see below |

### `BIL-QAI-T-3` — Pure rules: grey, KP decision tree, content hash, outstanding flags (TDD)

| Field | Value |
|---|---|
| **Final status** | **PASS** (attempt 1 of 3) |
| Date | 2026-09-16 |
| Skills assigned | `tdd`, `nestjs-expert` (task list, no deviation) |
| Effort | `high` (business rules with literal expectations) |
| Files | `onecgiar-pr-server/src/api/bilateral/services/quality-assessment/bilateral-quality-rules.ts` (+358), `.../bilateral-quality-rules.spec.ts` (+615) — both new |
| Requirements covered | `BIL-QAI-R-3` (grey clause), `R-4` (grey never recolours), `R-6` (hash determinism), `R-8` (outstanding flags), `R-9` (all three scenarios), `AC-10`, `AC-11` (rule half) |

**Attempt 1** — Implementer (sonnet). Red → green: spec written first (import error), then implementation. Verification (verbatim): `npx jest --silent --reporters=summary --forceExit --testPathPattern="bilateral-quality-rules"` → `Test Suites: 1 passed · Tests: 61 passed`; coverage `bilateral-quality-rules.ts | 100 | 100 | 100 | 100`; `npx eslint "src/api/bilateral/services/quality-assessment/**/*.ts" --quiet` → exit 0; grep for `@nestjs`/`typeorm` imports → none. Note: `--collectCoverageFrom` must be relative to `src/` (jest `rootDir`). No `Not Done`.

Implementer judgment calls: (1) `is_melia = true` + Journal Article + agreeing rows → `green` (design §5 branch 2 has no MELIA guard); (2) `KpRuleInput.evidence_count?: number` as the KP evidence shape; (3) removed a defensive `payload.sections?.evidence ?? []`.

**Leader adjudication (1):** `green` is correct. The owner's original SQL tree in the vault note (*Árbol KP*) has no MELIA guard on the Journal Article branch; `requirements.md` R-9 *Fallthrough* "a KP that is MELIA" is shorthand for the non-JA MELIA case. Reviewer concurred.

Reviewer (opus) → **PASS**. Summary: "`bilateral-quality-rules.ts` implements the four pure functions exactly as `design.md` §5 specifies, the reason/rationale strings match the design and the frozen contract character-for-character, and the 61-test table-driven suite carries literal expectations (no tautology) including both falsifying inputs from the work order; purity, `@akili-spec` header, 100 % branch coverage and lint are satisfied."

ADVISORY (recorded, not gating — none minted as tasks):
- RELIABILITY/RISK: `applyGreyRule` joins AI `evidence[]` to payload evidence by **array position** and ignores `item.index`, which the contract carries as the join key. A reordered response grades the wrong item grey; a private item the AI omits never gets a grey entry, although R-3 says it "MUST be graded grey regardless of what the AI answers". Not gated because design §5 literally writes `evidence[i]`. → **Escalated to the owner at the gate as a design clarification** (design §5 *Grey rule*: match by `index`, append grey for missing items). If approved, folded into the brief of the task that consumes the rule (`T-6`).
- RESILIENCE: 100 % branch coverage does not exercise the length-mismatch path (`response.evidence` longer than payload evidence). One extra case would pin it.
- RISK: `contentHash(payload: Record<string, unknown>)` rejects a value typed as the exported `QualityPayload` (TS2345, no implicit index signature). → **Forward pointer to `T-4`**: widen to `QualityPayload | Record<string, unknown>` and add one spec case `contentHash(buildPayload())` (compile-fix inside T-4's `tsc --noEmit` DoD).
- RELIABILITY: with the defensive `?? []` removed, `applyGreyRule` throws on a 2xx body lacking `evidence`/`sections`/per-section arrays. → **Forward pointer to `T-5`**: the light schema check MUST treat `sections`, `evidence` and each section's `strengths`/`issues` arrays as required keys (else `malformed`), so the orchestrator never 500s (R-7).
- READABILITY: `is_isi`/`is_peer_reviewed` both `false` is reported as "do not match" although the rule is "both must be true"; `evidence_count` optional means a forgetful caller silently drops the KP evidence listing (→ `T-6` brief: always pass it). Spec file lacks the literal `@akili-spec` tag (cosmetic).

| Commit | see below |

### Gate 1 (2026-09-16) — owner decisions

- Continue with wave 2: `T-4 ∥ T-5` (both depend only on `T-1`/`T-3`; disjoint files under `services/quality-assessment/`; **neither edits `bilateral.module.ts`** — provider registration is `T-6`'s, entity registration `T-2`'s, to avoid a shared-file collision).
- `T-2` DB access: owner connects VPN; Leader copied the main checkout `.env` into the worktree (gitignored, verified). `T-2` starts once a TCP probe to `DB_HOST` succeeds.
- **Spec amendment approved:** `design.md` §5 *Grey rule* now matches response evidence by `index` and appends grey entries for omitted payload items (was the literal `evidence[i]`). `tasks.md` `T-6` description carries the rule update as approved scope. Correction closure sweep: forward grep `evidence[i]` / "by position" across the spec folder → only the design paragraph (amended) and this log; backward: `requirements.md` R-3/R-4 already state the intent, no citation asserts the old wording. No ADR overturned (not in the TRD).
- `T-11(a)` (proxy ≥ 65 s in TEST) remains owner-only; required before `T-8`.
- Contract-copy advisories ("in this order", AI-side grey trigger) surfaced to the owner; no change requested at this gate.

### Wave 2 (2026-09-16)

`T-4 ∥ T-5` spawned after gate 1; `T-2` joined once the TCP probe to `DB_HOST` succeeded (VPN up, `.env` copied). Three concurrent workers — within the 3–4 ceiling; disjoint files (`services/quality-assessment/{builder,mappers,fixtures}` · `services/quality-assessment/client` · `entities/ + repositories/ + migrations/ + bilateral.module.ts`), no shared build output. Effort `high` on all three (T-2 would merit `max` per the effort dial for migrations, but the tier is T2 — kept at `high`, review compensates with **parallel lens reviewers**, as the command requires for migration-touching tasks).

### `BIL-QAI-T-5` — AI HTTP client with timeout mapping and body-free logging

| Field | Value |
|---|---|
| Date | 2026-09-16 |
| Skills assigned | `nestjs-expert`, `error-handling-patterns` (task list, no deviation) |
| Effort | `high` |
| Files | `services/quality-assessment/bilateral-quality-assessment.client.ts` (+304), `.spec.ts` (+346), `fixtures/ai-response.v0.1.json` (+47) — all new |
| Forward pointers consumed | T-1 advisory → `timeoutMs()`/`timeoutSeconds()` exposed for `constraints.timeout_seconds` · T-3 advisory → schema check requires `sections` (five keys, `strengths`/`issues` arrays), `evidence[]` with `index`, verdict enums |

**Attempt 1** — Implementer (sonnet). Verification (verbatim excerpts): `npx jest … --testPathPattern="bilateral-quality-assessment.client"` → `Test Suites: 1 passed · Tests: 10 passed`; `grep -n "JSON.stringify" …client.ts` → none; eslint quiet clean; `tsc --noEmit` clean (Leader re-ran after T-2 saw a transient TS2339 in the spec mid-edit: clean). Public API: `timeoutMs()`, `timeoutSeconds()`, `isConfigured()`, `assess(payload, {resultId}) → AiClientOutcome` (`ok | not_configured | timeout | http_error | malformed`, never throws). One `Logger` line per call, template `event=bilateral_quality_assessment_client …`. Axios `timeout` + `Promise.race` guard at the same ms. No `Not Done`.

Review mode: **parallel lens reviewers** (security surface — secrets in logs): Reviewer A = RISK + RELIABILITY, Reviewer B = READABILITY + RESILIENCE, both gating on spec conformance.

### `BIL-QAI-T-2` — Entity, repository and pruned migration for `bilateral_quality_assessments`

| Field | Value |
|---|---|
| Date | 2026-09-16 |
| Skills assigned | `nestjs-expert` (task list, no deviation) |
| Effort | `high` (T2 tier cap; migration correctness compensated by parallel lens review) |
| Files | `entities/bilateral-quality-assessment.entity.ts` (+152), `repositories/bilateral-quality-assessment.repository.ts` (+34, new folder), `bilateral.module.ts` (+6: import, `forFeature`, provider), `src/migrations/1789566953005-BilateralQualityAssessments.ts` (+160) |

**Attempt 1** — Implementer (sonnet). Migration generated against the dev DB (VPN) and **pruned**: the generator emitted drift for **31 unrelated tables** (ai_review_*, bilateral_ai_*, clarisa_project*, clarisa_global_unit*, notifications, otp_challenges, result, result_*, results_*, template, user_notification_settings, users, webhook_delivery) — all removed; only `bilateral_quality_assessments` kept verbatim (1 `CREATE TABLE`, 2 FK `ALTER TABLE`; `down` = 2 drop FK, 2 drop index, 1 drop table). Verification (verbatim excerpts): `npm run migration:check` → `Pending: 1 — BilateralQualityAssessments1789566953005` (**expected**: generated, not run; the owner runs migrations — memory rule; the check is green once applied); `tsc --noEmit` clean for the task's files; `grep -c "createTable\|CREATE TABLE"` = 1; `grep "new Date"` in the entity → none; eslint clean. Deviations recorded by the Implementer: composite index `(result_id, created_at)` **ascending** (TypeORM `@Index` has no per-column DESC precedent in this repo; `findLatestByResultId` orders `created_at DESC, id DESC` explicitly); `version_id` plain nullable bigint, no FK (design §3.1 marks no FK); repository provided but not exported (sibling pattern). No `Not Done`.

Leader note: **`migration:check` green requires the owner to run the migration on the dev DB** (`npm run migration:run`) — raised at gate 2. Review mode: **parallel lens reviewers** (migration surface).

**T-5 attempt 1 verdicts.** Reviewer A (RISK+RELIABILITY) → **PASS** — every log path traced (two `Logger` statements, ids/status/elapsed only; `classify()` reads only `error.code`/`response.status`); privacy test is behavioural (the `http_error` case embeds the host in `error.message` and the test proves it never surfaces); timeout proven at 4999 ms unsettled / 5000 ms `timeout`; schema check meets the Leader constraints; fixture matches the contract Response block key-for-key.
Reviewer B (READABILITY+RESILIENCE) → **FAIL**:
> 1. Outbound body missing `contract_version` (and `constraints.timeout_seconds` only reachable via a collaborator that does not exist yet); `QualityPayload` has no `contract_version`. Violates contract Request block / design §4.5. Remediation: set at call time next to `request_id`, or assign to T-4/T-6 and record ownership.
> 2. `score` never validated nor normalised; JSDoc defers to "callers" that do not exist. `"68"` or `900` would reach `overall_score tinyint`. Violates the Leader constraint for T-5 backed by contract §*Optional score* and design §3.1. Remediation: `sanitizeScores` keeping only integers 0–100; two spec cases.

**Leader adjudication:** Issue 1 → **out of T-5 scope, owned by `T-4`** (builder sets `contract_version: '0.1'` and `constraints.timeout_seconds` from opts; `QualityPayload` gains `contract_version` — instruction sent to the T-4 Implementer mid-run). Design §5 *Hash* keeps `contract_version` inside the hash (a contract bump ⇒ fresh assessment) — Reviewer B's counter-argument recorded, design not changed. Issue 2 → **in scope** (Leader constraint in the brief). Rework attempt 2 (effort `xhigh`) on issue 2 only; one advisory typo ("four colours") folded in as a one-word fix in the same file.

ADVISORY (T-5, recorded, not gating): A: pre-existing leak in `bilateral-ai/services/bilateral-ai-text-mining.service.ts:37-57` (logs request/response bodies and host + axios message) — **outside this spec; needs its own ticket**; guard `setTimeout` never cleared on success (pending 60 s timer, hence `--forceExit`); Logger spy covers log/warn/error only and asserts `length > 0` not `5`; outbound request config (URL path, `X-API-Key`, Axios timeout) not asserted; `isValidAiResponse` predicate wider than its checks (`request_id`, `summary`, `comments`, `reason` unchecked) → **T-6/T-7 must not assume them**. B: `SECTION_KEYS` duplicated from the rules file (export + import instead); 2xx `text/html` maps to `malformed` but untested; no `maxContentLength` bound (design §8 ≤ 32 KB budget); observable not unsubscribed on guard timeout (bounded only by the equal Axios timeout — comment it); default-timeout and trailing-slash-trim branches untested; `http_status?` offered on `timeout`/`not_configured` where always absent. → `T-6` brief: pass one `requestId` end-to-end (the client currently mints its own and overwrites the builder's).

**T-5 attempt 2** — Implementer (sonnet, effort `xhigh`). Files: client + spec only. Added `sanitizeScore`/`sanitizeScores` (integer 0–100 kept, anything else → `null`, new object, applied after `isValidAiResponse` and before `outcome: 'ok'`), removed the "callers treat it as absent" JSDoc, "four colours" → "three". Spec +3 cases (68 kept; `"68"` → null; section `900` → null; outcome `ok` in all). Mechanical compile fix: `buildPayload()` gained `contract_version: '0.1'` because T-4 made the field required on `QualityPayload` mid-run. Verification (verbatim): `Tests: 13 passed`; `grep JSON.stringify` → none; eslint clean; `tsc | grep client` → nothing. Re-review sent to Reviewer B with the exact attempt-1 → attempt-2 delta.

Reviewer B (re-review of the delta) → **PASS**. Summary: "Issue 2 is fixed at the correct boundary with real behavioural coverage (valid score kept, string and out-of-range sanitized to `null`, outcome stays `ok` in all three), and the 'four colours' advisory is applied; Issue 1 is out of scope per the Leader's adjudication to T-4." New advisories: `sanitizeScores` rebuilds `sections` from the five known keys, so an **unknown sixth section key is dropped on the `ok` path** (matches T-9's "unknown key ignored" rule — noted here so `T-6`/`T-9` do not rediscover it); no test asserts a valid *section* score survives (fixture `general_information.score: 91` unasserted).

| Field | Value |
|---|---|
| **Final status** | **PASS** (attempt 2 of 3; Reviewer A PASS on attempt 1, Reviewer B PASS on attempt 2) |
| Requirements covered | `BIL-QAI-R-7` *Timeout* (client outcome) and *Service not configured*; NFR *Privacy / secrets*; `BIL-QAI-AC-15` |
| Final verification | `Tests: 13 passed`; no `JSON.stringify`; eslint clean; tsc clean for the client files |
| Decisions | `score` sanitized to `null` (not key-dropped) — matches `number \| null` types and nullable columns · `contract_version`/`constraints` are the builder's (T-4), client sends `{...payload, request_id}` |
| Review rounds consumed (budget) | 1 extra round (T-1 and T-5 each needed one rework) — cumulative rework rounds: 2 of the 5-round tripwire |
| Commit | chained after T-4 (spec depends on `QualityPayload.contract_version` introduced by T-4) |

**T-2 attempt 1 — Reviewer A (RELIABILITY+RISK) → PASS.** All 19 columns match design §3.1 in type/length/nullability/order; `created_at` SQL-defaulted in entity and DDL; FK types match referenced PKs (`result.id` bigint, `users.id` int); `ON DELETE NO ACTION` on both FKs (correct for audit history); `down` reverses `up` with byte-identical names in the right order; exactly one table altered; ascending composite index judged conformant (MySQL serves `ORDER BY created_at DESC` via backward scan; a DESC index would re-open generator drift). `migration:check` `Pending: 1` carried as the open gate for the server group close (owner runs it).
ADVISORY (A): `down` has no `IF EXISTS` guards — a partially applied `up` would leave the table after a failed revert; sibling migration uses a single `DROP TABLE IF EXISTS` (more robust) · `sections`/`evidence` are `json NOT NULL` with no default → **T-6 must insert `sections: {}` / `evidence: []` explicitly on the `running` row** (else `ER_NO_DEFAULT_FOR_FIELD`), and the TS type `Record<QualitySectionKey, …>` forces a cast for that row · **`decided_at` must be stamped from SQL (`NOW()`), never a JS `Date`** (mysql2 local-time rule) → T-7 brief · `migration:revert` destroys the audit trail (disclosed in header; design §11 accepts) · `IDX_…_result_id` redundant with the composite index (kept: design asks for it).

**T-2 attempt 1 — Reviewer B (READABILITY+RESILIENCE) → PASS.** Column set 1:1 with §3.1; unions match spec value lists verbatim; verdict/section/evidence types imported from the rules file; `@akili-spec` on entity and migration; module registration mirrors the `BilateralHandoffCode` sibling, three additive hunks only; `findLatestByResultId` correct and T-6's other reads (latest non-running, `FOR UPDATE` lock) buildable on the inherited surface.
ADVISORY (B): **`bigint` columns (`id`, `result_id`, `version_id`) come back from mysql2 as strings** — T-7's "same result / same assessment id" checks must coerce with `Number(...)` on both sides (`where` comparisons are unaffected); `had_outstanding_flags` is `tinyint` → test truthiness, never `=== true` · `sections`/`evidence` `json NOT NULL` need `{}`/`[]` placeholders on the `running`/`not_configured` inserts (or a follow-up making them nullable; decide in T-6) · status/decision literals: consider `const` value maps or string enums before T-6 spreads them · migration header says "never updated" while the entity JSDoc correctly says "after `decided_at` is stamped" — align wording (cosmetic) · entity JSDoc does not name `BaseEntity`/`Auditable` explicitly · `IDX_…_result_id` redundant with the composite (kept per design).

| Field | Value |
|---|---|
| **Final status** | **PASS** (attempt 1 of 3; both lens Reviewers PASS) |
| Requirements covered | `BIL-QAI-R-8` (persistence fields); NFR *Backwards compatibility* (additive table) |
| Final verification | `tsc --noEmit` clean; `createTable` count 1; no `new Date`; eslint clean; `migration:check` → `Pending: 1` (this migration) — **open gate: owner runs `npm run migration:run` on the dev DB before the server group closes** |
| Decisions | Ascending composite index accepted (backward scan; DESC would re-open generator drift) · `version_id` no FK · repository not exported |
| Forward pointers | → `T-6`: insert `sections: {}` / `evidence: []` on non-terminal rows; pass `evidence_count` to `evaluateKpRule`; single `requestId` end-to-end; unknown sixth section key is dropped by the client · → `T-7`: `decided_at` from SQL `NOW()`; `Number(...)` coercion on bigint comparisons; truthiness on `had_outstanding_flags` |
| Commit | see below |

### `BIL-QAI-T-4` — Definitions-only payload builder with per-type fixtures

| Field | Value |
|---|---|
| Date | 2026-09-16 |
| Skills assigned | `nestjs-expert`, `tdd` (task list, no deviation) |
| Effort | `high` |
| Files | `services/quality-assessment/bilateral-quality-payload.builder.ts` (+88) + `.spec.ts` (+335); `mappers/{result-header,general-information,contributors-and-partners,geographic-location,evidence,type-specific}.mapper.ts`, `mappers/strip-identifiers.ts`, `mappers/errors.ts`; six `fixtures/*.fixture.json`; Leader-authorised delta in `bilateral-quality-rules.ts` (`QualityPayload.contract_version: string`; `contentHash` accepts `QualityPayload`) and `bilateral-quality-rules.spec.ts` (+1 hash case on a built payload) |
| Leader mid-run instruction | `contract_version: '0.1'` + `constraints.timeout_seconds` set by the builder (T-5 Reviewer B issue 1 → owned here); `QualityPayload` gains `contract_version` |

**Attempt 1** — Implementer (sonnet). Public API: `build(resultId, {requestId, timeoutSeconds})` (injects `BilateralService` + `ResultsService`) and pure `project(detail, formDetail, opts)`. Six fixtures: knowledge product (6), capacity sharing (5), innovation development (7), innovation use (2), policy change (1), other output (8 → `{type, fields: {}}` branch). Fallbacks declared: ToC node title only in `findOne`'s `toc_mappings[].title` — missing ⇒ `UnresolvableLabelError` (tested); **ToC `indicator` label not exposed by either read ⇒ `null` (declared open gap)**; lead/contributing project split falls back to all-contributing when `getBilateralResultById.contributingProjects` is empty. Verification (verbatim): `Test Suites: 2 passed · Tests: 105 passed`; `tsc --noEmit` clean; eslint clean. `bilateral.module.ts` untouched. No `Not Done`; the indicator gap is listed under fallbacks — Leader treats it as **scope still owed unless the Reviewer rules it an acceptable documented degradation**.

Reviewer (opus) → **FAIL** (attempt 1). Verified fixture keys against the emitting code. Accepted: denylist pass + literal regex assertions; evidence truth table; `contract_version` in hash, `request_id`/`constraints` stripped; module untouched; rules delta as authorised; most fixture blocks realistic. Issues (verbatim summary of each; full text passed to attempt 2):
> 1. `theory_of_change.contribution` reads `formDetail.tocMetadata.result_toc_result.result_toc_results` — one level too deep; `getBilateralResultById` (`results.service.ts:3708`) already unwraps to `tocMetadata.result_toc_results`. Always `null` in production; all six fixtures written against the invented nesting. Violates design §5/DD-2, contract Request block.
> 2. `type_specific.fields['Innovation use level']` reads `detail.results_innovations_use_object…`, which `enrichBilateralResultResponse` deletes (`bilateral.service.ts:3672`); the label lives in `detail.innovation_use_summary.innovation_use_level.name`. Violates R-2 THEN clause.
> 3. Innovation-use `formDetail.resultTypeResponse` fixture models a fallback branch that never runs; `getBilateralInnovationUseData` returns `[{actors, organizations, measures, investment_partners, investment_projects}]`, not a flat row — every count becomes `null`. Violates R-2 / AC-2.
> 4. `theory_of_change.indicator` hard-coded `null`; the label exists (`toc_results_indicators.indicator_description`, read by `ResultsService.getTocMetadata` `results.service.ts:3257-3313`; id in `result_toc_results[].indicators[].toc_results_indicator_id`). Violates R-2 ("indicator title"), contract, DD-2 "mapper fallback to a repository", Leader constraint (never emit an empty label silently).
> 5. Owner initiative without active ToC mapping ⇒ `getTocMappingsByResultId` yields `[{toc_result_id: null, title: null}]` ⇒ `UnresolvableLabelError` ⇒ whole build throws; must serialise as the empty `theory_of_change` branch. Violates R-2 AND clause (empty sections present as empty objects).
ADVISORY (T-4 attempt 1): `build()` ignores `status` on `getBilateralResultById` (`{response: {}, status: 404}` silently degrades labels to null) · `prms_repository + public` evidence row never exercised · `YEAR_KEY_PATTERN = /year/i` is a regex escape hatch in an "explicit allowlist" · denylist only drops integers (string ids pass) · `ID_KEY_PATTERN` scoped to `{result, sections}` (correct — `request_id` allowed) → reconcile `tasks.md` verification wording at archive.

**Leader adjudication:** all five are in scope (R-2 / DD-2 / contract). Rework attempt 2, effort `xhigh`. Indicator fallback (issue 4) via the already-injected `ResultsService.getTocMetadata` or `TocResultsRepository` if resolvable **without** editing `bilateral.module.ts` (T-6 owns registration; if a new provider is unavoidable, report it as `Not Done` for T-6 to wire).

**Budget tripwire check (after T-4 attempt 1):** tasks 11/11 planned (no new tasks minted) · rework rounds consumed **3** (T-1, T-5, T-4) of the 5-round tripwire · production LOC so far ≈ 1 100 (rules 358 · client 348 · builder+mappers ≈ 700 · entity/repo/migration ≈ 350) against the 2 200 tripwire — the server half is tracking above the ~750 server estimate; will re-measure after T-6/T-7 and escalate if the 2 200 line is threatened.

**T-4 attempt 2** — Implementer (sonnet, `xhigh`). All five issues fixed with emitting-line citations: (1) `tocMetadata.result_toc_results` flat shape (`results.service.ts:3708`, `results-toc-results.service.ts:710-771`); (2) innovation-use level from `innovation_use_summary.innovation_use_level.name` (`bilateral.service.ts:3209-3357`); (3) counts summed over `resultTypeResponse[0].actors`, unit/quantity from `measures[0]` (`innovation-use.service.ts:1128-1160`), `To be determined` from `innov_use_to_be_determined`; (4) indicator label via a new `IndicatorDescriptionResolver` interface + `INDICATOR_DESCRIPTION_RESOLVER` token, resolved in `build()` and passed as a lookup into the pure `project()`; missing entry ⇒ `UnresolvableLabelError`; (5) null-object mapping (`toc_result_id == null`) ⇒ empty `theory_of_change` branch. Advisories folded: `BilateralResultFormReadError` on form read `status >= 400`; public repository evidence row in the KP fixture. Verification (verbatim): `Tests: 111 passed`; `tsc --noEmit` clean; eslint clean. Judgment calls: actor counts summed across rows; first `measures` row only.
**`Not Done / Assumptions` (verbatim):** "Indicator-description provider is still unwired. `BilateralQualityPayloadBuilder` depends on `IndicatorDescriptionResolver` via `@Optional() @Inject(INDICATOR_DESCRIPTION_RESOLVER)` so `bilateral.module.ts` stays untouched per the Leader's constraint. No concrete implementation exists yet — until T-6 registers one, `build()` runs with `indicatorDescriptions = {}`, so any result whose ToC mapping carries an indicator will throw `UnresolvableLabelError` at runtime."
**Leader decision:** scope still owed by T-4, not transferable — the module ban was a concurrency guard and no other worker is active. Ban lifted for T-4; the same Implementer continues (attempt 2, remainder): concrete `TocIndicatorDescriptionResolver`, registration of the resolver and of `BilateralQualityPayloadBuilder` in `bilateral.module.ts`, `@Optional()` removed, resolver unit tests, DI-cycle statement. Reviewer spawn deferred until the remainder lands.

**T-4 attempt 2 (remainder)** — same Implementer. New: `toc-indicator-description-resolver.service.ts` (+73; `DataSource.query` on `${DB_TOC}.toc_results_indicators`, mirroring `getTocMetadata`; `TocResultsRepository` not exported by `ResultsTocResultsModule`, and importing `TocResultsModule` would drag MQAP/SharePoint/forwardRefs) + spec (+86: empty ids ⇒ no query; keyed de-duplicated map; missing row ⇒ absent key); `@Optional()` removed from the builder; `bilateral.module.ts` +3 providers (`BilateralQualityPayloadBuilder`, `TocIndicatorDescriptionResolver`, `{ provide: INDICATOR_DESCRIPTION_RESOLVER, useExisting }`), zero new `imports:`. DI check: no `BilateralCenterService → builder` edge; `app.module.spec.ts` 2/2. Verification (verbatim): `Tests: 111 passed` (115 with the resolver suite); `tsc --noEmit` clean; eslint clean. `Not Done` cleared. Re-review sent to the T-4 Reviewer with the full 22-file diff.

Reviewer (re-review of the full attempt-2 diff) → **PASS**. Summary: "All five FAIL issues and both folded advisories are fixed at the right key paths, each verified against the code that actually emits them rather than against the fixture; the new indicator resolver is correctly parameterised, `is_active`-filtered, log-free, and wired through a token alias without adding a module import or weakening any fixture assertion." Blob hashes confirm the untouched mappers are byte-identical to attempt 1.
ADVISORY (T-4 attempt 2): the doc block in `indicator-description-resolver.ts` still says the module is untouched / T-6 registers / `@Optional()` — stale, trim before archive · `mapInnovationUse` sums gender columns across actors; the innovation-use guide says `how_many` is authoritative for totals (rows without disaggregation would send four zeros) → **surface to the owner at the T-11 fixture review**; consider a `Number of people using the innovation` field from `how_many` · `BilateralResultFormReadError` interpolates the upstream message → **T-6 must map it to a generic 4xx and never log/surface it** · no fixture carries `indicators[]`, so the owner's T-11 review never sees a populated indicator label — add to one fixture at T-11.

| Field | Value |
|---|---|
| **Final status** | **PASS** (attempt 2 of 3; attempt 2 included a Leader-directed remainder after a `Not Done`) |
| Requirements covered | `BIL-QAI-R-2` (all three clauses; hash from persisted data), `R-3` (private/public evidence), `AC-2`, `AC-3` |
| Final verification | `Tests: 111 passed` (+4 resolver); `tsc --noEmit` clean; eslint clean; `app.module.spec.ts` 2/2 |
| Decisions | Indicator label resolved via a token-bound `TocIndicatorDescriptionResolver` (`DataSource.query`, mirrors `getTocMetadata`) because `TocResultsRepository` is not exported and `TocResultsModule` drags heavy imports · builder + resolver registered in `bilateral.module.ts` by T-4 (ban lifted; T-6 still registers orchestrator/client) · `contract_version` inside the hash · `ID_KEY_PATTERN` scoped to `{result, sections}` (request_id permitted) — reconcile `tasks.md` wording at archive |
| Rework rounds (budget) | cumulative **3** of 5 (T-1, T-5, T-4) |
| Commit | see below |

### Gate 2 (2026-09-16) — owner decisions

- Wave 2 landed: `T-2`, `T-4`, `T-5` PASS and committed (`d214f1c0a`, `6db6dd8ac`, `50483506b`). Integrated check on the quiet tree: `--testPathPattern="quality-assessment"` → 4 suites / 128 tests green; `tsc --noEmit` clean.
- **Pre-existing lint debt:** `npx eslint "src/api/bilateral/**/*.ts" --quiet` reported 56 prettier errors in `bilateral-center.service.ts`, `bilateral-center.service.spec.ts`, `bilateral-center.controller.spec.ts` — files untouched by this spec so far (inherited from `performance-refactor`). Leader applied `eslint --fix` (formatting only; `bilateral-center` suites re-run green) and committed it as a separate style commit so `T-6`'s functional diff stays reviewable. No production code authored by the Leader.
- Owner: **continue with `T-6`** (single Implementer; `T-7` after it) · owner runs `npm run migration:run` on the dev DB and reports back (open gate for the server group) · **`T-11(a)` will be done by the owner before `T-8`**.
- Owner informed of the T-11 fixture-review items (innovation-use totals `how_many` vs gender sum; add `indicators[]` to one fixture).

## Pivot Record: contract v0.2 (triggered 2026-09-16, before `BIL-QAI-T-6`)

**Trigger.** Daniela Gómez's consolidated review of contract v0.1 (Slack DM, 2026-09-16 09:35), read at the owner's request at gate 2. Four asks: (1) per-type `type_specific` field enumeration with **stable labels, strictly validated**, and disaggregated counts as **objects** (`{total, female, male, non_binary, unknown}`, `{long_term, short_term}`), `Delivery method` also feeding the geography rule; (2) optional `impact_areas[] {name, score, subcomponent}` + confirmation that evidence `tags` link evidence to impact areas; (3) `Innovation Developer` as its own `type_specific` field; (4) response additions `status: completed|partial|unavailable` + `degraded_reason`, and **`grey` allowed at section level** (not counted in the overall).

**Why this is a spec problem, not an implementation FAIL.** v0.1 (frozen in `T-1`) left `type_specific.fields` as free label→value pairs, defined grey for evidence items only, and had no `impact_areas`/`status`/`degraded_reason`. T-4 implemented v0.1 faithfully (flat labelled fields, e.g. "Number of women trained"); T-5 rejects a grey section as `malformed`. Continuing into `T-6`/`T-7` would persist a shape that v0.2 changes.

**Owner decisions (gate 2b, 2026-09-16).** Adopt **v0.2 now**. `impact_areas` **is sent** (optional for bilateral results; **absent ⇒ not grey, no penalty** — to be stated in the contract and told to Daniela). `Innovation developers`: send it **and** add the field to the bilateral result form (Program reviewers require it) — new task. Owner sends the Slack reply to Daniela from the Leader's draft.

**Direction.** Contract v0.2 in `integration-contracts.md` (per-type field schema with fixed labels; disaggregated count objects; `impact_areas`; response `status`/`degraded_reason`; section-level grey; absent-optional-block semantics); `design.md` §3.1 (+ `ai_status`, `degraded_reason` nullable columns → additive migration), §4.5, §5 (grey rule: section grey passes through; `hasOutstandingFlags` unchanged), §6 (dialog renders grey sections, `partial` notice); `requirements.md` R-2/R-4/R-8 wording; `tasks.md`: `T-2b` migration, `T-4b` type-specific + impact-areas mappers, `T-5b` schema widening, `T-12` innovation-developers field in the bilateral form, and adjustments to `T-6`/`T-7`/`T-9` descriptions. No TRD ADR overturned (DD-1..DD-10 stand; DD-2 extended). Scout dispatched for data-source facts before drafting; owner approval required on the amended docs before execution resumes.

- 2026-09-16: reply to Daniela sent by the Leader at the owner's explicit instruction (Slack DM `p1789570149572599`): v0.2 accepted; counts as objects; one training term per result; Innovation Use disaggregation women/men/youth; `impact_areas` optional and **absent ⇒ not grey, no penalty**; evidence tags = impact-area link; Innovation Developer as own field; `status`/`degraded_reason` + section-level grey accepted. Vault note updated.

**Scout facts (Explore, 2026-09-16) feeding the v0.2 drafting.** Innovation developers: column exists and both reads expose it, but the **bilateral form removed the field** (`type-innovation-dev.component.html:44-46`) and writes it silently from the Lead contact (`type-innovation-dev.component.ts:277`; ingest handler `innovation-development.handler.ts:56-75` does the same) — the exact "check that can never fail" Daniela described; standard form and reviewer drawer show it optional. Innovation use: actor type name; counts women/men/women_youth/men_youth/how_many — **no non-binary/unknown**; measures list; USD only via budgets (`kind_cash` ×3 sources). Capacity sharing: **one training term per result**; counts female/male/non_binary/unknown (no total column); delivery method; implementing orgs. Policy change: type/stage/implementing orgs/USD amount+status. Impact areas: bilateral results carry per-pillar tag levels (`findOne.dac_scores.<pillar>.tag_title`, sub-components when Principal; `getBilateralResultById.impactAreaScores[]`). Evidence tags: Gender, Youth, Nutrition, Environment & biodiversity, Poverty — **no "Climate" flag** (v0.1 example was wrong). Type-specific section saves go through legacy `api/results/summary/*` endpoints via `BilateralApiService`.

**Leader design decisions for v0.2 (handed to the drafting agent, T1/opus).** Fixed English labels per type frozen in one exported constant; count objects (`{total, female, male, non_binary, unknown}`; innovation use `{total, women, men, women_youth, men_youth}`); one training term as a string; `"Innovation developers": null` when empty, never substituted; `impact_areas[] {name, score, subcomponents[]}` as a root sibling of `sections`, optional, absent ⇒ no grey/no penalty, inside the hash; response `status`/`degraded_reason` required in v0.2, section grey allowed, overall stays 3-colour; PRMS row: `completed|partial` ⇒ `status = completed` + new `ai_status`/`degraded_reason` nullable columns (additive migration), AI `unavailable` ⇒ `unavailable_reason = 'ai_unavailable'`; dialog renders grey rows + partial notice. New tasks `T-1b`, `T-2b`, `T-4b`, `T-5b`, `T-12` (Innovation developers restored in the bilateral form); T-6/T-7/T-9/T-11 descriptions adjusted. **Budget tripwire tripped by design: 16 tasks > 14** → re-baselined budget (16 tasks · ~1 900 prod LOC · 5 review rounds; new tripwire > 19 / > 2 600 / > 7) submitted for owner approval together with the pivot. The contract copy (`integration-contracts.md`) is updated by `T-1b` through the triad, not by the drafter.

**v0.2 amendment drafted (T1/opus drafting agent, Leader-reviewed 2026-09-16).** `requirements.md` (+145: R-2/R-3/R-4/R-7/R-8 amended, new R-15, AC-16..18, glossary), `design.md` (+141: §3.1 `ai_status`/`degraded_reason`/`ai_unavailable`, §4.5 rewritten to v0.2 with the per-type field table and status mapping, §5/§6/§9/§10/§11, DD-11, DD-12, GAP-6 Climate pillar unlinkable by tag, GAP-7 no non-binary/unknown in innovation use, budget re-sized), `tasks.md` (+174: T-1b, T-2b, T-4b, T-5b, T-12 with full field structure; T-6/T-7/T-9/T-11 adjusted; graph, test plan, PR strategy (3 PRs), budget 16 tasks). Correction-closure sweep run by the drafter: forward hits fixed inside the three files; remaining `v0.1`/`"Climate"`/`in this order` strings are in `integration-contracts.md` (T-1b's deliverable), historical `execution.md` lines, code owned by T-4b/T-5b, and `proposal.md` (intent doc, left). Drafter judgment calls recorded: `subcomponents[]` plural (say so to Daniela in T-1b); `degraded_reason` truncation/URL-stripping in the client (T-5b); `partial` ⇒ row `completed` ⇒ submits as `submitted_anyway`; ingest-handler lead-contact fallback out of T-12 scope (§7 follow-up); T-12 as its own PR. **Status: pending owner approval** — execution resumes only after it.

### Gate 3 (2026-09-16) — v0.2 approved, T-2 migration applied

- Owner ran `npm run migration:run` from the worktree: `BilateralQualityAssessments1789566953005` executed successfully on the dev DB (`CREATE TABLE` + 2 FKs, row inserted in `migrations`; 487 → 488 executed). `migration:check` gate for `T-2` closed; `T-2b` can generate against the real state.
- Owner approved the v0.2 amendment and the re-baselined budget (16 tasks · ~1 900 prod LOC · 5 review rounds; tripwire > 19 / > 2 600 / > 7) — "Continúa" after reviewing the migration; Document Control of the three docs updated to *approved*.
- Client `npm ci` done in the worktree (for `T-12`).
- **Wave 3:** `T-1b ∥ T-2b ∥ T-12` — disjoint files (`docs/bilateral-module/integration-contracts.md` · entity + new migration · client `type-innovation-dev` component), no shared build output between server and client packages. Effort: T-1b `high` (contract surface), T-2b `high` (migration), T-12 `high` (behavioural change in a form + payload assertion). Then `T-4b ∥ T-5b`, then `T-6`.
- `T-11(a)` still owner-only before `T-8`.

- Note for `T-12`: the field's removal was a **product decision** recorded in the component (`type-innovation-dev.component.html:44-46`: "the Lead contact person IS the developer — Nicoleta Trifa via Ángel Jarrín, 2026-09-03"). The owner's 2026-09-16 instruction to restore it (Program reviewers require it; the substitution defeats the QA check) supersedes it for this spec; **the owner should tell Ángel** (PO-message rule: short). T-12's CLAUDE.md update must record both decisions, not erase the first.

### `BIL-QAI-T-2b` — Additive migration `AddAiStatusToBilateralQualityAssessments`

| Field | Value |
|---|---|
| Date | 2026-09-16 |
| Skills | `nestjs-expert` (task list) · Effort `high` |
| Files | `entities/bilateral-quality-assessment.entity.ts` (+2 columns, `ai_unavailable` reason, exported `BilateralQualityAssessmentAiStatus`), `src/migrations/1789571865104-AddAiStatusToBilateralQualityAssessments.ts` (new, 95 lines) |

**Attempt 1** — Implementer (sonnet). Generated against the dev DB (T-2 applied) and pruned: 29 drift tables removed, `up` = 2 `ADD COLUMN`, `down` = 2 `DROP COLUMN`, `CREATE TABLE` count 0, one table named. Verification (verbatim): `migration:check` → `Pending: 1 — AddAiStatusToBilateralQualityAssessments1789571865104` (expected, owner runs); `tsc --noEmit` clean; eslint clean; `jest "quality-assessment"` 4 suites / 128 tests. Deviation: the work order's `grep -c "ADD COLUMN\|ADD \`"` line has a shell-quoting artifact (backtick in double quotes) — Implementer re-verified with a safe pattern; **Leader: fix the recipe in `tasks.md` at archive**. No `Not Done` beyond run/commit by design. Review mode: parallel lens reviewers (migration surface).

### Wave 3 — parked 2026-09-16 (session rate limit)

**Environment blocker found and fixed (applies to every client task — `T-8`, `T-9`, `T-10`, `T-12`):** a fresh worktree has **no `onecgiar-pr-client/src/environments/environment.ts`** (gitignored, per-environment — memory rule `project_env_file_not_travels_in_merge`). Every client jest run therefore dies at import with the misleading `Cannot find module '../../../../environments/environment' from 'src/app/shared/services/api/bilateral-api.service.ts'` and reports `Tests: 0 total`, which reads like a broken spec rather than a missing file. Leader copied `environment.ts` + `environment.prod.ts` from the main checkout into the worktree (gitignored, verified). After the copy the same suite runs: **69 tests, 67 passed, 2 failed**. Record this before diagnosing any client suite as red.

**Session rate limit (429, resets 13:00 America/Bogota).** Three agents terminated mid-flight: the `T-12` Implementer and **both** `T-2b` lens Reviewers. No subagent can be spawned until the reset. Nothing was inlined by the Leader in their place — the Reviewer may never run inline (`author ≠ auditor`), and the Implementer fallback needs owner approval.

| Task | State | What exists | What is owed |
|---|---|---|---|
| `T-1b` | `[~]` **Implementer PASS, unreviewed** | `docs/bilateral-module/integration-contracts.md` § *Quality assessment (outbound)* rewritten to v0.2, lines 495–767 (+134/−10). Verification (verbatim): all four v0.2 greps present; no v0.1 leftovers inside the section; five contract tables; no `README.md` diff. Message draft for Daniela produced (below). | Reviewer audit against `design.md` §4.5 |
| `T-2b` | `[~]` **Implementer PASS, unreviewed** | Entity + `1789571865104-AddAiStatusToBilateralQualityAssessments.ts` (see the entry above). | **Both** lens Reviewers (died at spawn); owner's `migration:run` |
| `T-12` | `[~]` **parked mid-flight** | Field restored (`type-innovation-dev.component.html:44-53`), prefill guarded (`…component.ts:284` returns when a stored value exists), `buildPayload` sends `innovation_developers: this.body.innovation_developers?.trim() \|\| null` (`:301`), CLAUDE.md updated, spec largely rewritten. | **2 failing tests**, both under *rendered template › Innovation developers field rendering (BIL-QAI-R-15)*: "renders prefilled with the lead contact person when the stored value is empty" and "renders the stored value, NOT the lead contact person, when the stored value is not empty". The agent died writing the payload-level assertions the work order's disqualifier demands. Then: Reviewer. |

Committed as WIP so nothing is lost; `tasks.md` carries `[~]` for all three. `/akili-resume` picks up from here.

**`T-1b` deliverable 2 — message draft for Daniela (owner sends; not sent by the Leader):**

> Dani, cerramos el contrato v0.2. Etiquetas por tipo (`type_specific.fields`, texto exacto): **Policy change** `Policy type`, `Policy stage`, `Implementing organizations`, `USD amount` · **Innovation use** `User types`, `Number of people using`, `Other quantitative measures`, `Investment (USD)` · **Capacity sharing** `Number of people trained`, `Length of training`, `Delivery method`, `Implementing organizations` · **Innovation development** `Innovation typology`, `Readiness level`, `Innovation developers` · **Knowledge product** sin cambios, nunca se envía · **Other output / Other outcome** `fields: {}`.
> Semántica: `Length of training` es un solo valor por resultado, no por persona · `Delivery method` alimenta la regla de geografía (si es virtual, no penalicen una Geographic location vacía) · Innovation use **no tiene** conteos non-binary/unknown (solo women/men/youth) y una fila sin desagregación suma solo a `total`, así que las partes pueden sumar menos que el total sin ser sub-reporte · `Investment (USD)` = suma de `kind_cash` de las tres fuentes de presupuesto, `null` si no hay ninguna · `impact_areas` va como hermano de `sections` y su llave es `subcomponents` **en plural** · vocabulario cerrado de evidence tags: Gender · Youth · Nutrition · Environment & biodiversity · Poverty, **no hay tag de Climate** (el pilar sí puede aparecer en `impact_areas`) · `status` y `degraded_reason` obligatorios en la respuesta · `sections.<key>.verdict` acepta `grey` y no lo tratamos como malformed.

**`T-1b` Implementer deviation to check at review:** to satisfy its own verification grep (which bans the literal `"Climate"` inside the section), the Implementer rewrote the two places where that word would have appeared inside a quoted JSON string — using unquoted prose and swapping the example pillar for `Youth`. The documented facts are intact, but the **grep recipe in `tasks.md` `T-1b` is over-broad** (it cannot tell "mentions" from "contains") and should be narrowed at archive, together with the `grep -c "ADD COLUMN\|ADD \`"` quoting artifact `T-2b` hit.

### Wave 3 resumed (2026-09-16, after the rate-limit reset)

Spawned: `T-12` Implementer (resume, effort `xhigh`), `T-1b` Reviewer (lens checklist), `T-2b` Reviewers ×2 (parallel lens — migration surface), `T-12` Reviewer. Four concurrent at the ceiling; all four tasks touch disjoint files.

**`T-12` resume — Implementer report.** Only `type-innovation-dev.component.spec.ts` changed since the WIP commit `41db9be7a`; the component, template and guide were already correct. **Root cause of the two red tests: a test bug, not a component bug** — `app-pr-textarea` is a `ControlValueAccessor`, and standalone `[(ngModel)]` writes its initial value into the CVA inside a `resolvedPromise.then(...)` microtask (`NgModel._updateValue`), while the synchronous `writeValue()` during `setUpControl()` only pushes the new `FormControl`'s `null`. Both tests read `componentInstance.value` after a single synchronous `detectChanges()` and caught it mid-flight. Fix: a `renderAndSettle()` helper (`render(); await fixture.whenStable(); fixture.detectChanges();`) mirroring the package's existing `settleFocus()` pattern in `overview-controls.component.spec.ts`; both tests made async. The payload-level assertions the disqualifier demands were already in place from the parked work (`describe('innovation_developers save contract (BIL-QAI-R-15)')`: cleared ⇒ `null` never the lead contact · value trimmed · changing the lead contact after load never changes the saved payload).
Verification (verbatim): `Tests: 69 passed` (scoped) · `Test Suites: 46 passed, Tests: 1470 passed` (whole `src/app/pages/bilateral`) · `All files pass linting.` No `Not Done`.
**(e) server finding (read-only):** `summary.service.ts:605` destructures `innovation_developers`; `:628` (update) and `:659` (create) assign it **unconditionally**, not gated on truthiness. The client always sends the key (string or `null`, never omitted) and TypeORM `save()` skips only `undefined`. **The legacy endpoint persists an explicit `null` — a cleared value round-trips.** No escalation, no server change.

**`T-2b` attempt 1 verdicts.** Reviewer A (RELIABILITY+RISK) → **PASS**: DDL ↔ entity ↔ design §3.1 match three ways on both columns (`varchar(16)`/`varchar(255)`, nullable, no DEFAULT — MySQL's implicit `DEFAULT NULL` is what satisfies `R-8`'s "default both fields to null for every pre-v0.2 row"); `ai_unavailable` code-only (column stays `varchar(32)`); `T-2`'s file untouched; one table in the class body, zero `CREATE TABLE`; `down` drops only the two columns in reverse order, so `T-2`'s audit trail survives a revert of `T-2b`; class name === `name` literal === filename and `1789571865104` sorts after the applied `1789566953005` and is the highest in `src/migrations/`. Noted: the DoD's "`migration:check` green" and "owner runs it" cannot both hold at hand-off — red-by-design with exactly one pending file is the maximal pre-run evidence, so the task stays `[~]` until the owner runs it.
Reviewer B (READABILITY+RESILIENCE) → **FAIL**, one issue: the `degraded_reason` JSDoc ends with "`null` whenever `ai_status` is `null`", which is true for KP, transport no-answer and pre-v0.2 rows but **false for an AI `unavailable` row**, which stores `ai_status = null` **and keeps the reason text`**. A `T-6` implementer following it would null out the reason on exactly the branch v0.2 exists to preserve.

**Leader adjudication:** FAIL upheld — verified at the source: `design.md:238` ("an AI `unavailable` writes … and keeps `degraded_reason`"), `requirements.md:269` ("… and the reason text"), `requirements.md:294` (the `null` default is scoped to pre-v0.2 and KP rows). The entity JSDoc is `T-2b`'s own deliverable and is the contract `T-6`/`T-7` read, so an inverted citation is a gate failure, not a style note. Rework attempt 2 at `xhigh`, **documentation only — the SQL is reviewed and must not change**; folded in three factual defects in the same artifacts (the `<ts>` placeholder at entity:50; the pruning note claiming 29 tables while listing 32, byte-identical to `T-2`'s list which claims 31; the "code deployed without applying it" bullet understating that TypeORM selects every column, so the whole entity surface errors).

ADVISORY (T-2b, recorded, not gating): **→ `T-5b` (forward pointer, copy into its brief):** `BilateralQualityAssessmentAiStatus = 'completed' | 'partial'` is right for the *column* and must not be widened — a third member would make `ai_status = 'unavailable'` representable, which the §4.5 mapping forbids. `T-5b` needs a wider **wire** type (`… | 'unavailable'`) declared as a derivation in the client. Failure mode to avoid: `AiClientOutcome`'s failure variant has **no field for a reason**, so modelling an AI `unavailable` as another `AiClientFailureOutcome` member would make `degraded_reason` structurally unable to reach the orchestrator and silently lose it (`R-7`). The AI `unavailable` must travel on a variant carrying `ai_status` + `degraded_reason`. · **→ `T-4b`:** `contract_version`'s JSDoc still reads `` e.g. `0.1` `` (written by `T-2`); `T-4b` owns the value change to `0.2`. · **→ default branch (pending, shared-file discipline):** "repo rule 25" cited in 10 migration headers resolves to no rule in any guide — define the numbered rules or restate the section unqualified. · The "backticked table names: … only" evidence line is only true scoped to the class body; scope that grep after `export class` in future migrations.

**`T-2b` attempt 2** — Implementer (sonnet, `xhigh`), documentation only. `degraded_reason` JSDoc rewritten to the mapping table's actual rule (verbatim): *"`null` for KP rows, for every pre-v0.2 row, and for a transport no-answer (`timeout`/`http_error`/`malformed`/`not_configured`) — but **set** on an AI `unavailable` row, where `ai_status` is `null` and `unavailable_reason` is `ai_unavailable` (`design.md` §4.5 mapping table, `BIL-QAI-R-7`)."* Also: `<ts>` placeholder resolved to the real filename; the "code deployed without applying it" future corrected to the true blast radius (`ER_BAD_FIELD_ERROR` on every read/write of the entity, not just the two columns) with the Jenkins pipeline-ordering point from `onecgiar-pr-server/CLAUDE.md` §5 and an explicit "no defensive code added"; the pruning note's contradictory count dropped, the 32-name list kept (Implementer's reason: a number that cannot be re-verified in a documentation-only pass is worse than no number). Verification (verbatim): `tsc --noEmit` clean; eslint clean; `jest "quality-assessment"` → 4 suites / 128 tests. No `Not Done`.

Reviewer B (re-review) → **PASS**. It verified the DDL independently rather than on report: `up`/`down` byte-identical to attempt 1, both hunks above the JSDoc terminator, column decorators unchanged — so Reviewer A's DDL PASS is undisturbed. It also confirmed the new cross-references resolve (`CLAUDE.md` §5 does state the pipeline applies migrations; `findLatestByResultId` is real), unlike "repo rule 25". Summary: "the single FAIL item is fixed at the source of the defect and the two columns' JSDoc now hands `T-6`/`T-7` the §4.5 mapping correctly, including the `ai_unavailable` row where the reason is stored while `ai_status` is null."
New advisory (recorded): the pruning note now spends seven lines explaining why it omits a number — longer than the corrected fact, and it conflates "counting the list above" (trivially possible) with "re-deriving the generator's diff" (needs a live DB). Delete or compress if the file is opened again; not worth a rework cycle.

| Field | Value |
|---|---|
| **Final status** | **PASS** (attempt 2 of 3; Reviewer A PASS on attempt 1 — DDL/risk, Reviewer B PASS on attempt 2 — readability/resilience) |
| Requirements covered | `BIL-QAI-R-7` scenarios *The AI answers `partial`* / *The AI answers `unavailable`* (storage half); `BIL-QAI-R-8` (persist `ai_status`, `degraded_reason`; both default `null` for pre-v0.2 and KP rows) |
| Final verification | `tsc --noEmit` clean · eslint clean · `jest "quality-assessment"` 4 suites / 128 tests · `migration:check` → `Pending: 1` (this file only) |
| **Open owner gate** | `npm run migration:run` for `AddAiStatusToBilateralQualityAssessments1789571865104` on the dev DB. The DoD's "`migration:check` green" and "the owner runs it" cannot both hold at hand-off; exactly one pending file with no unrelated drift is the maximal pre-run evidence. Same handling as `T-2`, which the owner ran on 2026-09-16 |
| Decisions | Two nullable columns, no DEFAULT (MySQL's implicit `DEFAULT NULL` is what backfills pre-v0.2 rows, satisfying `R-8` by construction) · `ai_unavailable` added to the union in code only, no DDL · `T-2`'s migration untouched · `ai_status` union deliberately kept at two members (a third would make `ai_status = 'unavailable'` representable, which §4.5 forbids) |
| Commit | see below |

### `BIL-QAI-T-1b` — Contract v0.2 copy and the per-type label enumeration for Daniela

| Field | Value |
|---|---|
| Date | 2026-09-16 · Skills `api-design-principles`, `cognitive-doc-design` · Effort attempt 1 `high` → attempt 2 `xhigh` |
| Files | `docs/bilateral-module/integration-contracts.md` § *Quality assessment (outbound)*, lines 495–767 |

**Attempt 1** — Implementer (sonnet). Section rewritten to v0.2 (+144/−14). Verification: the four v0.2 greps present; no v0.1 leftovers inside the section; five contract tables; no `README.md` diff. Message draft for Daniela produced (recorded above, owner sends).

Reviewer (opus) → **FAIL** (2 issues). Accepted on everything else — per-type `fields` table, the four dependency notes, frozen-label rule, `impact_areas` root-sibling placement with the no-penalty clause, response `status`/`degraded_reason` and section-grey semantics, the status-mapping table, bump procedure, change log; both falsifying inputs absent, disqualifier does not fire, `.cursorrules` clean.
> 1. **`impact_areas` invents a pillar.** The `name` enumeration listed six values including `Youth` and the example emitted `{"name": "Youth", …}`. PRMS has **five** pillars (`DAC_PILLAR_CONFIG`, `bilateral.service.ts:164-190`); `Youth` is a **sub-component of Gender** (`1760113104098-ImpactAreasScoresComponents.ts:25`) and an **evidence tag** (`evidence.mapper.ts:19`) — so the section had the GAP-6 asymmetry backwards and taught the AI to expect a top-level entry `T-4b` can never produce. The list also imported evidence-tag spellings into the pillar column. Violates design §4.5 (which never enumerates `name` — a closed six-name list is invention), §13 GAP-6, `requirements.md` Glossary ("the **five** PRMS impact-area pillars") and `R-2` ("the pillar label the form paints").
> 2. **`Delivery method: "Virtual"` is a literal the form never emits.** The catalogue is `Virtual / Online` · `In person` · `Blended (in-person and virtual)` (`1672758869055-changeDeliveryName.ts:7-15`, DTO enum `create-bilateral.dto.ts:323`). An AI implementing the rule as written would compare against `"Virtual"`, never match, and silently down-grade every virtual-delivery result's geography — "a rule that looks implemented and grades nothing". Violates design §4.5 note 2 (which names no value) and `DD-11`, which rejects exactly this failure mode.
> On the declared deviation (avoiding the literal `"Climate"` to satisfy its own grep): **accepted** — meaning survives, and the chosen example `["Gender", "Poverty"]` is the one `requirements.md:173` uses. **The `tasks.md:50` grep recipe is what is over-broad** (it cannot tell "mentions" from "emits"); narrow it at archive to something like `'"tags".*Climate'`.

**Leader adjudication.** Both upheld. On the one question the Reviewer said to put to the owner — does `Blended (in-person and virtual)` also exempt geography? — **the Leader decided rather than blocked: only `Virtual / Online` exempts; blended keeps geography required**, because a blended delivery has an in-person component. Recorded here as a decision, and it must be in the message to Daniela. Rework at `xhigh`, surgical; the Reviewer's Innovation-use arithmetic advisory folded in because it is a number the AI will copy.

**Attempt 2** — five pillars stated as **form labels** with a new label↔short-name table (verified against `DAC_PILLAR_CONFIG` for the short names and the form tooltips at `section-general-info.component.ts:28-80` for the labels); GAP-6 mirror sentence added (`Youth` is a tag with no pillar, as `Climate` is a pillar with no tag); the three real delivery-method labels quoted with the exemption named semantically **and** by value; examples corrected (`"Virtual / Online"`; `impact_areas` uses real pillars); Innovation-use numbers changed to `{total: 340, women: 100, men: 150, women_youth: 20, men_youth: 15}` so the breakdown sums to **less** than the total, plus a clause stating youth counts are **subsets** of women/men, never additional buckets. Verification (verbatim): every surviving `Youth` hit is an evidence tag, the GAP-6 mirror or the change log — never an `impact_areas.name`; every `Virtual` hit is a real catalogue label; `git diff --stat` 1 file, +20/−8. Implementer judgment call: pillar labels taken from the tooltip `<strong>` heading rather than the `label:` field, which appends the UI-only word "tag".

ADVISORY (T-1b, recorded): **→ `T-4b`:** the frozen score enum is `(0) Not Targeted` (capital T) and matches design and the client's `TAG_LEVELS`, but the value the mapper actually reads is `gender_tag_level.description` via `dac_scores.<pillar>.tag_title`, and a captured real payload (`bilateral-result-summaries.en.md:154`) shows `"(0) Not targeted"` (lowercase t) — **the mapper must normalise or the fixture will enshrine the mismatch**. · `Other output · Other outcome` share one example typed `other_output`; the `other_outcome` API string never appears. · Authority is stated circularly (design §4.5 line 173 says the contract copy is the authority; the contract line 499 says the spec is) — settle it in one sentence at archive. · Narrow the `tasks.md:50` grep recipe at archive.

Reviewer (re-review of attempt 2) → **FAIL** (1 issue, third invented value on this task). It confirmed the judgment call (tooltip `<strong>` headings are the pillar labels; `label:` appends the UI-only word "tag" naming the *score control*) and confirmed both attempt-1 issues fixed and the arithmetic advisory correctly folded in.
> 1. **The `impact_areas` examples emit a sub-component value PRMS cannot produce.** Both examples send `"subcomponents": ["Women's empowerment"]`. The vocabulary is the seeded catalogue `impact_areas_scores_components` (only `INSERT`: `1760113104098-ImpactAreasScoresComponents.ts:21-41`, 13 rows); Gender's are `Gender equality`, `Youth`, `Social Inclusion`. `"Women's empowerment"` appears nowhere in the repo except these two doc lines. The delta made it a contradiction: line 602 now says `Youth` **is** a Gender sub-component, so the field table and the example disagree about that pillar's sub-components — and the example is what the AI team reads first. Violates design §5 *Impact-areas mapper* (values come from `impact_area_names[]` = `ImpactAreasScoresComponent.name`) and `R-2` ("populated only where the form populates it").

**Leader verification and adjudication.** I read the seed myself: 13 rows, and neither `1768967506640-CreateResultImpactAreaScore.ts` nor `1769009398774-UpdateGeneralInfoGreen.ts` inserts or updates a name — the catalogue is unchanged since the seed. FAIL upheld. **Pattern named to the Implementer:** all three failures on this task are the same defect class — a plausible value written from intuition instead of read from the source — so attempt 3's brief requires a self-check listing every literal in the section against the file that defines it. Attempt 3 also **adds the full 13-row sub-component catalogue** to the contract: the section already enumerates every other closed vocabulary it depends on (evidence tags, score labels, delivery methods), and the QA criterion "a Principal score must have a sub-component selected" cannot be validated against an unknown vocabulary. Scope judgment recorded rather than assumed. Attempt 3 is the **last before HALT**.

### Merge of `origin/performance-refactor` (2026-09-16, owner request)

In-flight work committed first (`19bb0ce0d`, WIP: `T-1b` attempt 3 + `T-12` attempt 2, both pending their Reviewer) so the merge had a clean tree. `git merge origin/performance-refactor --no-edit` → **exit 0, no conflicts** (exit checked directly, not through a pipe — memory rule), 51 incoming commits, 67 files, +5 490/−237. Three files overlapped with ours (`type-innovation-dev.component.{html,spec.ts}`, `bilateral.module.ts`); all our changes survived, verified by grep: the key-presence prefill gate, the restored field label, and the six `BilateralQuality*` / `INDICATOR_DESCRIPTION_RESOLVER` registrations.
Post-merge verification: server `tsc --noEmit` clean · server `jest "quality-assessment"` 4 suites / 128 tests · client `jest src/app/pages/bilateral` **46 suites / 1 486 tests** (up from 1 473 — the merge brought more tests into the area). `onecgiar-pr-client/package.json` changed (new local test-tooling scripts only, no new dependencies); `npm ci` re-run in the client. `src/environments/*` survived the reinstall.
Leader note: the client `npm ci` ran while the `T-12` Reviewer was active, which the concurrency rule forbids for measurement commands. The Reviewer is read-only (`Read`/`Grep`/`Glob`) and runs no build, so no contamination was possible — recorded rather than glossed.

### `BIL-QAI-T-12` — Innovation developers field restored in the bilateral form

| Field | Value |
|---|---|
| **Final status** | **PASS** (attempt 2 of 3) |
| Date | 2026-09-16 · Skills `angular-developer`, `ui-ux-pro-max` · Effort `xhigh` both attempts (a `[~]` resume starts one level up) |
| Files | `pages/bilateral/components/section-type-specific/type-innovation-dev/{component.ts,component.html,component.spec.ts,CLAUDE.md}` |
| Requirements covered | `BIL-QAI-R-15` (both scenarios), `BIL-QAI-AC-18` |

**Attempt 1** (parked by the rate limit, finished on resume) restored the field, removed the overwrite from `buildPayload()`, added the payload-level tests and diagnosed a CVA-microtask test bug. Reviewer → **FAIL** (2 issues):
> 1. **A cleared value does not survive a reload.** `applyInnovationDevelopersPrefill()` gated on truthiness (`if (this.body.innovation_developers?.trim()) return;`), but once a row exists the server returns the key **present with `null`**, and `null?.trim()` is `undefined` — so the guard fell through and re-filled from the lead contact. Worse, the prefilled value sat in `body`, so the next `onFieldChange()` on any field in the section wrote it back. "The column is therefore still never empty for any result whose lead contact is set, which is exactly the unfalsifiable-criterion condition the task exists to remove." Violates `R-15` ("the form must NOT re-fill it from the lead contact person on the next load"), `AC-18`, and the task's own DoD ("a cleared value round-trips").
> 2. **`CLAUDE.md` is 155 lines** against the 120-line hard cap in `onecgiar-pr-client/docs/COMPONENT-DOCS.md` §4.

**Leader adjudication:** both upheld. Issue 1 is the defect the task exists to remove, merely relocated from every save to the first edit after every reload — no test could see it because none used `{ innovation_developers: null }`, the shape the server actually returns after a clear. Rework at `xhigh` with **two mandatory** regression tests (model-level **and** the follow-on-write half) plus the DOM twin for `AC-18`.

**Attempt 2** — gate changed to **key presence**: `if ('innovation_developers' in this.body) return;`. The Implementer verified the premise at the source: `InnovationDevExists` selects the column explicitly (`results-innovations-dev.repository.ts:281`) and returns `undefined` with no row (`:312`); `getInnovationDev` spreads `...innDevExists` (`summary.service.ts:1005`), so the key is absent for a never-saved section and present-as-`null` once a row exists. Three tests added: `does NOT prefill a stored null — the column exists, the user cleared it`, `keeps a reloaded null out of the payload even after an unrelated field changes`, `renders empty — not the lead contact person — when the stored value is a reloaded null`. `CLAUDE.md` 155 → **exactly 120**; the story consolidated there, a two-line docstring pointer and a one-line template comment. Verification (verbatim): scoped `Tests: 72 passed` · `src/app/pages/bilateral` `46 suites / 1 473 passed` (1 486 after the `performance-refactor` merge) · `All files pass linting.` No `Not Done`.

Reviewer → **PASS**. It re-verified the premise independently and checked the **mirror risk** the new gate could introduce — that a pre-created row would suppress the prefill for everyone: it does not exist, because `POST_createBilateralHeader` creates no type-specific row (the centre service only *deactivates* `results_innovations_dev`, `bilateral-center.service.ts:621`) and the row is born on first save (`summary.service.ts:659`); a type-change-and-back re-opens the prefill because the join filters on `is_active`, and the bulk-upload path writes a non-null value the gate leaves alone. All three tests are falsifying under the old gate. On the doc trim the Leader flagged as the risk in the fix: **nothing load-bearing was lost** — all 12 Trap bullets survive compressed, every `file:line` citation and ticket ID survives, and the two cut rationales (`reference_materials`, investment key families) live verbatim in the component source the doc points at, which is where `COMPONENT-DOCS.md` §3 says they belong.
Summary: "The key-presence gate closes `BIL-QAI-AC-18` at its real root … and the three added tests all fail under the old truthiness gate, so the removed substitution now has a regression net on both halves; the `CLAUDE.md` trim to 120 lines cost no invariant, citation or `file:line`."

ADVISORY (recorded): two micro-facts left the guide and are re-derivable in one file-open ("Catalogues … already loaded in root"; the general "everything else is full metadata and does not count"). **→ `T-11`(b) copy gate:** the field sits under the always-visible MDS note, but is explicitly not MDS — one clause ("plus optional fields") would resolve it; owner copy call. **→ `T-11`(b) / Daniela:** even with the fix, R-15's prefill semantics mean the column is non-empty for every result the reporter does not actively clear, so the AI's "missing developer" criterion only fires on a deliberate clear — nobody should read a high green rate there as signal. The reference form passes `[description]`/`[tooltip]` on this textarea and the bilateral one does not (consistency-neutral, not required by `T-12`). The ingest handler's own lead-contact fallback (`handlers/innovation-development.handler.ts:56-75,95`) stays **out of scope** — different module, owner notified per the module-ownership rule.

| Final verification | scoped 72 tests · `pages/bilateral` 1 486 tests · `ng lint --quiet` clean · `CLAUDE.md` 120 lines |
| Server finding (read-only) | the legacy summary endpoint persists an explicit `null`; a cleared value round-trips. No server change |
| Commit | see below |

