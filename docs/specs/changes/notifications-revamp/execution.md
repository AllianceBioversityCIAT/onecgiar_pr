# Module Spec — `execution.md`

## 1. Document Control

- **Spec:** `docs/specs/changes/notifications-revamp`
- **Leader session model:** Sonnet 5 (`claude-sonnet-5`). Registry note: `AGENTS.md` → `## Model Routing` recommends `opus` for T1 Leader; flagged once to the user at run start, not switched (their call, never blocking).
- **Approval Mode:** not declared in `requirements.md`/`design.md` Document Control — treated as gated (continue/pause prompt after each task, no `pre-approved` auto-advance).
- **Implementer model:** `sonnet` (T2, per registry, via `.claude/agents/akili-implementer.md` wrapper).
- **Reviewer model:** per `.claude/agents/akili-reviewer.md` wrapper (T3, author ≠ auditor honored — Reviewer spawned as a fresh-context agent independent of the Implementer's session).
- **Started:** 2026-09-25.

---

## 2. Task Execution History

### `NOTIF-T-1` — Add Spartan `checkbox` and `badge` components

- **Final status:** PASS (attempt 1)
- **Date:** 2026-09-25
- **Skills assigned:** `spartan` (Leader override of default — task is pure Spartan CLI scaffolding, no other skill fits). **Effort:** low (mechanical/scaffolding, per Effort dial).
- **Files changed:**
  - `onecgiar-pr-client/src/app/spartan/badge/src/index.ts` (new)
  - `onecgiar-pr-client/src/app/spartan/badge/src/lib/hlm-badge.ts` (new)
  - `onecgiar-pr-client/src/app/spartan/checkbox/src/index.ts` (new)
  - `onecgiar-pr-client/src/app/spartan/checkbox/src/lib/hlm-checkbox.ts` (new)
  - `onecgiar-pr-client/tsconfig.json` (CLI-added `@spartan/badge`, `@spartan/checkbox` path aliases)
- **Implementer verification:**
  - Pre-check: `npx ng g @spartan-ng/cli:info --json` — no version mismatch (angular ^21.2.18, spartanBrain ^1.1.0, spartanCli ^1.1.0); neither component installed beforehand (no Disqualifier triggered).
  - `npx ng g @spartan-ng/cli:ui --name=badge` and `--name=checkbox` — both CLI-generated, no hand-authoring.
  - Post-check: `npx ng g @spartan-ng/cli:info --json` → `installedComponents` includes `checkbox` but **not** `badge`.
  - Lint: `npx ng lint --quiet` → "All files pass linting."
- **Not Done / Assumptions (Implementer-flagged):** the task's literal Falsifier ("both `checkbox`/`badge` appear under `installedComponents`") is not satisfied for `badge`. Root cause (Implementer's claim, independently verified by Reviewer): the CLI's `installedComponents` detector is a regex scan for files importing `@spartan-ng/brain/*` or the `@spartan/*` alias; `badge` has no Brain primitive package and self-registers only once some consumer imports `@spartan/badge` — composition is explicitly out of this task's scope (deferred to `NOTIF-T-6`/`NOTIF-T-7`). Implementer declined to add a throwaway import to force detection (would be unscoped fabricated code).
- **Reviewer verdict:** `PASS` — ruled the falsifier gap an **acceptable, well-evidenced exception, not a FAIL**. Independent verification:
  - Both `hlm-badge.ts` and `hlm-checkbox.ts` are byte-for-byte structural matches of the Spartan CLI's own generator templates (`node_modules/@spartan-ng/cli/src/generators/ui/libs/{badge,checkbox}/files/lib/*.template`) — zero hand-authored drift.
  - Cross-checked the detector's real behavior: 19 folders exist under `src/app/spartan/*`, only 17 are reported; `field` is reported but has no folder (it's a Brain import, not a Helm folder); `breadcrumb` and `kbd` are on-disk, Brain-less, and **also** absent from `installedComponents` — pre-existing, not introduced by this task. `skeleton` (Brain-less) IS detected, but only via a real consumer (`hlm-sidebar-menu-skeleton.ts`) — confirming the "Brain-less + no consumer ⇒ undetected" pattern.
  - The repo's own guide already documents this: `onecgiar-pr-client/src/CLAUDE.md` §21.7 — "`ng g @spartan-ng/cli:info --json` misreports `installedComponents`. Trust `ls src/app/spartan/`."
  - **Decision:** falsifier satisfied via substitute evidence (folder presence + template match + `tsconfig.json` alias), per `src/CLAUDE.md` §21.7. `tasks.md`'s Falsifier text should be read with this amendment for any future re-verification of this task.
- **Spec-doc correction applied (Reviewer finding, not a task defect):** `design.md` §6.3 line 95 incorrectly stated Spartan `checkbox` renders a native `<input type="checkbox">`; Reviewer verified against `@spartan-ng/brain` source that `BrnCheckbox` renders `<button role="checkbox">` (ARIA-conformant, not a styled `<div>`, but not a native input either). Corrected in `design.md` 2026-09-25 so `NOTIF-T-6`'s Reviewer doesn't fail a conformant implementation against an impossible literal.
- **ADVISORY (non-gating):** `components.json` was listed in `tasks.md`'s "Files (expected)" but correctly untouched — that file has no per-component registry; the real registration point is `tsconfig.json`'s `paths` alias, which was added correctly.
- **Requirements covered:** `NOTIF-R-5`, `NOTIF-R-10`, `NOTIF-R-4` (components staged for later composition; no UI change yet).
- **Decisions made:** accepted the badge falsifier exception per Reviewer's independent verification, rather than re-spawning or escalating — the root cause is structural (a known, documented CLI limitation, not an implementation gap), fully evidenced, and the correct fix (a real import) is explicitly out of this task's scope.
- **Issues encountered:** CLI `installedComponents` false-negative for Brain-less, not-yet-consumed components (documented above).
- **Final verification result:** `checkbox` verified via CLI info command; `badge` verified via `ls src/app/spartan/` + template diff + `tsconfig.json` alias, per `src/CLAUDE.md` §21.7. Lint clean.

---

### `NOTIF-T-2` — `group-notifications-by-recency` pipe

- **Final status:** PASS (attempt 1)
- **Date:** 2026-09-25
- **Skills assigned:** `angular-developer`, `tdd` (Leader selection — algorithmic date-boundary logic, red→green pays here). **Effort:** medium (default, well-specified task).
- **Files changed:**
  - `onecgiar-pr-client/src/app/pages/results/pages/results-outlet/pages/results-notifications/pipes/group-notifications-by-recency.pipe.ts` (new)
  - `onecgiar-pr-client/src/app/pages/results/pages/results-outlet/pages/results-notifications/pipes/group-notifications-by-recency.pipe.spec.ts` (new)
- **Implementer verification:**
  - TDD loop: spec written first (11 cases) → red (module not found) → implementation → 3 tests failed on first green attempt (test-fixture bug: hardcoded UTC timestamps landed in the wrong local-TZ bucket on the dev machine, America/Bogota UTC-5) → fixed test fixtures (not implementation) → green.
  - `npx jest --testPathPattern=group-notifications-by-recency.pipe.spec --silent --reporters=summary --no-coverage` → **11 passed, 11 total**.
  - Lint: `npx ng lint --quiet` → "All files pass linting."
- **Not Done / Assumptions (Implementer-flagged, adjudicated as an acceptable judgment call, not owed scope):** design.md/tasks.md specify "local-midnight boundaries" and "Today/This week/Earlier" but don't pin the exact this-week window (calendar week vs. rolling 7 days). Implementer chose a rolling 7-local-day window (inclusive lower bound). Reviewer independently checked `requirements.md` `NOTIF-R-2`, `design.md` §6.2/§1A, and `mockups/README.md` — none specifies calendar week vs. rolling 7 days, and the chosen interpretation satisfies the stated falsifier exactly. Ruled not a gap.
- **Reviewer verdict:** `PASS`. Confirmed pure `@Pipe`, no DI, correct signature, no re-sort, missing-date → `earlier` fallback matches `design.md` §1A `NOTIF-P-1`'s prescribed "if false" behavior (not the silent-coercion the Disqualifier bans). Test suite covers the exact falsifier plus both boundary edges, empty/null list, single item, missing date, custom `dateKey`.
  - **ADVISORY (non-gating, recorded for follow-up):**
    - **Reliability:** `startOfWeek` uses fixed-millisecond arithmetic off a local-midnight anchor (`startOfToday.getTime() - 7 * MS_PER_DAY`) — across a DST transition the lower bound can land at 23:00/01:00 local instead of true midnight, mis-bucketing one row twice a year. Fix: `new Date(now.getFullYear(), now.getMonth(), now.getDate() - 7)`, reusing the existing `startOfLocalDay` idiom.
    - **Reliability (test):** the first falsifier test hardcodes a UTC instant that holds for UTC/Bogota but would flip bucket at extreme positive UTC offsets (UTC+14); the other 9 clock-sensitive tests derive expectations locally and are TZ-robust.
    - **Readability:** JSDoc claims missing dates are "logged as a data-quality gap upstream" — no such logging exists yet anywhere in the notifications path (`design.md` `NOTIF-P-1` calls for it). Soften the wording or leave a TODO.
    - **Risk (hand-off note for `NOTIF-T-5`):** pipe is `standalone: false` — must be added to `notification-item.module.ts`'s `declarations`/`exports` (where sibling filter pipes live) or it is inert. Registered pipe name is `groupNotificationsByRecency`, not the `groupByRecency` shorthand `design.md` §6.2 / `tasks.md` `NOTIF-T-5` use — `NOTIF-T-5`'s Implementer must use the real name.
- **Requirements covered:** `NOTIF-R-2`.
- **Decisions made:** accepted the rolling-7-day window as the working definition; accepted the missing-date fallback design as spec-conformant.
- **Issues encountered:** none blocking; DST edge case and module-registration hand-off logged as ADVISORY for `NOTIF-T-5` to pick up.
- **Final verification result:** 11/11 tests passing, lint clean.

---

### `NOTIF-T-3` — Center and Bilateral-project filter pipes

- **Final status:** PASS (attempt 1)
- **Date:** 2026-09-25
- **Skills assigned:** `angular-developer`, `tdd` (mirrors an existing pipe's shape, logic-light but benefits from test-first on the field-path lookup). **Effort:** medium.
- **Files changed:**
  - `.../pipes/filter-notification-by-center.pipe.ts` (+ `.spec.ts`, new)
  - `.../pipes/filter-notification-by-bilateral-project.pipe.ts` (+ `.spec.ts`, new)
- **Implementer verification:**
  - `npx jest --testPathPattern="filter-notification-by-(center|bilateral-project).pipe.spec" --silent --reporters=summary` → **8 passed, 8 total**.
  - Lint: `npx ng lint --quiet` → clean.
- **Not Done / Assumptions (Implementer-flagged, adjudicated as a correct reading, not a gap):** "bilateral project" identifier resolves to `obj_result.result_code` — the bilateral branch of `notification-item.component.html` exposes no distinct project entity/id, only `result_code`. Reviewer independently confirmed this against the live template (line 41-44) and ruled it the only identifier the template exposes, not a misread.
- **Reviewer verdict:** `PASS`. Verified center field path (`obj_result.result_center_array[0].clarisa_center_object.clarisa_institution.id`) matches the template's chain up to `.acronym`, terminating on `.id` per the task's own Falsifier wording. Confirmed `NOTIF-P-4` unrefuted via `rg result_center_array` (only 3 hits: the template + the 2 new files). Confirmed loose `==` equality is the exemplar pipe's established convention, not new drift, and is the safer choice here (numeric id vs string checkbox value). 8/8 tests read as behavioral (real fixture shapes), not presence-only assertions.
  - **ADVISORY (non-gating, forward pointers for `NOTIF-T-6`):**
    - Center facet options in `NOTIF-T-6` must be sourced from the same `clarisa_institution.id` path (not a different CLARISA id-space) or the facet will silently return zero matches — no unit test in this task can catch an id-space mismatch, it's an integration concern for `NOTIF-T-6`.
    - `result_code` is also rendered in non-bilateral branches of the same template (lines 69, 152, 196) — the bilateral-project pipe isn't intrinsically bilateral-only, so `NOTIF-T-6` must populate its filter options from bilateral rows only, not the full list.
    - Both new pipes are `standalone: false` and not yet declared in any NgModule — `NOTIF-T-6` must add both to `notification-item.module.ts`'s declarations/exports (same module `GroupNotificationsByRecencyPipe` needs per `NOTIF-T-2`'s advisory) or the template fails at `npm run build` (not caught by `tsc --noEmit` alone).
- **Requirements covered:** `NOTIF-R-5`.
- **Decisions made:** accepted `result_code` as the bilateral-project identifier; accepted `.id` as the center identifier despite the template only ever *rendering* `.acronym` (the field is presumed present on the same object, per the Falsifier's own wording).
- **Issues encountered:** none blocking.
- **Final verification result:** 8/8 tests passing, lint clean.

---

### `NOTIF-T-4` — Segmented visual for Received/Sent

- **Final status:** PASS (attempt 1)
- **Date:** 2026-09-25
- **Skills assigned:** `spartan`, `angular-developer`. **Effort:** low-medium.
- **Files changed:**
  - `.../pages/requests/requests.component.html` (Helm Tabs visual classes applied to existing `routerLink` anchors; `[attr.data-active]` mirrors `routerLinkActive`'s own `isActive`, no navigation change)
  - `.../pages/requests/requests.component.scss` (removed dead `.requests_header_item`/`.active` rules)
  - `.../pages/requests/requests.component.spec.ts` (new `RouterTestingHarness`-based describe block asserting deep-link + active-state; fixed the pre-existing describe block's TestBed config, which needed `RouterModule` instead of just `RouterOutlet` for `routerLink`/`routerLinkActive` to resolve at all)
- **Implementer verification:** `npx jest --testPathPattern=requests.component.spec --silent --reporters=summary --no-coverage` → **9 passed, 9 total** (3 suites incl. siblings). Lint clean.
- **Not Done / Assumptions:** none blocking — two documented judgment calls (dropped unused `ng-icon` selector fragments from the copied class string; verified router mechanics with lightweight stub components at `received`/`sent` instead of the real lazy modules, to keep the test hermetic and in-scope).
- **Reviewer verdict:** `PASS`. Independently confirmed: no Spartan `Tabs` component instantiated (only classes/structure copied, diffed verbatim against `hlm-tabs-list.ts`/`hlm-tabs-trigger.ts`); router still owns navigation (`routerLink`/`routerLinkActive` both real, `data-active` is a pure mirror, and `RouterLinkActive.update()`'s deferred write avoids `ExpressionChangedAfterItHasBeenChecked`); the new spec asserts real post-navigation DOM state (not a stubbed predicate); no hardcoded hex (all Helm semantic tokens, verified they resolve to real project vars in `styles.scss`); SCSS removal is genuinely dead code (`rg requests_header_item` → zero hits); test-infra change is minimal and justified (the original describe block's directives were previously inert without `RouterModule`).
  - **ADVISORY (non-gating):**
    - **Readability/design-system:** the copied trigger classes include `text-sm`, which on this codebase's `12px` root font-size resolves to ~10.5px, vs. the replaced `pr-body-2`'s 12px — a ~1.5px shrink in tab-label size. Not a FAIL (`NOTIF-DD-1` mandates verbatim Helm classes, and every Helm component here carries `text-sm`), but flagged for an explicit eyeball at `NOTIF-T-8`'s manual visual pass.
    - **Reliability:** `group-data-horizontal/tabs:h-9` is inert (Tailwind resolves `data-horizontal`, not `data-orientation=horizontal`) — reproduced verbatim from the Helm source itself, not an Implementer error; do not "fix" it into a divergence from the real component.
    - **Risk (low):** the new spec hand-writes its route config instead of importing the real `requestsNotificationsRouting` table from `src/app/shared/routing/routing-data.ts` — a future edit to the real child paths wouldn't be caught by this test. Not a current gap (this diff doesn't touch that table).
- **Requirements covered:** `NOTIF-R-1`.
- **Decisions made:** accepted the visual-only restyle per `NOTIF-DD-1`; accepted the test-infra fix to the pre-existing describe block as in-scope (it was needed to make the directives resolve at all, not a rewrite).
- **Issues encountered:** none blocking.
- **Final verification result:** 9/9 tests passing, lint clean.

---

### `NOTIF-T-5` — Regroup Received/Sent by recency

- **Final status:** PASS (attempt 2 of 3)
- **Date:** 2026-09-25
- **Skills assigned:** `angular-developer`, `spartan`. **Effort:** medium (attempt 1) → **high** (attempt 2).
- **Attempt 1 — Implementer:** merged Pending∪Done via `.concat()`, chained existing filters, grouped via `groupNotificationsByRecency`, three `@if` blocks per bucket with Helm `badge` count pills. Dropped `@defer (on viewport)`. `npx jest --testPathPattern="(received|sent)-requests.component.spec"` → 10/10 passed; regression check on `notification-item.component.spec|group-notifications-by-recency.pipe.spec` → 78/78 passed. Lint clean.
- **Attempt 1 — Reviewer verdict: `FAIL`** (findings carried verbatim):

  1. **Within-bucket ordering is no longer `requested_date` descending — the merge breaks the pipe's own sortedness premise.** The merged list is built via plain `.concat()`; `ResultsNotificationsService` sorts the Pending and Done arrays **independently** desc, so the concatenation is only piecewise sorted. `GroupNotificationsByRecencyPipe` documents and relies on the input already being fully desc-sorted and does not re-sort. Result: inside any bucket holding both pending and decided rows (routinely Today), every pending row renders above every decided row regardless of actual date.
     - **Violated Rule:** `requirements.md` §8 `NOTIF-AC-1` ("in `requested_date` descending order within each group"); `NOTIF-T-2`'s stated input premise.
     - **Why the green suite missed it:** the fixture's two rows land in different buckets, so intra-bucket order is never asserted.
     - **Remediation:** sort each output bucket desc by `dateKey` inside `GroupNotificationsByRecencyPipe` itself (pure, no template mutation) rather than fixing the merge order at each call site. Do NOT `.sort()` the service-owned arrays in place, and do NOT add manual sort logic inside the `@let` template expression. Add a test: two same-bucket rows (one pending older, one done newer) asserting the newer renders first.

  - **ADVISORY (non-gating):** dropping `@defer (on viewport)` is an acceptable simplification (no NFR requires it, `NOTIF-DD-2` deletes the section it lived in by design, lists are unpaginated/in-memory) — but the Implementer's stated reason ("jsdom has no `IntersectionObserver`") is wrong and must not be recorded as precedent (Angular's `TestBed`/`DeferBlockFixture` supports `@defer` testing without a real `IntersectionObserver`). Also flagged: `apiServiceMock: any` declared-but-unassigned in both new spec files (legibility only).

- **Attempt 2 — Implementer:** added a private `sortByDateDesc<T>()` helper to `GroupNotificationsByRecencyPipe` — sorts each output bucket (`today`/`thisWeek`/`earlier`) desc by `dateKey` on a copy (`[...bucket].sort(...)`), never mutating the input. Undated rows sort via `-Infinity` (last within their bucket). TDD: new ordering test written first, confirmed RED against the un-fixed pipe, then GREEN after the fix. Also fixed the ADVISORY readability nit (`apiServiceMock: any` unassigned → `const apiServiceMock: any = {}`) in both component spec files.
- **Attempt 2 verification:** `npx jest --testPathPattern="group-notifications-by-recency.pipe.spec|(received|sent)-requests.component.spec"` → **22 passed, 22 total** (12 pipe = 11 pre-existing + 1 new; 10 component = 5+5 pre-existing, unmodified). Lint clean.
- **Attempt 2 — Reviewer verdict: `PASS`.** Confirmed the sort is copy-based (never mutates the pipe-local buckets or the service-owned source arrays); undated-row tiebreak (`-Infinity`) is consistent with `NOTIF-P-1`'s bucket-only fallback (no intra-bucket position specified); the new test is a genuine red→green falsifier for the exact piecewise-merge bug (constructs the wrong order as input, asserts the correct order as output); all 11 pre-existing pipe tests + 10 pre-existing component tests intact and unmodified; readability fix landed correctly in both files; scope is clean (templates untouched, fix lives entirely in the pipe per the Reviewer's own preferred remediation from attempt 1); attempt-1's audit trail in `execution.md` was correctly left untouched (history, not live state).
  - **ADVISORY (non-gating, take-it-or-leave-it):** the pre-existing pipe test title "…no re-sort" (from `NOTIF-T-2`) is now factually stale — the pipe does re-sort as of this fix; the test still passes because its own input happens to already be ordered. Renaming it to something like "preserves order for an already-sorted bucket" would remove the stale claim but is optional and must not consume a future rework attempt.
- **Requirements covered:** `NOTIF-R-2`, `NOTIF-AC-1` (including the descending-order clause, now genuinely enforced).
- **Decisions made:** accepted the pipe-level fix (robust to any caller, not just this one) over a call-site fix, per the Reviewer's original attempt-1 remediation preference.
- **Issues encountered:** one real rework round (ordering bug), resolved in one attempt.
- **Final verification result:** 22/22 tests passing (pipe + both components), lint clean.

---

### `NOTIF-T-7` — Restyle `notification-item` row + decision chip

- **Final status:** PASS (attempt 2 of 3)
- **Date:** 2026-09-25
- **Skills assigned:** `spartan`, `angular-developer`. **Effort:** high (attempt 1) → **xhigh** (attempt 2).
- **Attempt 1 — Implementer:** compact row shell (avatar + `font-mono` result code + single-line clamp) applied across all three `@switch` branches; Helm `badge` decision chip added alongside the existing "Status: Accepted/Rejected by …" text for cases 2/3, tinted via `--pr-color-green-500`/`--pr-color-red-300`; `.ts` genuinely untouched (all 63 pre-existing tests unmodified); `notification-item/CLAUDE.md` updated + `Verified:` re-stamped. `npx jest --testPathPattern=notification-item.component.spec` → 67/67 passed (63 pre-existing + 4 new). Lint clean.
- **Attempt 1 — Reviewer verdict: `FAIL`** (`full` review mode, per this task's own designation). Findings carried verbatim:

  1. **The "compact row shell" is a dead CSS selector — the avatar renders stacked above the text, not beside it, in all three status branches.** The SCSS rule `&.notification_row_shell { display:flex; ... }` is nested under `.notification .notification_content`, but the HTML applies the `notification_row_shell` class one level down, on `.notification_content_body` — the compiled selector matches nothing. `.notification_avatar` (a descendant selector) still applies and renders as a block-level circle on its own line, pushing the text below it. The new tests only assert class-string presence — jsdom computes no layout, so the green suite certified a no-op.
     - **Violated Rule:** `requirements.md` §6 `NOTIF-R-3`; `design.md` §6 file table (row shell restyle); `tasks.md` `NOTIF-T-7` description ("compact row shell").
     - **Remediation:** move the flex rule inside `.notification_content_body` (`&.notification_row_shell { display:flex; align-items:center; gap:12px; }`), or per the Tailwind-first hard rule, drop the SCSS and put `flex items-center gap-[12px]` directly on the template's body div (preferred — makes the bug structurally impossible to reintroduce). This property has no jsdom gate — carry visual verification into `NOTIF-T-8`'s manual browser check. Re-stamp `CLAUDE.md`'s `Verified:` line again once fixed.
  2. **`id="accept-contribution-btn"` / `id="decline-contribution-btn"` produce duplicate DOM ids** — `<app-notification-item>` renders inside `@for` loops in `received-requests`/`sent-requests`, so 2+ pending rows emit multiple elements sharing the same `id` (invalid HTML, breaks `getElementById`, a WCAG 2.1 AA unique-identifier defect). The chip in the same diff already used the correct pattern (`data-notif-decision-chip`) — internal inconsistency, not a judgment call.
     - **Violated Rule:** `requirements.md` §7 Accessibility (WCAG 2.1 AA).
     - **Remediation:** replace both with `data-testid="accept-contribution-btn"` / `data-testid="decline-contribution-btn"`; update the falsifier test's selector to match.

  - **Adjudicated judgment calls (both resolved, don't reopen):**
    - `primeicons` (`pi-user`/`pi-building`) instead of Lucide — **NOT gating**. `onecgiar-pr-client/CLAUDE.md` hard rule #21 is scoped to "redesign surfaces" and conflicts with `src/CLAUDE.md` §17 ("use primeicons for general iconography"); the Implementer's reading stands given the documented guide conflict. Recommended (non-blocking): switch to `lucideUser`/`lucideBuilding2` while the file is open for the Issue 1 fix, or log the deviation in `onecgiar-pr-client/docs/DESIGN-DEVIATIONS.md` if kept.
    - Chip label "Declined" vs. adjacent text "Rejected" — **CORRECT, required by `NOTIF-R-4`'s literal wording**. Do not "fix" this divergence.
  - **ADVISORY (non-gating):** new SCSS class blocks for pure layout/color (against Tailwind-first hard rule, mitigated — legacy SCSS-heavy component, migrate opportunistically) — natural to fold into the Issue 1 fix. `-webkit-line-clamp: 1` with no tooltip fallback loses information on narrow viewports for the bilateral branch's long sentence — not gating, worth considering.

- **Attempt 2 — Implementer:** deleted the dead `.notification_row_shell` SCSS block entirely; put `flex items-center gap-[12px]` directly on the `.notification_content_body` div (the actual parent of `.notification_avatar`) as Tailwind utilities, in all three `@switch` branches. Replaced both `id="accept-contribution-btn"`/`id="decline-contribution-btn"` with `data-testid="..."` equivalents; updated the falsifier test's selector to match. Added one new honest structural test (`.notification_avatar`'s `parentElement` carries the flex classes — a DOM/class-string claim, explicitly NOT a layout claim, with the visual-compactness verification correctly deferred to `NOTIF-T-8`'s manual check). Re-stamped `notification-item/CLAUDE.md`'s `Verified:` line a second time with an accurate, additive account of both fixes.
- **Attempt 2 verification:** `npx jest --testPathPattern="notification-item/notification-item\.component\.spec"` → **39 passed, 39 total** (34 pre-existing + 4 original NOTIF-T-7 tests + 1 new structural test, 0 failed). Lint clean. Hex check clean (only a prose CSS-selector-name string, no real color literals).
- **Attempt 2 — Reviewer verdict: `PASS`.** Independently confirmed on disk: all three `@switch` branches carry the flex utilities on the correct div with `.notification_avatar` as a direct child (not nested deeper); `.notification_row_shell` fully deleted from all `.scss`/`.html` (repo-wide grep, only prose mentions remain in docs/comments); `.notification_avatar`'s own visual rules (32px circle, tokens, no hex) intact and still correctly scoped under `.notification .notification_content`; the new test's title and assertions are an honest DOM-structure claim with an explicit "cannot prove visual compactness, deferred to `NOTIF-T-8`" comment, mirrored in `CLAUDE.md`; both `id`→`data-testid` conversions land correctly with zero `id=` attributes remaining anywhere in the template; regression-clean (pre-existing 34 tests + original 4 unchanged in shape); folder-doc `Verified:` line re-stamped accurately for the second touch, within the 120-line cap.
  - **ADVISORY (non-gating):** the new structural test only exercises the `request_status_id: 2` branch; cases 1 and 3 have identical markup (verified) but a regression touching only one of them wouldn't be caught by this test alone. A cheap `describe.each([1,2,3])` would close that gap — not required, doesn't count toward rework.
- **Requirements covered:** `NOTIF-R-3`, `NOTIF-R-4`, `NOTIF-R-9`, `NOTIF-AC-2`.
- **Decisions made:** accepted Tailwind-on-template (not SCSS) as the row-shell fix, per the Reviewer's own preferred remediation, which also structurally forecloses the class of bug that caused attempt 1's FAIL; accepted `data-testid` over `id` for test hooks going forward in this component.
- **Issues encountered:** two real rework rounds' worth of findings resolved in one attempt (dead selector + duplicate ids).
- **Final verification result:** 39/39 tests passing, lint clean, folder doc current.

---

## 3. Forward pointers for later tasks (carry these into the brief when spawning)

- **`NOTIF-T-5`:** must add `GroupNotificationsByRecencyPipe` to `notification-item.module.ts`'s pipe declarations/exports (or wherever the sibling filter pipes are registered) — it is `standalone: false` and currently unregistered anywhere. Use the real pipe name `groupNotificationsByRecency` in templates, not `groupByRecency`. Consider the DST boundary-arithmetic fix from `NOTIF-T-2`'s ADVISORY while touching this area (optional, non-gating).
- **`NOTIF-T-6` / `NOTIF-T-7`:** the first file that imports `@spartan/badge` (or `@spartan/checkbox`) will make it appear in `installedComponents` for the first time — expected, not a surprise regression.
- **`NOTIF-T-6`:** `design.md` §6.3's checkbox a11y line was corrected — Spartan `checkbox` is an ARIA `role="checkbox"` `<button>`, not a native `<input>`. Don't let a Reviewer fail this on a literal-text technicality.
- **`NOTIF-T-6`:** must add `FilterNotificationByCenterPipe` and `FilterNotificationByBilateralProjectPipe` to `notification-item.module.ts`'s declarations/exports (same module as `GroupNotificationsByRecencyPipe` above) — both are `standalone: false` and currently unregistered anywhere; not caught by `tsc --noEmit`, only by `npm run build`. Source Center facet options from the same `clarisa_institution.id` path the pipe filters on (an id-space mismatch would silently zero out the facet). Populate the Bilateral-project facet from bilateral rows only — `result_code` also appears on non-bilateral rows, so the pipe alone won't scope it correctly.
- **`NOTIF-T-8`:** eyeball the segmented Received/Sent tab labels during the manual visual pass — `NOTIF-T-4`'s Helm-verbatim classes render them at `text-sm` (~10.5px on this codebase's 12px root), a ~1.5px shrink from the replaced `pr-body-2` (12px). Not a defect (mandated by `NOTIF-DD-1`), just worth confirming it still reads fine against the mockup.
- **`NOTIF-T-8`:** confirm the `notification-item` row genuinely renders compact/single-line with the avatar beside the text (not stacked) at both desktop and tablet widths — **already confirmed live by the user** after `NOTIF-T-7`'s attempt-3 fix and a dev-server restart; re-check only if a future change touches this file again.
- **`NOTIF-T-8`:** exercise the Filter popover (all facets + chips + clear-all) on BOTH the Requests and Updates tabs explicitly — `NOTIF-T-6`'s Reviewer flagged that no automated test renders the popover's interior, and this exact class of tab-leak defect took two rework rounds to fully close.
- **`NOTIF-T-8`:** eyeball "Clear all" against `NOTIF-AC-4` — it also nulls `phaseFilter` (pre-existing `clearAllFilters()` behavior, unchanged by `NOTIF-T-6`, but worth confirming the resulting empty state reads as intended, per `NOTIF-T-6`'s Reviewer ADVISORY).
- **Non-blocking cleanup, not gating for `NOTIF-T-8`:** `ResultsNotificationsService.hideInitFilter` appears to be dead code (no template reference found by `NOTIF-T-6`'s Reviewer) — confirm "Clear all" still visually resets the Program/Entity select during the manual pass, then it's safe to delete the flag + its `setTimeout` dance in a follow-up. Also: the Filter popover's `[autoFocus]="false"` was copied from an existing pattern without the focus-management code that pattern pairs it with elsewhere in the app — minor WCAG 2.4.3 friction (focus doesn't move into the popover on open), not a failure, worth a look during the manual keyboard-operability check.

---

## 3a. `NOTIF-T-5` — Regroup Received/Sent by recency (in progress — attempt 1 FAILed)

- **Status:** rework in progress (attempt 1 of 3)
- **Date:** 2026-09-25
- **Skills assigned:** `angular-developer`, `spartan`. **Effort:** medium (attempt 1) → **high** (attempt 2, per the loop's "bump one level on retry" rule).
- **Attempt 1 — Implementer:** merged Pending∪Done via plain `.concat()`, filtered with existing pipes, grouped via `groupNotificationsByRecency`, three `@if` blocks per bucket with Helm `badge` count pills. Dropped `@defer (on viewport)` (cited `IntersectionObserver` not polyfilled in jsdom as the reason). `npx jest --testPathPattern="(received|sent)-requests.component.spec"` → 10/10 passed. Lint clean.
- **Attempt 1 — Reviewer verdict: `FAIL`** (full findings below, carried verbatim per the Structured Feedback rule):

  1. **Within-bucket ordering is no longer `requested_date` descending — the merge breaks the pipe's own sortedness premise.** `received-requests.component.html`/`sent-requests.component.html` build the merged list via plain `.concat()`; `ResultsNotificationsService` sorts the Pending and Done arrays **independently** desc, so the concatenation is only piecewise sorted. `GroupNotificationsByRecencyPipe` documents and relies on the input already being fully desc-sorted and does not re-sort. Result: inside any bucket holding both pending and decided rows (routinely the Today bucket), every pending row renders above every decided row regardless of actual date.
     - **Violated Rule:** `requirements.md` §8 `NOTIF-AC-1` — "in `requested_date` descending order within each group." Also `NOTIF-T-2`'s stated input premise.
     - **Why the green suite missed it:** the fixture's two rows land in *different* buckets, so intra-bucket order is never asserted.
     - **Remediation:** restore the invariant before/at grouping — preferred: have `GroupNotificationsByRecencyPipe` sort each output bucket desc by `dateKey` (pure, no template mutation), or merge the two already-sorted arrays in date order rather than concatenating. Do NOT `.sort()` inline in the `@let` (mutates the service-owned arrays in place). Add a test with two same-bucket rows (one pending older, one done newer) asserting DOM order.

  - **ADVISORY (non-gating):** the dropped `@defer (on viewport)` is ruled an acceptable simplification, NOT a FAIL — no NFR in `design.md`/`requirements.md` requires it, `NOTIF-DD-2` deletes the section it lived in by design, and the lists are unpaginated/in-memory already. But the Implementer's stated *reason* ("jsdom has no `IntersectionObserver`") is wrong and must not be recorded as precedent — Angular's `TestBed`/`DeferBlockFixture` supports `@defer` testing without a real `IntersectionObserver`. Correct the justification to "deliberate simplification, no perf requirement" if this is revisited.
  - Also flagged (non-gating): `apiServiceMock: any` declared-but-never-assigned in both new spec files (works only because the stub component never dereferences `api` — legible fix, not a functional issue); `groupByRecency` naming is stale in `design.md`/`tasks.md` (already tracked, not this task's fix to make on this branch).

**Next:** attempt 2 — re-spawning Implementer now with this feedback verbatim + effort bumped to `high`.

---

## 3b. Post-PASS defect: `NOTIF-T-7` row-shell layout still broken live (user-reported)

- **Date:** 2026-09-25
- **Discovered by:** the user, testing in a hard-refreshed live browser — NOT caught by either Reviewer pass on `NOTIF-T-7` (both verified DOM/class structure only; neither could compute layout, and both said so explicitly as an ADVISORY/limitation).
- **Symptom:** the avatar/entity icon rendered ABOVE the row text instead of beside it, despite attempt 2's Reviewer-verified fix (Tailwind `flex items-center gap-[12px]` on the correct template element).
- **Root cause (working theory, not independently confirmed against the live bundle):** a Tailwind JIT/arbitrary-value class (`gap-[12px]`) likely wasn't reliably present in the live dev-server's compiled CSS, even though the class was correctly written in the template and verified structurally by both automated tests and a Reviewer.
- **Fix (attempt 3):** removed the Tailwind utility classes from the template; added `display:flex; align-items:center; gap:12px` as a direct, unconditional SCSS property block on `.notification_content_body` (compiled with the component, no separate JIT step to race). Updated the structural test to assert the Tailwind classes are gone and the layout hook now lives on the class-only div. Re-stamped `notification-item/CLAUDE.md`'s `Verified:` line a third time with an honest account of all three attempts.
- **Verification:** `npx jest --testPathPattern=notification-item.component.spec` → 39/39 (this component) + 2 unrelated pre-existing files also green (68 total in the pattern match). Lint clean.
- **Reviewer verdict: `PASS`.** Independently traced the selector by hand: `.notification_content_body { display:flex; ... }` is a plain, unconditional property block nested correctly under `.notification .notification_content`, with no modifier-class indirection (the exact bug class from attempt 1) — this is now compiled into the component stylesheet, no Tailwind-JIT dependency at all. Confirmed zero Tailwind utility remnants on the div. Confirmed the updated test is an honest, falsifiable DOM claim, not a tautology, and explicitly does not overclaim visual proof.
  - **Explicitly flagged by the Reviewer, carried forward verbatim:** *"No automated check in this repo can certify the property the user actually reported... 'the avatar renders beside the text' remains proven only by the user's hard-refreshed browser. Two previous attempts passed review and failed live, so `NOTIF-T-7` should not be treated as closed until that manual confirmation (`NOTIF-T-8`) lands."*
- **Status: CONFIRMED CLOSED by the user, live, after a full `ng serve` restart.** The user tested before/after hard-refresh (avatar still stacked — bundle not yet rebuilt) and after a full dev-server restart (avatar correctly renders beside the text; `NG8002` gone). Both symptoms — the layout defect and the `NG8002` error — were caused by the same thing: a stale/corrupted incremental-compiler cache from several concurrent Implementers writing to `notification-item.module.ts`/`.html`/`.scss` in quick succession across `NOTIF-T-2`/`T-5`/`T-7`'s three attempts, not a code defect. Leader's hypothesis confirmed; no further action needed on either symptom.
- **Fixed and reviewed:** the decided-state (`request_status_id` 2/3) status area looked visually messy — the pre-existing "Status: Accepted by … on …" text (styled as a bordered/rounded box, `.notification_content_actions_status`) and the new `NOTIF-T-7` decision-chip badge rendered as two visually competing, differently-shaped boxes stacked on top of each other, not the mockup's single clean pill. Fix: dropped the boxed/bordered treatment (`border-left`/`border-right`/`border-radius`/`padding`) from `.notification_content_actions_status`, replaced with plain caption styling (`text-align:right`, typography `body-3`, `color: var(--pr-color-accents-5)` — an existing token, already used elsewhere in this file); added `gap: 6px` on the parent flex column to space the caption from the chip cleanly. `NOTIF-R-4`/`NOTIF-DD-4`'s requirement to keep the who/when text is fully preserved (verified: template text/inline accent colors untouched, nothing hidden). 68/68 tests pass unmodified (pure CSS-property change, no structure change). Lint clean.
  - **Reviewer verdict: `PASS`.** Confirmed token discipline, chip untouched, scope clean, regression plausibility (no test could have depended on the removed border/padding). **Non-blocking observation carried forward:** `body-3` is 10px on this codebase's 12px root — smaller than the `.notification_date` line below it (`body-2`, 12px) — the "by X on date" caption may read as small or wrap more than intended inside the 280px column. Same caveat as every prior `NOTIF-T-7` visual fix: rendered layout can't be computed in a read-only review — final word belongs to the user's live browser.
  - **User confirmed live, 2026-09-25:** screenshot at `/result/results-outlet/results-notifications/requests/received` shows the avatar correctly inline beside the text, the "This week"/"Earlier" recency groups with count pills, and the "Accepted" decision chip reading clean (single pill, no competing boxes) — the `body-3` caption size read fine, no follow-up requested. `NOTIF-T-7` and its two post-PASS live defects are now genuinely closed, not just review-closed.

---

### `NOTIF-T-6` — Filter popover (Phase + Program + Center + Bilateral project)

- **Final status:** PASS (attempt 3 of 3)
- **Date:** 2026-09-25
- **Skills assigned:** `spartan`, `angular-developer`, `tailwind-design-system`. **Effort:** xhigh (attempt 1) → **max** (attempt 2).
- **Attempt 1 — Implementer:** replaced the always-visible Phase/Program/Search/Clear-filters row with a Spartan `hlm-popover` Filter button (badge count, `aria-expanded`) containing the unchanged Phase/Program selects plus new Center/Bilateral-project `hlm-checkbox` multi-select lists, sourced live from `receivedData`/`sentData` (not a separate catalog, avoiding the id-space risk `NOTIF-T-3`'s Reviewer warned about); active facets as removable chips; wired the two new filter pipes into `received`/`sent`-requests' existing chains; added filter state to `ResultsNotificationsService`. Proactively ran a full `ng build` and caught/fixed a real template-type-checker error (two pipes' local types were structurally unrelated when chained — widened both to `any[]`, narrowed type kept on the internal `.filter()` callback). `npx jest` combined regression sweep → 168/168 passed across 14 suites. Lint clean, build clean.
- **Attempt 1 — Reviewer verdict: `FAIL`** (`full` review, per this task's own designation; effort max). Findings carried verbatim:

  1. **The new Filter popover, its two new facets, and the chips render on the Updates tab too, where they filter nothing.** `results-notifications.component.html`'s filter block is still gated only on `*ngIf="!router.url.includes('/results-notifications/settings')"` — i.e. it renders on Requests AND Updates. `updates.component.html` does not (correctly) chain the two new pipes, and the facet options are derived from Requests-only data (`receivedData`/`sentData`), so on Updates the user sees checkbox facets built from data that has no effect on the list in front of them — and worse, a chip/active-count badge can stay on screen across a tab switch while the visible list is demonstrably unfiltered, actively misrepresenting state.
     - **Violated Rule:** `requirements.md` §3 In scope ("...only while the Requests tab is active"); `tasks.md` `NOTIF-T-6` Description (same); `requirements.md` §8 `NOTIF-AC-5` ("Updates renders exactly as it does today — confirms this spec did not regress the sibling tab").
     - **Why the green suite missed it:** all 8 new component tests exercise the class directly; none renders the template under an Updates URL — this class of defect is structurally invisible to the suite as written (the same structural blind spot the `NOTIF-T-5` and `NOTIF-T-7` Reviewers each independently flagged in their own reviews).
     - **Remediation:** scope the new popover+chips+"Clear all" cluster to the Requests branch only (e.g. wrap in `@if (router.url.includes('/results-notifications/requests'))`), keeping the pre-existing Phase/Program selects + old Clear-filters affordance rendering for Updates (or at minimum hide the Center/Bilateral-project sections + their chips outside Requests). Add spec assertions for both branches (Requests URL → popover present; Updates URL → the two new facet sections absent).
  2. **`notification-item.module.ts` was edited (registered two new pipes) without updating/re-stamping that folder's own `CLAUDE.md`.** Violates the documented folder-doc convention (`onecgiar-pr-client/CLAUDE.md` §10, `onecgiar-pr-client/docs/COMPONENT-DOCS.md`) — a future maintainer has no pointer to where `FilterNotificationByCenterPipe`/`FilterNotificationByBilateralProjectPipe`/`GroupNotificationsByRecencyPipe` are registered.
     - **Remediation:** add 2-3 lines to `notification-item/CLAUDE.md` noting this module is the registration home for the folder's filter pipes plus `GroupNotificationsByRecencyPipe`, and re-stamp `Verified:`.

  - **Verified clean (the two highest-risk items from the brief, both confirmed correct):** Phase-gating hard carryover fully preserved (traced by hand — `[disabled]`, tooltip, placeholder, `onPhaseChange` binding, P2-3106 default-phase bootstrap all untouched); facet/pipe field consistency correct (`centerFacetOptions` reads the byte-identical path the pipe filters on; `bilateralProjectFacetOptions` correctly scoped through a private `isBilateralRow` helper that is byte-identical to `notification-item.component.ts`'s own `isBilateralResult` getter, not just similarly named).
  - **Also ruled clean/acceptable (no action needed):** state correctly lives in the service, not the component; `resetFilters()`/`clearAllFilters()` chain traced and covers both new facets; pipe-chain position correct in both templates, doesn't disturb `NOTIF-T-5`'s merge/sort; module registration has no clobbering; the `any[]` pipe-type widening is a genuine template-type-checker artifact (verified against the two pipes' actual type shapes), not a cover for a field-shape bug; a11y verified at the library level (Escape-dismiss traced into `@spartan-ng/brain`'s actual overlay source, not taken on trust); no hardcoded hex, correct arbitrary-px Tailwind usage; the defensive move of putting the OUTER layout in plain SCSS (not Tailwind) was already taken, and the one new Tailwind utility class used is already present in 90+ other places in the live bundle (low risk of the `NOTIF-T-7`-style JIT-timing bug repeating here); icon choice (`material-icons-round`, not Lucide) ruled correct per an existing adopted precedent (`onecgiar-pr-client/docs/DESIGN-DEVIATIONS.md` §15, "nearest existing siblings" rule) — the file's own pre-existing tab icons already use `material-icons-round`; bilateral-project chip label format ruled reasonable (mirrors the existing search-pipe's own label construction).
  - **ADVISORY (non-gating):** `activeFilterChips` calls the two facet-option getters once per chip (O(chips × rows) per CD tick) — fine at today's sizes, `computed()` signals would be the fix if lists grow; `ResultsNotificationsService.hideInitFilter` appears to be dead code (no template reference found) — flagged for `NOTIF-T-8`'s manual pass to confirm "Clear all" still visually resets the Program select, then delete the flag; `[autoFocus]="false"` on the popover was copied from `program-overview`'s pattern without the accompanying focus-management code that flag exists to enable there (WCAG 2.4.3 friction, not a failure); a `gap-[16px]`/`p-[16px]` Tailwind override could lose a Helm-baked-in `gap-4`/`p-4` collision (Helm classes concatenate, don't `tailwind-merge`) — cosmetic, not gating; facet options pool across both Received AND Sent data (deliberate, per the Falsifier's "narrows both lists identically" requirement) — flagged only so it isn't "fixed" away later by accident; `clearAllFilters()` also nulls `phaseFilter` (pre-existing behavior, unchanged by this task) — worth an eyeball against `NOTIF-AC-4` at `NOTIF-T-8`.

- **Attempt 2 — Implementer:** wrapped the entire Filter-popover cluster in `@if (router.url.includes('/results-notifications/requests'))`, restored the pre-`NOTIF-T-6` Phase/Program/Clear-filters row verbatim in the `@else` branch for Updates, moved the Search input outside the `@if`/`@else` to render on both tabs, added branch-scoping spec tests, updated `notification-item/CLAUDE.md` additively. `npx jest` → 19/19 (component) + 14/14 (received/sent, no regression). Lint clean, `ng build` succeeded.
- **Attempt 2 — Reviewer verdict: `FAIL`** (one issue; effort max). Finding carried verbatim:

  1. **The Updates tab no longer renders "exactly as it does today" — Search and "Clear filters" have swapped visible positions.** The old row was Phase | Program | Search | Clear filters. Moving the Search input to a single shared copy AFTER the `@if`/`@else` block makes Updates render Phase | Program | **Clear filters | Search** instead — because `.notifications_filters` is `display:flex; justify-content:space-between`, the "Clear filters" button visibly moves off the right edge and Search takes its place. The Search wrapper's own markup also changed (`style="width: 100%"` on a plain div → `flex min-w-[220px] flex-col gap-[4px]` wrapper + `class="w-full"` on the input) — a cosmetic change to a control that's supposed to be untouched on this tab.
     - **Violated Rule:** `requirements.md` `NOTIF-AC-5` — "Updates renders exactly as it does today (unchanged)."
     - **Why the tests missed it:** the new branch-scoping tests check presence/absence of the popover and `.clear_filters_container`, but nothing asserts on their RELATIVE ORDER — so a swap in visual position between two elements that are each individually still present passes both tests.
     - **Remediation:** don't share one copy of the Search input across both branches — put a copy of it back INSIDE each branch, in its original relative position: in `@else`, restore the original Search markup verbatim between the Program `<div>` and `.clear_filters_container` (both bind the same `searchFilter`, so behavior is identical, only the position changes); keep the current Search block in the `@if` branch, positioned after the cluster as already landed. Add a DOM-order assertion to the Updates test (e.g. `compareDocumentPosition`) so this can't silently regress again.
  - **Verified clean (do not touch again):** the `@if`/`@else` wrapping itself, the `@else` branch's Phase/Program/Clear-filters restoration (byte-identical to the pre-`NOTIF-T-6` row), the Phase-on-Updates reasoning (`onPhaseChange`'s re-fetch, not a template pipe, so both branches calling it is sufficient), everything else in the popover cluster (a11y attributes, checkbox wiring, icons, chip format) spot-checked byte-identical to the attempt-1-reviewed state. `notification-item/CLAUDE.md` fix — **PASS**, additive, correctly re-stamped, 118/120 lines. The new branch-scoping tests are honest and would have caught attempt 1's original bug — only the order-of-elements gap is new.

- **Attempt 3 — Implementer:** stopped sharing one Search copy across branches; gave each branch its own copy in its original relative position — Requests keeps its Tailwind-restyled copy after the filter cluster; Updates gets the original markup restored verbatim between the Program select and `.clear_filters_container` (Phase | Program | Search | Clear filters, exact pre-`NOTIF-T-6` order and inline styling). Added a genuine `compareDocumentPosition` DOM-order assertion to the Updates test. `npx jest` → 20/20 (component, 19 pre-existing + 1 new) + 14/14 (received/sent, no regression). Lint clean, `ng build` succeeded.
- **Attempt 3 — Reviewer verdict: `PASS`.** Traced the Updates branch by hand line-by-line (Phase L168-180 → Program L182-195 → Search L197-200, original markup, character-for-character → Clear filters L202-210) — genuinely matches the pre-`NOTIF-T-6` order and styling. Confirmed the new DOM-order test is real (queries actual DOM nodes, `NO_ERRORS_SCHEMA` means no other custom-element input could produce a false match) and would have caught attempt 2's exact regression (traced through the bitmask logic: `PRECEDING & FOLLOWING = 0`, assertion correctly fails). Spot-checked the rest of the popover/facets/chips/a11y — all match the attempt-2-reviewed state, nothing else drifted.
- **Requirements covered:** `NOTIF-R-5`, `NOTIF-R-6`, `NOTIF-R-7`, `NOTIF-R-10`, `NOTIF-AC-3`, `NOTIF-AC-4`, and (via the tab-scoping fix) `NOTIF-AC-5`.
- **Decisions made:** accepted per-branch Search copies (not a shared one) as the correct fix for a positional/styling difference between two tabs with genuinely different filter surfaces; accepted deriving Center/Bilateral facet options live from loaded data (not a separate catalog service) to guarantee id-space consistency with the filter pipes; accepted `material-icons-round` over Lucide for this file's new icons, per an existing adopted "nearest sibling" precedent.
- **Issues encountered:** three rework rounds, each catching a real, distinct defect — (1) tab-scoping leak onto Updates, (2) Search/Clear-filters position swap introduced while fixing (1), (3) none — attempt 3 closed cleanly. This was the highest-risk task in the spec and used the full rework budget; no HALT needed.
- **Final verification result:** 20/20 + 14/14 tests passing, lint clean, `ng build` succeeded across all three attempts' final states.

---

### `NOTIF-T-8` — Bell/badge alignment + full manual verification (code portion)

- **Final status (code portion):** PASS (attempt 1). **Manual QA portion: pending user.**
- **Date:** 2026-09-25
- **Skills assigned:** `tailwind-design-system`, `angular-developer`. **Effort:** medium.
- **Files changed:**
  - `shell-topbar.component.scss` — badge border/digit color tokenized (`#fff` → `var(--pr-color-white, #fff)`); sizing/position confirmed already correct against the mockup, untouched.
  - `shell-topbar/CLAUDE.md` — folder-doc convention: re-stamped `Verified:`, one new Traps bullet recording the change.
  - `results-notifications.service.ts` (+ `.spec.ts`) — removed confirmed-dead `hideInitFilter` flag and its `setTimeout` reset dance, per the forward-pointer `NOTIF-T-6`'s Reviewer left in this file.
- **Implementer verification:** bell/badge: compared pixel values against the mockup reference image and README spec — genuinely already matched (32px button, 16px badge, 2px border), no layout change made, only the color-literal cleanup. `hideInitFilter`: grepped the whole client `src/` tree, zero references found before removing. `npx jest --testPathPattern=shell-topbar.component.spec` → 43/43. `npx jest --testPathPattern=results-notifications.service.spec` → 28/28. Lint clean. `ng build` succeeded.
- **Reviewer verdict: `PASS`.** Independently confirmed `--pr-color-white` exists and resolves to `#fff`; confirmed no sizing/position values were touched (diff is 2 lines); confirmed the `var(x, #hex)` fallback convention is used 50 times elsewhere in the same file, so leaving `--pr-color-primary-300`'s own hex fallback untouched was correct restraint, not an inconsistency (pre-existing bare hex literals elsewhere in the file are out of this task's scope, not a FAIL); confirmed updating the un-listed `shell-topbar/CLAUDE.md` was correct per the folder-doc convention, not a shared-file-write-discipline violation (that discipline only protects root guides/personas/templates/TRD, not component folder docs — and `tasks.md`'s own file list already named this folder); confirmed `hideInitFilter` is genuinely fully gone (independent grep, zero matches) and `resetFilters()`'s other behavior (including `NOTIF-T-6`'s `centerIdsFilter`/`bilateralProjectIdsFilter` resets) is unaffected.
- **Requirements covered:** `NOTIF-R-8`, `NOTIF-AC-6` (code portion only — visual confirmation pending).
- **Decisions made:** accepted "no layout change needed, already matches" as a valid, honest outcome rather than requiring the Implementer to invent changes; accepted the `hideInitFilter` cleanup as in-scope loose-end tidying for the spec's last task.
- **Issues encountered:** none.
- **Final verification result (code portion):** all tests passing, lint clean, build succeeds. **The task's own Definition of Done items — the desktop/tablet manual check and the Updates-tab regression confirmation — remain open pending the user's manual sweep (see below).**

---

### `NOTIF-T-9` (unplanned defect-fix task) — Filter popover mis-anchored + avatar content/shape mismatch

- **Final status:** PASS (attempt 1)
- **Date:** 2026-09-25
- **Discovered by:** the user, live, after `NOTIF-T-6`/`NOTIF-T-7` were already PASSed — comparing the running app against the actual mockup source (not just the earlier screenshot): (1) Filter popover panel rendered centered in the viewport instead of anchored below the Filter button, and was 340px wide instead of the mockup's 280px; (2) row avatars showed a generic person/building icon instead of the requester's initials (individual rows) or a distinctly-shaped entity icon (bilateral rows).
- **Skills assigned:** `angular-developer`, `spartan`, `tailwind-design-system`. **Effort:** high.
- **Fix 1 root cause (genuinely found, not guessed):** `hlm-popover`'s `[state]`/`(stateChanged)`-controlled pattern (used by both this button and the `program-overview` exemplar) never calls `BrnOverlay.setOrigin()` — that's only called by the `hlmPopoverTrigger` directive, which neither consumer uses (both need a custom `(click)` toggle instead). With no origin and no `attachTo` bound, `BrnOverlayService.open()` deterministically falls back to `global().centerHorizontally().centerVertically()` — a fixed code path, not a timing race, and it fully explains the symptom on any viewport width. Fix: added a `#filterTriggerBtn` template ref + `filterTriggerRef = viewChild<ElementRef<HTMLButtonElement>>(...)` + `[attachTo]="filterTriggerRef()"` on `<hlm-popover>`. Width moved to plain SCSS (`.notifications-filter-popover-content { width: 280px; max-width: calc(100vw - 32px); }`), matching the mockup source (`mockups/PRMS-Reporting.dc.html`'s `ntFilterOpen` panel) exactly.
- **Fix 2:** individual-requester rows now show initials (computed inline in the template from `obj_requested_by.first_name`/`.last_name`, no `.ts` change) inside a circle; bilateral rows keep the `pi-building` icon but now inside a rounded square (8px radius) — visually distinct shapes, matching the mockup's own avatar-vs-entity-icon treatment. Avatar size corrected 32px → 28px per the mockup.
- **Implementer verification:** `npx jest --testPathPattern="results-notifications.component.spec|notification-item.component.spec"` → 93/93 passed. Lint clean. `ng build` succeeded.
- **Reviewer verdict: `PASS`.** Independently re-verified the library-internals claim by reading the actual compiled `@spartan-ng/brain` source (not trusting the Implementer's trace) — confirmed the exact code path (`getAttachTo()` → `getPositionStrategy()` → `BrnOverlayService.open()`'s fallback branch) and ruled it a deterministic bug, more credible than the JIT-timing hypothesis the Leader originally suspected (a missing utility class changes styling, not the CDK position strategy — it can't explain centering at all). Confirmed the `viewChild`/`[attachTo]` binding is safe (the `[state]` effect that calls `open()` only runs after view-init, and the popover's own `if (!attachTo)` guard handles an initial `undefined`). Confirmed 280px matches the mockup source exactly. Confirmed avatar fix: `.ts` untouched, tests assert real content (initials text, icon presence/absence, class names) not just wrapper presence.
  - **One inline correction applied by the Leader post-review (cheap, no rework attempt consumed):** the Implementer's code comment claimed `!important` was "load-bearing" because Helm classes only concatenate — the Reviewer found this specific claim factually wrong for `classes()`-based Helm directives like `hlm-popover-content` (it DOES `tailwind-merge`; the real reason `!important` isn't even needed here is that a non-Tailwind-utility class name can't be seen by twMerge's conflict detection at all, so cascade order alone already wins). Corrected the comment in place; the CSS itself (including the harmless, belt-and-braces `!important`) was untouched.
  - **ADVISORY (non-gating):** setting `[attachTo]` also fixed a latent side-effect — before, clicking the trigger while the popover was open triggered CDK's outside-dismiss (closing it) before the click handler re-opened it, likely causing a flicker/reopen; now trigger clicks are correctly recognized as "within origin." Worth including in `NOTIF-T-8`'s manual check. **Sibling bug found, out of scope, flagged for a follow-up ticket:** `program-overview`'s Scope popover has the identical latent `attachTo` bug (same controlled-overlay pattern, same missing binding) — not touched here, recommend a separate fix.
- **Requirements covered:** `NOTIF-R-5` (filter popover functions correctly), `NOTIF-R-3` (avatar/icon fidelity).
- **Decisions made:** accepted the `viewChild`+`[attachTo]` fix over any Tailwind-JIT-timing workaround, since the real root cause was found and verified; accepted 28px avatar size and initials-vs-icon shape distinction per mockup fidelity.
- **Issues encountered:** none — PASSed on first attempt despite being a structurally significant fix, because the Implementer did real diagnostic work (traced library source) rather than guessing.
- **Final verification result:** 93/93 tests passing, lint clean, build succeeds. **Visual anchoring/positioning at desktop and tablet widths still needs the user's live confirmation** (same as all prior visual fixes in this spec) — added to the `NOTIF-T-8` manual-check list below.

---

### `NOTIF-T-9` (continued) — Accept/Decline button restyle per `NOTIF-DD-6`

- **Final status:** PASS (attempt 2 of 3)
- **Date:** 2026-09-25
- **Skills assigned:** `spartan`, `tailwind-design-system`. **Effort:** medium (attempt 1) → **high** (attempt 2).
- **Attempt 1 — Implementer:** `[showBackground]="false"` + a new class on each button (`notification_accept_btn`/`notification_decline_btn`), with `::ng-deep .notification_accept_btn .pr_button { border: ...; border-radius: 8px; background-color: ... }` and `::ng-deep .notification_decline_btn .pr_button { .text, i { color: var(--pr-color-accents-5); } }`. `npx jest --testPathPattern=notification-item.component.spec` → 71/71 passed (no test changes needed). Lint clean, build succeeded.
- **Attempt 1 — Reviewer verdict: `FAIL`** (finding carried verbatim):

  1. **The Decline colour override loses the CSS cascade — Decline's text/icon will stay violet, not turn gray.** `pr-button` uses Angular's emulated encapsulation, so its base rule compiles with an `[_ngcontent-x]` attribute selector attached to every simple selector: `.pr_button[_ngcontent-x] .text[_ngcontent-x]` (specificity 0,4,0) and `... i[_ngcontent-x]` (0,3,1). The new `::ng-deep .notification_decline_btn .pr_button .text` rule gets NO attribute added by `::ng-deep`, so it compiles to (0,3,0) for `.text` and (0,2,1) for `i` — LESS specific in both cases, so the base (unwanted) rule wins. **The same problem, milder, affects Accept's `border-radius: 8px`** — (0,2,0) against pr-button's own (0,2,0) `border-radius: 5px`; the winner then depends on stylesheet injection order, which may render 5px instead of 8px.
     - **Violated Rule:** `design.md` §12 `NOTIF-DD-6` ("Consequences: Accept/Decline now visually match the mockup") — the override as written doesn't produce that result.
     - **Why the green suite missed it:** jsdom doesn't compute cascade/specificity resolution across encapsulated stylesheets; a class-presence assertion can't catch a losing specificity battle.
     - **Precedent already in this codebase for the exact same problem:** `onecgiar-pr-client/src/app/pages/bilateral/components/section-evidence/section-evidence.component.scss` (lines ~314-322) overrides `.pr_button .text, i { color: ... !important }` for exactly this reason; `rd-evidences.component.scss` and `sync-button.component.scss` do the same.
     - **Remediation:** add `!important` to the Decline `color` declaration and the Accept `border-radius` declaration (matching the house precedent), OR raise selector specificity (e.g. `:host ::ng-deep app-pr-button.notification_decline_btn .pr_button .text` — the `:host` prefix also stops the rule from leaking app-wide, a nice side benefit). Update `notification-item/CLAUDE.md`, which currently states the Decline color override works — it needs correcting alongside the code fix. Prove it with `getComputedStyle` reasoning since jsdom can't show the cascade result directly (this stays a live-browser-confirmable item, same as every other visual fix in this spec).

- **Attempt 2 — Implementer:** added `!important` to every declaration in both `::ng-deep` override rules (Decline's `.text`/`i` color, Accept's `border`/`border-radius`/`background-color`). Confirmed the precondition (`pr-button.component.scss` has zero `!important` anywhere, so nothing competes with the fix). Corrected `notification-item/CLAUDE.md`'s attempt-1 claim that the override "worked as written." `npx jest --testPathPattern=notification-item.component.spec` → 71/71 passed unmodified. Lint clean, build succeeded.
- **Attempt 2 — Reviewer verdict: `PASS`.** Independently re-verified the CSS mechanics (`!important` wins regardless of specificity when not competing against another `!important`) and the precondition (read all 87 lines of `pr-button.component.scss`, confirmed zero `!important`). One factual correction to the Implementer's own reasoning, non-gating: the three "precedent" files don't actually use a BARE top-level `::ng-deep` as claimed — each nests it under a component-specific selector; the real shared convention across all three is `!important` on every overriding declaration, not the exact selector shape. The fix itself is still safe (the two new classes are unique to these two buttons, can't leak), just the code comment's characterization of "why bare `::ng-deep` matches the precedent" was overstated. Corrected the comment in place (Leader, post-review, no rework attempt consumed) rather than spending a third attempt on a wording-only nit.
- **Requirements covered:** the visual-fidelity request behind `NOTIF-DD-6` (Accept: bordered/white/accent-text; Decline: plain muted-gray text, no border/background).
- **Decisions made:** accepted `!important` (matching established codebase precedent for this exact `.pr_button` override problem) as the correct fix, over raising selector specificity via `:host ::ng-deep`.
- **Issues encountered:** one real rework round (a CSS specificity bug that would have silently failed to apply visually despite passing all automated tests) — exactly the kind of defect this spec's visual fixes keep surfacing, caught here before it reached the user.
- **Final verification result:** 71/71 tests passing, lint clean, build succeeds. **Visual confirmation (Accept bordered/white, Decline plain gray) still needs the user's live check**, same as every other visual fix in this spec.

---

### `NOTIF-T-10` (unplanned, user-directed task) — Row restructure to match an updated reference file

- **Final status:** PASS (attempt 2 of 3)
- **Date:** 2026-09-25
- **Trigger:** the user provided a NEWER reference design file (`C:/Users/santiagosanchez/Downloads/PRMS Reporting_Tool_interface_(1)/PRMS Reporting.dc.html`, different from/superseding the spec's original `mockups/PRMS-Reporting.dc.html`) and confirmed it should be followed exactly, after live testing surfaced several mismatches: header chip pills (Requester/Responder/bilateral source) had no equivalent in the reference; each row was individually card-styled instead of one flat list with border-bottom separators; button labels read "Accept contribution"/"Decline contribution" instead of "Accept"/"Decline"; the decided-state caption sat in a separate right-side column instead of under the main text.
- **Skills assigned:** `angular-developer`, `spartan`, `tailwind-design-system`. **Effort:** max (attempt 1, held at max for attempt 2 — already at ceiling).
- **Attempt 1 — Implementer:** removed the header-chip block from all 3 `@switch` cases; restructured the row into avatar → text column (main sentence + decided-state caption below it) → timestamp → actions/chip; moved the "card" look from each row onto the group container (`received-requests`/`sent-requests`'s `.received_container_content_item`/`.sent_container_content_item`); shortened button labels to "Accept"/"Decline" (noting this supersedes an earlier ticket, P2-3106); updated 2 tests to match the new DOM shape, left every accept/decline/ToC-dialog/business-logic test unmodified. `npx jest` → 86/86 passed. Lint clean, build succeeded.
- **Attempt 1 — Reviewer verdict: `FAIL`** (`full` review, effort max). Findings carried verbatim:

  1. **The "no centering bug found" conclusion was wrong — a real, missed root cause.** `pr-button.component.html` always renders a `<div class="filter">` (not behind `*ngIf`), and `pr-button.component.scss` only positions it when `.show_background` is present. With `[showBackground]="false"` (both notification buttons), `.filter` stays a normal zero-width flex item, and `.pr_button`'s `gap:10px` still puts 10px between it and the text — pushing the label ~5px left of true center on both buttons. The Implementer correctly ruled out the icon (`*ngIf`-removed, no phantom space) but missed this second always-rendered element.
     - **Remediation:** `::ng-deep .notification_accept_btn .pr_button .filter, ::ng-deep .notification_decline_btn .pr_button .filter { display: none !important; }` (safe — `.filter` does nothing when `showBackground` is false; preferred over `gap:0` since the loading-icon state still needs the gap). Correct the now-wrong SCSS comment and `CLAUDE.md` bullet claiming no bug existed.
  2. **Pending rows still draw their own violet border/tint — breaks the "one flat list, border-bottom only" result the whole restructure was for.** `notification-item.component.html`'s `[ngStyle]` on the outer `.notification` element (`border: 1px solid var(--pr-color-primary-100)` + tinted background when `request_status_id === 1`) was left in place from before this task — every pending row in Received/Sent draws a violet box on all four sides inside the new grouped container, visible as a tinted strip around the `toc_review` block's own margins, and doubles up with `:host(:last-child)`'s border-bottom removal (which only targeted the wrong element).
     - **Violated Rule:** the reference file's own row spec (flat `var(--surface)` background, border-bottom only); `onecgiar-pr-client/CLAUDE.md` hard rule #7 ("no violet border and no tinted background" on content surfaces).
     - **Remediation:** remove the `[ngStyle]`; move the border-bottom separator from `.notification_content` to `.notification` itself, and retarget `:host(:last-child)` accordingly (keeps the `toc_review` block grouped with its own row, still separates from the next row). If a pending-state visual cue is still wanted, that's a `docs/DESIGN-DEVIATIONS.md` decision, not a leftover style.
  3. **`notification-item/CLAUDE.md` is 139 lines**, over the project's documented 120-line hard cap (`onecgiar-pr-client/docs/COMPONENT-DOCS.md` §4). The Reviewer specifically rejected the Implementer's framing ("keep traps or cut them") — the actual bulk is the `NOTIF-T-10` change-narrative section, which §4's own guidance says belongs in `execution.md`, not the file itself; the Traps list itself doesn't need cutting.
     - **Remediation:** shrink the `NOTIF-T-10` block to ~5 lines of current-state facts; move the narrative here (already being done, since this entry captures it); re-stamp `Verified:`.
  - **Verified clean (do not re-litigate):** `.ts` untouched; header chips genuinely gone from all 3 cases; row shape (avatar → text column → timestamp → actions) matches the reference in all 3 cases; the decided-state caption correctly moved and says "Rejected"/"Accepted" (not "Declined") distinct from the separate decision chip; button label change + its superseding-P2-3106 comment; token discipline (no new hex outside comments); `:host(:last-child)` structurally sound for the row separator it WAS targeting (just targeting the wrong element, per Issue 2); the two changed tests are honest, and every other test (accept/decline, ToC dialogs, business logic) confirmed unmodified.
  - **ADVISORY (non-gating):** a stale doc-comment overclaiming the new structure test proves "flex row" layout (it doesn't, and shouldn't claim to); ~40 lines of rework history in an SCSS comment block that should move to `execution.md`; missing structure tests for the status-3 caption and the no-caption case-1 branch (only status-2 covered); a pre-existing (not-this-task) gap where decided bilateral rows still render the non-bilateral sentence next to the bilateral icon; a stale `CLAUDE.md` "Where it is used" pointer (`received/`/`sent/` vs. the real `received-requests/`/`sent-requests/` folder names); this task had no `tasks.md` entry (now being added, see below); new SCSS (vs. Tailwind) is consistent with how this legacy-SCSS-heavy file has always been treated in this spec, not a new problem.

- **Attempt 2 — Implementer:** hid `pr-button`'s always-rendered `.filter` div via `::ng-deep ... .filter { display: none !important; }` on both notification buttons (correcting the wrong "no bug" comment); removed the `[ngStyle]` entirely from `.notification`, moved the row-separator `border-bottom` from `.notification_content` to `.notification` (the outer wrapper) and retargeted `:host(:last-child)` accordingly, so the `toc_review` sibling block stays grouped under its own row's one separator; shrunk `notification-item/CLAUDE.md`'s `NOTIF-T-10` section to current-state facts only, leaving the Traps list and every other section untouched. `npx jest` → 86/86 passed, no test changes needed. Lint clean, build succeeded.
- **Attempt 2 — Reviewer verdict: `PASS`.** Independently confirmed: `.filter` is a real always-rendered flex child (not `*ngIf`-gated) that only gets positioned under `.show_background`, so `display:none` genuinely removes it from flex layout (no competing rule, `!important` uncontested) — the root cause and fix are both correct. Confirmed the `[ngStyle]` is fully gone (not commented out), the separator now lives on `.notification` with `toc_review` correctly grouped as a sibling inside the same wrapper, producing exactly one border-bottom per notification. Confirmed `CLAUDE.md` is 114 lines (under the 120 cap), Traps list and all other sections read as intact (unable to diff word-for-word without shell access, but nothing looks cut or reworded). Confirmed no test could have depended on the removed `[ngStyle]` (grepped the spec file for style/border/background references — none found).
- **Requirements covered:** the user's full mockup-fidelity request (header chips removed, flat list, short button labels, centered buttons, no leftover pending-row chrome).
- **Decisions made:** accepted `display:none !important` on `.filter` over zeroing `.pr_button`'s `gap` (preserves the gap needed for the loading-icon state); accepted moving the separator to the outer `.notification` wrapper so a case-1 row's `toc_review` sibling block stays grouped under one border.
- **Issues encountered:** two real, distinct bugs caught across attempt 1 (missed `.filter` div, leftover pending-row violet chrome) plus a doc line-cap violation — all resolved in one rework round.
- **Final verification result:** 86/86 tests passing, lint clean, build succeeds. **Visual confirmation (flat list, centered buttons, no violet pending-row box) still needs the user's live check.**

---

### `NOTIF-T-11` (unplanned, user-directed task) — Move Search/Filter popover into Requests tab, align per reference row

- **Final status:** PASS (attempt 3 of 3)
- **Date:** 2026-09-25
- **Trigger:** the user's updated reference file shows Received/Sent, Search, Filter popover, and a "Notification settings" button all in ONE row inside the Requests tab — architecturally impossible with the Search/Filter popover living in the parent `ResultsNotificationsComponent` (above the router outlet) while Received/Sent segmented tabs live in the routed child `RequestsComponent`.
- **Skills assigned:** `angular-developer`, `spartan`. **Effort:** max (attempt 1, held at max — already at ceiling).
- **Attempt 1 — Implementer:** relocated the entire Search+Filter-popover cluster (state, facet derivation, the `NOTIF-T-9` `attachTo` overlay fix, chips, clear-all) plus Phase/Program logic (`phaseList`, `entityLabel`, `filteredInitiatives`, `getAllPhases`, `onPhaseChange`, `filterInitiativesByPhase`) from `ResultsNotificationsComponent` into `RequestsComponent`, choosing to DUPLICATE the Phase/Program state (one copy per component) rather than build a shared abstraction, since Requests/Updates are mutually-exclusive routed tabs. Added a "Notification settings" button reusing the existing gear-tab's navigation. Simplified the parent's `@if`/`@else` to a single Updates-only branch. 48 tests passing across 4 suites, lint clean, build succeeded.
- **Attempt 1 — Reviewer verdict: `FAIL`** (`full` review, effort max — 3 issues, several points verified clean). Findings carried verbatim:

  1. **The Phase/Program state duplication is a real bug, not just a style preference.** `RequestsComponent` and `ResultsNotificationsComponent` each now have their OWN `phaseList`/`filteredInitiatives`/`entityLabel`, updated independently by each component's own `onPhaseChange`/`filterInitiativesByPhase`. Since `phaseFilter` itself is shared (lives on the service), but `filteredInitiatives`/`entityLabel` are NOT, switching phase on Requests then clicking over to Updates leaves Updates' Program dropdown showing the OLD phase's initiatives under the OLD portfolio label — a real, reproducible `NOTIF-AC-5` violation ("Updates renders exactly as it does today — unchanged"), invisible to any test because every test exercises one component in isolation.
     - **Preferred remediation (the Reviewer's own words):** give `phaseList`/`filteredInitiatives`/`entityLabel` ONE owner — move them, with `getAllPhases`/`onPhaseChange`/`filterInitiativesByPhase`, into `ResultsNotificationsService`, which already owns `phaseFilter`/`initiativeIdFilter` for exactly this no-prop-drilling reason. Both components then bind the SAME state, and the duplicate fetch (flagged separately, ADVISORY below) goes away as a side effect. Add a test that changes phase via `RequestsComponent` and asserts Updates' Program options/label update accordingly.
  2. **A regression-guard test was lost in the relocation, with nothing equivalent replacing it.** `NOTIF-T-6` attempt 3 added a `compareDocumentPosition` DOM-order assertion (Phase → Program → Search → Clear filters) specifically to catch a silent element-reordering regression on the Updates row — that exact assertion is gone from the current `results-notifications.component.spec.ts`; the replacement tests only check presence, the same blind spot that let `NOTIF-T-6` attempt 2's bug through originally.
     - **Remediation:** restore the `compareDocumentPosition` assertion in the "Updates filter row — unaffected by the relocation" describe block.
  3. **A test comment in `requests.component.spec.ts` overclaims what's proven** — it says this suite's popover assertions are more real than the old parent spec's because it "declares/imports RequestsComponent's own template dependencies," but the suite actually still uses `NO_ERRORS_SCHEMA` with no real Spartan imports, so `<hlm-popover>` is opaque and the `attachTo` assertion only proves the binding expression resolves, not that the real overlay got an origin (same honest limitation every prior visual-property test in this spec has had to record, per the `.agents/reviewer.md` "presence-assertion is not behavioral proof" rule).
     - **Remediation:** correct the comment to state what's actually proven; note the real anchoring is confirmed by the pending live visual check (this whole `NOTIF-T-11` task, like every other visual fix in this spec, still needs the user's live confirmation).
  - **Verified clean:** the deep-link/query-param ordering claim (independently traced through Angular's actual router source — parent `ngOnInit` genuinely completes, including synchronous `setQueryParams()`, before the routed child is constructed; one pre-existing, unchanged gap noted — navigating Updates→Requests without a full parent remount doesn't re-hydrate from a fresh `?phase=` query param, not a regression); `updateQueryParams()` confirmed genuinely dead code (zero production call sites, independently grepped); no-`ngOnDestroy`-on-child reasoning confirmed correct; facet-derivation/`attachTo`-fix/Center-Bilateral-filtering logic confirmed preserved verbatim; module wiring confirmed correct (Spartan imports correctly moved, nothing left dangling); "Notification settings" button confirmed reusing the exact existing navigation, gear tab confirmed untouched; row/chips layout confirmed matching the reference exactly.
  - **ADVISORY (non-gating, but the Issue-1 fix removes most of it as a side effect):** every Requests entry now triggers MORE duplicate data fetches than the code comments claim (not just phase list — `get_section_information`/`get_sent_notifications`/`get_updates_notifications` all re-fire from both the parent's and the child's independent `onPhaseChange`, on top of `received`/`sent-requests`' own fetch, briefly blanking already-rendered lists) — in tension with the Performance NFR; re-selecting the default phase on every `RequestsComponent` re-creation is a minor behavior change worth documenting; a stale code comment still references the removed `@else` branch; the "Notification settings" button's href (`../settings` relative resolution) has no test; the button also reuses the existing `pi pi-cog` primeicon + inline `style` (matches its sibling gear tab's pre-existing convention, not a new violation).

- **Attempt 2 — Implementer:** moved `phaseList`/`filteredInitiatives`/`entityLabel` + `getAllPhases()`/`onPhaseChange()`/`filterInitiativesByPhase()` onto `ResultsNotificationsService` (single owner); both components now delegate/read through the service; added a `getAllPhases()` re-fetch guard (`if (this.phaseList.length > 0) return;`); restored a DOM-order test and corrected the misleading comment. `npx jest` → 85/85 passed (5 suites). Lint clean, build succeeded.
- **Attempt 2 — Reviewer verdict: `FAIL`** (final attempt before HALT — 4 issues, one root decision). Findings carried verbatim:

  1. **The `getAllPhases()` re-fetch guard causes a real regression: Updates shows stale data after the first visit, and deep-linking with `?phase=` stops re-deriving state.** `ResultsNotificationsService` is `providedIn: 'root'` and `phaseList` is never cleared, so the guard (`if (this.phaseList.length > 0) return;`) makes every subsequent entry into `results-notifications` a silent no-op — `onPhaseChange()` (which fetches Updates' own data) never re-runs. `UpdatesComponent` fetches nothing itself; it depends entirely on `onPhaseChange`'s fan-out. This is a NEW regression introduced by attempt 2, not present in attempt 1 or before `NOTIF-T-11`.
     - **Violated Rule:** `NOTIF-AC-5` ("Updates renders exactly as it does today — unchanged"); the Backwards-compatibility NFR.
     - **Remediation:** remove `this.resultsNotificationsSE.getAllPhases()` from `RequestsComponent.ngOnInit()` entirely — the parent (`ResultsNotificationsComponent`) always mounts first and stays mounted across the Requests↔Updates tab switch (already established in attempt 1's own review), so the child's call is fully redundant, not a safety net. Then remove the `phaseList.length` guard (or narrow it to an in-flight-request flag, not a "already-populated" flag) so each PARENT mount does the full fetch + default-phase logic + `onPhaseChange` again, matching pre-`NOTIF-T-11` behavior.
  2. **The "no double-fetch" claim is false on the real entry path, and the test proving it uses a synchronous mock that can't see the race.** Since both components' `getAllPhases()` were still being called in attempt 2 (before Issue 1's fix), and the real `GET_versioning` is async, both calls fire before `phaseList` is populated — 2× `GET_versioning` + 2× the `onPhaseChange` fan-out. The service-spec test mocks the HTTP call with a synchronous `of(...)`, which cannot reproduce this in-flight race.
     - **Remediation:** resolved as a side effect of Issue 1's fix (only one component calls `getAllPhases()` at all). If any guard remains, test it with a deferred observable (e.g. a `Subject`) to prove single-fetch under real async timing.
  3. **The restored DOM-order test is mostly a tautology.** It destructures `filterRow.children` in index order and asserts `children[i].compareDocumentPosition(children[i+1]) & FOLLOWING` — but sibling order in `.children` is document order by definition, so those assertions can never fail regardless of actual element identity/order. The only real check is that the 4th child has `.clear_filters_container` — catches a Search/Clear swap, but not a Phase↔Program or Program↔Search swap.
     - **Remediation:** select each node by IDENTITY (e.g. `app-pr-select[label="Phases"]`, the Program select via its bound `label` matching `entityLabel`, `input[hlmInput]`'s parent, `.clear_filters_container`), then assert `compareDocumentPosition` BETWEEN those identified nodes — so swapping any pair actually fails the test.
  4. **The cross-component state-sharing test is weaker than its own comment claims.** It drives the phase change by calling `onPhaseChange()` directly through the service reference on one component and reads the field back through the OTHER component's own service reference — proving the SAME shared object, not that either component's TEMPLATE actually binds to and re-renders from that state. It would have failed under attempt 1 only because the methods/fields didn't exist yet on the service (a TypeError), not because it caught a stale-binding bug.
     - **Remediation:** after driving the phase change, call `detectChanges()` on a component mounted at the Updates route and assert on the RENDERED Program `app-pr-select` element's bound properties (`label`/`options`) — proving the template genuinely reflects the shared state, not just that two references point at the same object.
  - **Verified clean:** single-owner state relocation itself is structurally correct (grepped, confirmed no leftover local copies anywhere); the corrected test comment (attempt 1's Issue 3) is honest; all previously-reviewed logic (facet derivation, `attachTo` wiring, Center/Bilateral handlers, chips, Notification-settings button, module wiring) untouched and still correct; `clearAllFilters()` staying duplicated-but-consistent across both components is an acceptable, low-risk choice (ADVISORY: extract to a shared service method to prevent future drift, non-gating).

- **Attempt 3 (final) — Implementer:** removed `getAllPhases()` from `RequestsComponent` entirely — the component no longer implements `OnInit` at all (an empty lifecycle method would fail `@angular-eslint/no-empty-lifecycle-method`, so the hook itself was removed, not left as a no-op, once its only job — the now-deleted delegation call — was gone). Removed `ResultsNotificationsService.getAllPhases()`'s re-fetch guard entirely, restoring exact pre-`NOTIF-T-11` behavior (full fetch + P2-3106 default-phase logic + `onPhaseChange()` fan-out, unconditionally, on every call). Fixed the DOM-order test to query elements by identity (`app-pr-select[label="Phases"]`, the Program select via its bound `entityLabel`, the Search wrapper, `.clear_filters_container`) instead of tautological `.children[i]` indexing. Fixed the cross-component test to assert on a RENDERED template binding (`detectChanges()` + reading the Program select's bound `.label`/`.options`) instead of same-object-reference identity. Added a genuinely rigorous service test using a deferred `Subject` (not a synchronous mock) proving `getAllPhases()` re-fetches on every call with zero memoization. `npx jest` → 85/85 passed. Lint clean (the empty-lifecycle-method fix was itself required to pass lint). Build succeeded.
- **Attempt 3 — Reviewer verdict: `PASS` (final attempt, no HALT needed).** Independently verified every fix: `RequestsComponent` has zero `OnInit`/`ngOnInit` trace; `getAllPhases()` has no early return; grepped the whole client tree and confirmed exactly one production call site (`ResultsNotificationsComponent.ngOnInit()`); the new service test would fail under ANY of three plausible memoization reintroductions (length guard, in-flight flag, `shareReplay`) — traced through each explicitly; the DOM-order test's four elements are genuinely un-nested siblings so the chained `compareDocumentPosition` assertions pin one true order (a Program↔Search swap would fail it); the cross-component test's rendered-binding assertion would fail if the template didn't actually read the shared service state. Confirmed the empty-lifecycle-method removal was necessary (project's ESLint config includes `@angular-eslint/no-empty-lifecycle-method`) and lost nothing (both prior reviews had already reduced that method to just the delegation call being removed).
  - **ADVISORY (non-gating, cheap fixes applied inline by the Leader, no rework attempt consumed):** one stale comment reference (`RequestsComponent`'s "ngOnInit()" — the method no longer exists) corrected in `results-notifications.service.ts`. Two remaining non-gating advisories left as-is (a `not.toHaveBeenCalled()` assertion in `requests.component.spec.ts` that passes trivially without `detectChanges()` — the adjacent `toBeUndefined()` check is the real assertion and already covers the intent; a missing one-line comment noting `ngOnInit()` runs twice in the cross-component test, harmless since `phaseFilter` stays null across the second run).
- **Requirements covered:** completes `NOTIF-T-11`'s full scope — the combined Requests-tab row, single-owner Phase/Program state, no cross-tab staleness, no duplicate fetches.
- **Decisions made:** accepted centralizing Phase/Program state on `ResultsNotificationsService` (attempt 2's core fix) as architecturally correct and kept it; accepted removing the redundant fetch call entirely (attempt 3) over patching around it with a guard, since the guard itself was the source of attempt 2's regression.
- **Issues encountered:** the most rework-intensive task in this entire spec — 3 full attempts, each catching a genuine, distinct defect (tab-scoping leak → had already been fixed in `NOTIF-T-6`; here: cross-tab state duplication → a broken re-fetch guard causing stale data → two under-powered tests). Every FAIL was a real bug, not a nitpick; the process worked as designed even under sustained pressure.
- **Final verification result:** 85/85 tests passing, lint clean, build succeeds. **Visual confirmation of the combined row layout and the popover's live behavior still needs the user's live check**, same as every visual/behavioral fix in this spec.

---

### Leader-inline fix — removed the redundant icon-only Settings tab

- **Date:** 2026-09-25. **Trigger:** user request, live testing — with `NOTIF-T-11`'s labeled "Notification settings" button now in the Requests tab's own row, the old icon-only gear tab (top `Requests | Updates | ⚙` row) was a redundant duplicate to the same destination.
- **Change:** removed the gear-icon `<a>` (and its now-empty wrapping `.tabs` div) from `results-notifications.component.html`. Confirmed `.options`'s `justify-content: space-between` degrades gracefully with one child (left-aligned, no layout gap). Confirmed no test in `results-notifications.component.spec.ts` asserted on the removed element.
- **Handled inline by the Leader** (per the Delegation Thresholds — a single-file, no-logic markup deletion, not routed through the full Implementer/Reviewer loop): ran `npx ng lint --quiet` → clean. Not committed.

---

### `NOTIF-T-12` (unplanned, user-reported task) — Filter popover overflows viewport on narrow windows

- **Final status:** PASS (attempt 1)
- **Date:** 2026-09-25
- **Discovered by:** the user, live, with screenshots showing the Filter popover panel clipped/overflowing past the right edge of a narrower browser window, forcing a horizontal scrollbar.
- **Skills assigned:** `angular-developer`, `spartan`. **Effort:** high.
- **Root cause (genuinely found, verified against the actual `@spartan-ng/brain` library source, not guessed):** `BrnPopover.getAttachPositions()` always uses the SAME `align` value for both `originX`/`overlayX` in its two generated position pairs (only the vertical origin flips for above/below fallback) — there is no horizontal (left/right) fallback built into the library at all. `BrnPopover.getPositionStrategy()` also calls `.withPush(false)` unconditionally, disabling CDK's own viewport-push safety net for every `hlm-popover` consumer app-wide. Binding a wider `attachPositions` input wouldn't help either — `BrnPopover`'s override of `getAttachPositions()` ignores that input entirely. Conclusion: there is no way to get automatic left/right repositioning from the library; it has to be computed by the caller.
- **Fix:** added `filterPopoverAlign = signal<'start'|'end'>('start')` + a `computeFilterPopoverAlign()` method that measures the Filter trigger button's `getBoundingClientRect()` against `window.innerWidth` (panel width 280px + 32px margin, matching `.notifications-filter-popover-content`'s own CSS) right before the popover opens, and binds `[align]` to it (was a static `align="start"` attribute). Recomputes only on the closed→open transition, not on every render.
- **Reviewer verdict: `PASS`.** Independently re-verified the root cause against the library source; confirmed `rect.left` (not `rect.right`) is the mathematically correct reference point for `align='start'`'s CDK semantics (traced through `ConnectedPosition` behavior by hand); confirmed the 280px/32px constants match the actual CSS; confirmed the new tests use realistic mocked geometry and the expected outcomes are independently verifiable as the visually-correct answer, not just "whatever the code returns"; confirmed recompute timing only fires on open, traced through `BrnOverlay`'s own `state()` effect to confirm the new `align` value is genuinely read before the position strategy builds. One evidence correction (non-gating): the Implementer's reported test/count breakdown was slightly off (6 new tests in this file, not 7; 17 pre-existing, not 30 — the 37-test total itself was accurate, just spread across `requests`/`received-requests`/`sent-requests` since the Jest pattern matches all three).
  - **ADVISORY (non-gating), one applied inline by the Leader:** a stale, inert `align="start"` attribute on `<hlm-popover-content>` (that class has no `align` input at all, so it did nothing and now visually contradicted the dynamic binding above it) — removed by the Leader post-review. Also recorded: a small, self-healing edge case where reopening the popover within its ~100ms close animation reuses the existing overlay with the previous alignment (fixes itself on the next full open, not worth engineering around); no test directly asserts the template wiring itself (`[align]` reaching the rendered element), covered only by the build succeeding; a `window.innerWidth` test mock isn't restored in `afterEach`, harmless today.
- **Requirements covered:** the user's live-reported viewport-overflow defect; no formal `NOTIF-R-*` ID (this bug post-dates the original requirements, same as `NOTIF-T-9`/`T-10`/`T-11`).
- **Decisions made:** accepted computing alignment at open-time as the correct, permanent fix (not a workaround) given the confirmed library limitation; accepted no resize-while-open re-measurement as a reasonable, common popover-library scope boundary.
- **Issues encountered:** none — PASSed on first attempt, with a genuinely diagnosed root cause.
- **Final verification result:** 37/37 tests passing, lint clean, build succeeds. **Visual confirmation at a genuinely narrow browser width still needs the user's live check** — jsdom cannot compute real layout/overflow.

---

### `NOTIF-T-13` (unplanned, user-reported task) — Filter popover overflows past the top of the viewport

- **Final status:** PASS (attempt 1)
- **Date:** 2026-09-25
- **Discovered by:** the user, live, with screenshots showing the popover panel rendering far above/detached from the Filter button, extending past the top of the viewport, even on a normal-width desktop window (distinct from `NOTIF-T-12`'s narrow-viewport horizontal overflow, which had already been fixed).
- **Skills assigned:** `spartan`, `tailwind-design-system`. **Effort:** medium.
- **Root cause:** the reference mockup specifies `width:280px;max-height:420px;overflow-y:auto` as ONE rule for the whole filter panel — only `width` was ever carried over (in `NOTIF-T-9`); `max-height`/`overflow-y` were simply missed. Without a height cap, the panel's rendered height grows unbounded with the Center/Bilateral-project checkbox lists. When it doesn't fit "below the trigger," CDK's connected-overlay position strategy flips it to "above the trigger" — and since `withPush(false)` is unconditional in this popover library (established in `NOTIF-T-9`/`NOTIF-T-12`), the panel renders overflowing past the top when there isn't enough room above either.
- **Fix:** added `max-height: 420px; overflow-y: auto;` to `.notifications-filter-popover-content`, matching the mockup exactly.
- **Reviewer verdict: `PASS`.** Confirmed the mockup's exact values; confirmed the nested two-level scroll pattern (outer 420px-capped panel containing two independently 160px-capped facet lists) mirrors the mockup's own intentional design (which nests an outer 420px cap around 180px-capped facet lists — the 160-vs-180 difference is a pre-existing, out-of-scope value from `NOTIF-T-6`); confirmed this credibly fixes the reported symptom for ordinary viewport heights; confirmed the Implementer's honestly-disclosed residual risk (an extreme case — a trigger with less than ~430px of room both above AND below it — can still overflow, since `withPush(false)` remains unconditional) is accurately characterized, not overclaimed or dismissed. One wording-only comment correction (a backwards phrasing of the `withPush(false)` explanation) fixed in place by the Leader post-review.
- **Requirements covered:** the user's live-reported panel-overflow defect.
- **Decisions made:** accepted capping the panel's own height (matching the mockup) as the correct fix, with the residual extreme-case risk explicitly recorded rather than papered over.
- **Issues encountered:** none — PASSed on first attempt.
- **Final verification result:** 37/37 tests passing, lint clean, build succeeds. **Visual confirmation still needs the user's live check.**

---

### `NOTIF-T-14` (unplanned, user-directed task) — Rebuild Filter dropdown as a plain positioned panel (replaces CDK popover, per `NOTIF-DD-7`)

- **Final status:** PASS (attempt 1)
- **Date:** 2026-09-25
- **Trigger:** after `NOTIF-T-9`/`T-12`/`T-13` each fixed a real, distinct bug in the Spartan `hlm-popover` (CDK overlay) mechanism, the user reported yet another positioning symptom (panel rendering far below the trigger) with no clear remaining root cause on the CDK side. Per `NOTIF-DD-7` (recorded before this task), decided to replace the mechanism entirely rather than continue chasing edge cases in a library directive (`BrnPopover`) that unconditionally disables CDK's own viewport-push safety net.
- **Skills assigned:** `angular-developer`, `tailwind-design-system` (deliberately not `spartan` — moving away from a Spartan primitive for this one control). **Effort:** max.
- **Implementer:** replaced `<hlm-popover>`/`<hlm-popover-content>`/`*hlmPopoverPortal` with a plain `position:relative` wrapper + `@if`-gated `position:absolute` panel, matching the user's own reference mockup's technique exactly. Added `@HostListener` document-click and Escape dismissal, replacing CDK's built-in handling. Kept the panel's inner content, facet-derivation logic, and the `NOTIF-T-12` alignment-decision logic (now driving a CSS class instead of a CDK `align` input) completely unchanged. Rewrote the test suite to test the new mechanism honestly (real DOM presence/absence, real dispatched click/keydown events) instead of CDK-specific property assertions. `npx jest` → 44/44 passed. Lint clean, build succeeded.
- **Reviewer verdict: `PASS`** (`full` review, effort max). Confirmed CDK/Spartan-popover genuinely fully removed (zero remaining bindings/imports); traced the classic "does the same click that opens the dropdown immediately re-close it" race by hand through the actual event-bubbling order and confirmed it does NOT occur (the trigger's own click sets state before the event bubbles to `document`, and the document handler correctly recognizes the trigger as "inside"); confirmed the Escape handler doesn't swallow the key globally and follows the same pattern already used elsewhere in this app (`shell-topbar`); confirmed the visual-parity tokens/z-index/shadow genuinely trace to existing project tokens and match a real, cited precedent pattern used elsewhere for the same kind of absolutely-positioned dropdown; confirmed the new tests are genuine (real dispatched events, hand-verifiable alignment math), not tautological; confirmed the old CDK-specific tests are genuinely gone, not left dangling; confirmed test coverage of user-facing behavior went UP, not down, despite the mechanism swap.
  - **One reliability finding fixed inline by the Leader (cheap, no rework attempt consumed):** the outside-click handler used `event.target`/`Node.contains()`, which can silently misfire if a click's target element gets removed from the DOM before the event finishes bubbling to `document` (a concrete, if not-currently-live, case: `hlm-checkbox`'s own checkmark sits behind an `@if`, so unchecking it removes that exact node) — switched to `event.composedPath()`, which captures the full original propagation path regardless of any synchronous DOM mutation mid-dispatch, making the fix correct independent of this app's current zoneless-change-detection timing. Re-verified 44/44 tests still pass, lint clean, after the change.
  - **ADVISORY (non-gating, left as-is):** if focus is inside the panel when Escape is pressed, focus drops to `<body>` instead of returning to the trigger (a minor a11y polish, not a functional break); an absolutely-positioned panel (unlike the old CDK overlay) can now be clipped by any ancestor with `overflow:hidden`/`auto` — checked the immediate ancestors and found none, but the app shell's outermost scroll container wasn't independently verified; flagged for `NOTIF-T-8`'s manual sweep (try opening the panel with a short list, near the bottom of the viewport). Some code comments still reference the old CDK mechanism (harmless staleness); the panel's tokens use `--pr-color-white`/`--pr-color-accents-2` (primitive) rather than the cited precedents' `--pr-surface-card`/`--pr-border` (semantic) — visually near-identical, not worth a rework round; the now-unnecessary `!important`s on the panel's sizing rules (no longer fighting a competing Tailwind class) are harmless leftovers.
- **Requirements covered:** the user's live-reported positioning defect; supersedes `NOTIF-T-9`'s `attachTo` fix and `NOTIF-T-12`'s `align` fix (both now moot — the CDK mechanism they patched no longer exists), while preserving `NOTIF-T-12`'s alignment-decision LOGIC (now driving CSS, not a CDK input) and `NOTIF-T-13`'s height-cap values verbatim.
- **Decisions made:** accepted a full mechanism replacement (documented as `NOTIF-DD-7`) over continuing to patch a shared library directive with known, unconditional limitations (`withPush(false)`, same-alignment-only fallback positions) that our own code cannot override.
- **Issues encountered:** none in review — PASSed on first attempt for the mechanism swap itself; one reliability hardening applied post-PASS by the Leader.
- **Final verification result:** 44/44 tests passing, lint clean, build succeeds. **Visual confirmation (opens directly below the trigger, correct shadow/border/radius, no clipping by any ancestor) still needs the user's live check** — jsdom cannot compute real layout.

---

### `NOTIF-T-15` (unplanned, user-requested task) — Search inputs on Center/Bilateral-project facets

- **Final status:** PASS (attempt 1)
- **Date:** 2026-09-25
- **Trigger:** the user asked for the per-facet search inputs the reference mockup shows above the Program/Center/Bilateral-project checkbox lists. Clarified with the user first: Program was confirmed OUT of scope (its `app-pr-select` already shows a built-in search automatically once there are 5+ options — `showSearchInput = computed(() => this.selectableOptionCount() >= 5)` in `custom-fields/pr-select`), so only Center and Bilateral-project (hand-built `hlm-checkbox` lists from `NOTIF-T-6`, no built-in search) needed anything added.
- **Skills assigned:** `angular-developer`, `tailwind-design-system`. **Effort:** medium.
- **Implementer:** added `centerSearchQuery`/`bilateralProjectSearchQuery` signals + `filteredCenterFacetOptions`/`filteredBilateralProjectFacetOptions` getters (case-insensitive substring match on `label`, layered on top of the unmodified base facet-derivation getters); added a search `<input>` above each of the two lists (mockup's exact placeholder text: "Search centers"/"Search projects"); split the `@empty` messaging to distinguish "no options match your search" ("Nothing matches that search.", mockup's exact copy) from "no options at all" (existing message, unchanged). `npx jest` → 53/53 passed (10 new). Lint clean, build succeeded.
- **Reviewer verdict: `PASS`.** Confirmed the filter is a pure additive layer over the unmodified base getters (the `clarisa_institution.id` path and bilateral-only scoping untouched); confirmed the Program facet has zero changes; confirmed the two empty-state messages are correctly distinguished by checking the RAW list's length inside the `@empty` block; confirmed checkbox check/uncheck logic still operates on id/code (not list position), so it's correct-by-inspection under search narrowing; confirmed exact mockup wording match; confirmed scope discipline (independently re-verified via `git diff --stat`, scoped to `requests.component.*` only).
  - **ADVISORY (non-gating):** the Implementer's own reasoning for choosing `[ngModel]`/`(ngModelChange)` over `[(ngModel)]` (claiming the shorthand can't bind to a signal) was factually outdated — Angular 17.2+ DOES support `[(ngModel)]="mySignal"` (no parens) directly; the explicit split is still a fine, harmless style choice, just recording the corrected reasoning here since it'll otherwise persist as a wrong "fact" in the codebase's institutional memory. Two test-coverage gaps flagged, not gating: no test for "No bilateral projects available yet." on a genuinely-empty raw list (only Center has this covered); the two "checkbox while narrowed" tests call the change methods directly rather than clicking the actually-rendered (search-narrowed) checkbox element — code is correct by inspection, but the test doesn't exercise the real DOM path.
- **Requirements covered:** the user's mockup-fidelity request for facet search.
- **Decisions made:** confirmed with the user upfront that Program stays single-select/out-of-scope (already had search for free), narrowing this to exactly Center + Bilateral-project.
- **Issues encountered:** none — PASSed on first attempt.
- **Final verification result:** 53/53 tests passing, lint clean, build succeeds.

---

### `NOTIF-T-16` (unplanned, user-reported defect) — Bilateral-project facet server payload fix

- **Final status:** PASS (attempt 2).
- **Date:** 2026-09-25
- **Trigger:** user screenshot showing the Bilateral-project filter listing RESULT codes/titles instead of true bilateral PROJECT codes ("B-A1080" style). Investigation subagent confirmed root cause: `share-result-request.service.ts`'s `getRequest()` never joined `results_by_projects` → `clarisa_projects`, so no correct field existed client-side — not a client mis-mapping.
- **Attempt 1 (effort: high, review mode: checklist):** added `obj_result_by_project` (→ `obj_clarisa_project: { id, shortName, fullName }`) to `getRequestSelectFields()`/`getRequestRelations()`; rewired `bilateralProjectFacetOptions`/`filter-notification-by-bilateral-project.pipe.ts` to key off `shortName`/`fullName` (many-to-many fan-out, one facet entry per linked project). Server 26/26 + client 75/75 passing, both lints clean.
  - **Reviewer verdict: `FAIL`.** The new join returns `results_by_projects` rows regardless of `is_active` — this table soft-deletes (`is_active: false`, never row-deleted) when a project is unlinked from a result, per `results_by_projects.service.ts:92-100`. Neither the select, `getRequest()`'s map, nor the client filters this, so a result can surface under a bilateral project it was already unlinked from. Reviewer confirmed via reading `results_by_projects.entity.ts`/`clarisa-projects.entity.ts` that the relation/select shape itself is correct (real relation name, real fields), the many-to-many fan-out and dedup are correct, and the pipe's "any linked project matches" logic is correct — the ONLY blocking issue is the missing `is_active` filter.
  - **ADVISORY (non-gating):** no facet-level test for a row linked to 2+ projects (pipe spec covers fan-out, getter/facet doesn't); the facet only reads bilateral rows but the pipe filters all rows (pre-existing asymmetry, shared with the Center pipe — flagged for a future decision, not this task); `shortName` used as filter key isn't guaranteed unique per schema (no unique constraint) — flagged as a future risk, not blocking since no requirement names an identifier.
- **Attempt 2 (effort bumped: max):** added `is_active: true` to the select; `getRequest()`'s existing per-result map now strips inactive links (`.filter(link => link.is_active)`), placed right after the pre-existing `source_name` normalization inside the same `if (result.obj_result && !Array.isArray(result.obj_result))` guard. Added a server spec (raw, unfiltered fixture with one active + one inactive link, asserting only the active one survives). Server 27/27 (was 26 — the +1 is the new spec), client re-confirmed 58/58 unaffected (no client change needed — neither the facet getter nor the pipe ever referenced `is_active`). Both lints clean.
  - **Reviewer verdict: `PASS`.** Confirmed all three callers (`getReceivedResultRequest`, `getReceivedResultRequestPopUp`, `getSentResultRequest`) funnel through this one private `getRequest()`, so pending/done/received/sent/pop-up all get the filter; confirmed the new spec is genuine (raw unfiltered fixture, not pre-filtered — only the real `.filter()` can make it pass); confirmed `?? []` safely no-ops for rows with no links (already exercised by 3 pre-existing specs); confirmed the surrounding `if` guard's actual purpose is a stale/mistyped `Result[]` array annotation on a real `@ManyToOne` (always a single object at runtime) and never excludes a genuine bilateral row; re-confirmed neither the client facet getter nor the pipe reference `is_active` (no client change needed, correctly).
- **Requirements covered:** the user's live-reported bilateral-project data-correctness defect, closed end-to-end (server relation + soft-delete correctness + client rewire).
- **Decisions made:** kept `bilateralProjectIdsFilter` typed `string[]` keyed on `shortName` rather than widening to numeric project id (attempt 1's own scope decision, unchallenged by the Reviewer — `short_name` uniqueness risk flagged as advisory, not blocking).
- **Issues encountered:** attempt 1 FAILed on a real, non-obvious correctness gap (soft-deleted project links); attempt 2 closed it cleanly on the first rework try.
- **Final verification result:** server 27/27, client 58/58, both lints clean. **Visual/data confirmation in a live environment (a result actually tagged/untagged from a bilateral project) still needs the user's own check** — this was verified against the entity/soft-delete logic and unit tests, not a live database.

### `NOTIF-T-17` (unplanned, user-reported defect) — Notifications flush against the page background (no boxed card)

- **Final status:** PASS (attempt 1)
- **Date:** 2026-09-25
- **Trigger:** user inspected DevTools and flagged that the live app wraps all Notifications content in `<div class="local_container section_container">`, while the mockup's content sits flush on the page background.
- **Implementer:** confirmed via the mockup (`showNotifications`'s root div: `padding:32px`, no card chrome) and via `results-notifications.component.html`'s own root (`.local_container` inline `padding: 20px 50px 50px 50px`, i.e. it already carries its own inset) that reusing the existing `--flush` modifier (zero outer margin/padding/background/shadow/radius) — the same one `results-list` already uses — is correct, rather than inventing a second padded modifier. Added `isResultsNotifications`/`isFlushRoute` computed signals in `results-outlet.component.ts`, rewired the two class bindings in `results-outlet.component.html`. `results-outlet.component.spec.ts` (smoke test) passing, lint clean.
- **Reviewer verdict: `PASS`.** Confirmed `results-list`'s rendering is provably unaffected (OR-logic preserves its prior truth value for every URL); confirmed the inner `results-notifications.component.html` padding is untouched by the outer shell's SCSS (component-scoped), so notifications keeps its own inset rather than going flush-to-viewport-edge like `results-list`. Flagged (not blocking): since this outlet only ever hosts these two children, `isResultsList() OR isResultsNotifications()` is a **tautology** — always `true` — making the `.section_container` binding permanently dead code. Manual browser QA still owed (visual-only property, no automated gate — same precedent as `NOTIF-TEST-7`).
  - **Cleanup applied inline by the Leader post-PASS (no rework attempt consumed):** per the Reviewer's own finding, simplified away the now-provably-constant `isFlushRoute`/`isResultsNotifications` signals — the wrapper div now carries a static `local_container local_container--flush` class pair instead of two always-resolving-the-same-way bindings, with a comment recording that a real per-route condition should return if this outlet ever gains a third child that should keep the card. Re-verified: `results-outlet.component.spec.ts` still passing.
- **Requirements covered:** the user's live-reported mockup-fidelity gap.
- **Decisions made:** reused `--flush` rather than adding a second "flush but padded" modifier, since Notifications' own content already supplies its own padding.
- **Issues encountered:** none blocking — one readability-only cleanup applied post-PASS.
- **Final verification result:** 1/1 spec passing (smoke test only — no class-binding assertion existed or was required for a checklist-mode review of this size), lint clean. **Visual confirmation still needs the user's live check.**
- **Known residual, explicitly out of scope:** `results-notifications.component.html`'s inline padding (`20px 50px 50px 50px`) doesn't match the mockup's uniform `32px` — flagged for a future follow-up task, not fixed here (editing that file was outside this task's declared shared-shell-only scope).

### `NOTIF-T-18` (unplanned, user-requested task) — Notifications page explainer text moved into an ⓘ tooltip

- **Final status:** PASS (attempt 2 — attempt 1 FAILed review).
- **Date:** 2026-09-25
- **Trigger:** user flagged (with a mockup screenshot showing "Notifications ⓘ" + a floating explainer card) that the permanently-visible `<p class="request_description">` text under the tabs should instead live inside a tooltip triggered by an info icon next to the page title, matching the mockup.
- **Leader-inline (single-file-cluster, trivial):** added an `<i class="material-icons-round notifications_title_info">` info icon next to the `<h1>`, wired to the existing `[prTooltip]` directive (already available via `CustomFieldsModule` → `PrTooltipDirectiveModule`, already used elsewhere in this same template) instead of building anything new. Removed both always-visible `<p class="request_description">` blocks (Requests-tab and Updates-tab copy) and replaced them with a `notificationsInfoTooltip` getter on the component that switches copy by `router.url`, mirroring the exact same per-tab branching the paragraphs used (empty string on Settings/anywhere else, matching the paragraphs' prior no-render behavior there). Added 3 new spec cases covering all three branches. `results-notifications.component.spec.ts`: 20/20 passing (17 pre-existing + 3 new). Lint clean.
- **Requirements covered:** the user's live-reported mockup-fidelity request; also bundled the user's separate "remove the Results Center breadcrumb from Notifications" ad-hoc request (`results-outlet.component.html`'s `<app-page-header>Results Center</app-page-header>`, previously rendered only for Notifications since results-list always suppressed it — removed outright).
- **Attempt 1 (Leader-inline, checklist review):** built the ⓘ trigger as a bespoke `<i class="material-icons-round">info</i>` with hand-rolled SCSS.
  - **Reviewer verdict: `FAIL`.** Three findings: (1) bypassed the codebase's own shared ⓘ-trigger pattern (`.sgi-dac-info` button + `<app-pr-info-icon>` SVG + `prTooltipStyleClass="sgi-dac-tooltip"`, used everywhere else per P2-3262/P2-3339 — a raw material-icons ligature reintroduces the exact defect `PrInfoIconComponent` was created to remove, i.e. the literal word "info" painting if the font fails to resolve); (2) the trigger rendered even on Settings, where the tooltip getter returns `''` — a dead, do-nothing, pointer-cursor icon, contradicting the task's own stated intent; (3) dead code left behind (orphaned `.request_description` SCSS rule, `PageHeaderComponent` still imported in `results-outlet.module.ts` after its only template usage was removed, a stale doc comment).
- **Attempt 2 (Leader-inline fix):** replaced the trigger with the shared `.sgi-dac-info`/`<app-pr-info-icon>`/`sgi-dac-tooltip` pattern (verbatim match to `result-detail.component.html`'s section-guidance ⓘ); wrapped it in `@if (notificationsInfoTooltip)` so it never renders on Settings; removed the dead SCSS rule, the unused `PageHeaderComponent` import, and reworded the stale comment. Added 2 new spec cases asserting the trigger is absent on Settings and present on Requests. 23/23 tests passing, lint clean.
  - **Reviewer verdict: `PASS`.** Confirmed all 3 issues genuinely closed by re-reading the current files (not the diff description); confirmed `app-pr-info-icon`/`.sgi-dac-info`/`.sgi-dac-tooltip` are all in scope via the already-imported `CustomFieldsModule`; confirmed the new spec's `.sgi-dac-info` selector is unambiguous and the hide/show assertions can't pass by accident (the positive case proves the selector resolves at all); confirmed no other template references `<app-page-header>` in this outlet's tree; confirmed the only remaining `request_description` mention is historical prose in a doc comment, not dead code.
- **Decisions made:** kept exactly the same source copy verbatim (no wording changes) for both tabs; one shared tooltip rather than per-tab icons, since the mockup shows a single icon next to one title; bundled the breadcrumb removal into the same review pass since both are small, same-day, same-file-tree user requests.
- **Issues encountered:** attempt 1 FAILed on a real, non-cosmetic finding (the shared-component bypass carries a known production defect risk) — attempt 2 closed all 3 findings cleanly.
- **Final verification result:** 23/23 tests passing, lint clean. **Visual confirmation of tooltip position/hover behavior and the breadcrumb's absence still needs the user's live check.**

---

## 4. Leader decisions (task-boundary clarifications, not spec pivots)

### 2026-09-25 — `NOTIF-T-5`/`NOTIF-T-6` filter-chaining boundary

`NOTIF-T-5`'s own description says "existing filter pipes (`filterNotificationByInitiative`, `appFilterNotificationBySearch`, and the new center/bilateral-project pipes from `NOTIF-T-3`) still run before grouping" — but `NOTIF-T-3`'s pipes filter on `centerIdsFilter`/`bilateralProjectIdsFilter` state that doesn't exist until `NOTIF-T-6` creates it on `results-notifications.component.ts`, and the dependency graph runs `NOTIF-T-5` before `NOTIF-T-6` is required to land (both only depend on `NOTIF-T-1`/`NOTIF-T-3`, not on each other). Chaining pipes that reference not-yet-existing state would either block `NOTIF-T-5` on `NOTIF-T-6` (undeclared dependency) or force `NOTIF-T-5`'s Implementer to invent placeholder state now to be replaced later.

**Decision:** `NOTIF-T-5` wires only the recency grouping (`NOTIF-T-2`'s pipe) around the *currently* available filters (`filterNotificationByInitiative`, `appFilterNotificationBySearch`); `NOTIF-T-6` — which already owns creating `centerIdsFilter`/`bilateralProjectIdsFilter` and must expose them through `ResultsNotificationsService` for `received`/`sent`-requests to consume (per its own "Consumers" note) — closes the loop by adding those two pipes into `received-requests.component.html`/`sent-requests.component.html`'s existing pipe chains. `tasks.md` `NOTIF-T-6`'s "Files (expected)" amended to include those two files. Not a spec pivot (the requirements/design intent doesn't change, only which task performs which already-planned line of the same pipe chain) — no `requirements.md`/`design.md` edit needed.

---

## Summary

All 8 planned tasks' CODE has PASSed review, plus one unplanned defect-fix task (`NOTIF-T-9`, three separate rounds) the user's own live testing surfaced after everything else was already green:
- Filter popover rendering centered/global instead of anchored to its trigger — a real, traced library-internals bug (the button never registered itself as the CDK overlay's anchor), not a guess, independently verified against the compiled `@spartan-ng/brain` source. Fixed via a `viewChild` + `[attachTo]` binding. **Same latent bug found in a sibling component (`program-overview`'s Scope popover) — out of scope, flagged for a follow-up ticket.**
- Filter popover panel width (280px per the mockup, was 340px).
- Row avatars now show the requester's initials in a circle (individual rows) vs. an icon in a rounded square (bilateral/entity rows), replacing a generic person icon for both — matching the mockup's own distinct shapes.
- Accept/Decline buttons restyled per an explicit, user-confirmed mockup reference (bordered/white/accent-text Accept, plain muted-gray Decline) — a legitimate scope amendment recorded as `NOTIF-DD-6`, since the original spec had explicitly kept these buttons "unchanged." Took 2 rounds: attempt 1 had a genuine CSS-specificity bug (Angular's compiled encapsulation attribute made the override lose the cascade silently) that attempt 2 fixed with `!important`, matching an existing codebase precedent for the same problem.

**What's left is not code — it's the manual QA sweep that is `NOTIF-T-8`'s own Definition of Done** (desktop + tablet, Received/Sent, filters incl. the popover anchoring/width fix, accept/decline incl. bilateral ToC dialogs and their new colors, decision chips, avatar shapes, bell, Updates-tab regression check). Per the task's own Disqualifier, this has no automated gate and a skipped manual check counts as incomplete, not passed — so the spec is not `[x]`-closeable yet. Nothing has been committed — all changes sit unstaged, per the user's standing no-auto-commit rule.
