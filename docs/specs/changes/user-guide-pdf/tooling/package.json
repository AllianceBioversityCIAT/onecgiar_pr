{
  "name": "user-guide-pdf-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone, dev-only Playwright pipeline that captures annotated screenshots of the reporting tool and assembles them into the end-user PDF guide. Not wired into onecgiar-pr-client or onecgiar-pr-server.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "capture": "ts-node src/capture.ts",
    "assemble": "echo \"assemble.ts not implemented yet (see UG-T-12)\" && exit 1",
    "verify-structure": "echo \"verify-structure.ts not implemented yet (see UG-T-13)\" && exit 1",
    "pdf": "echo \"pdf.ts not implemented yet (see UG-T-14)\" && exit 1",
    "build-guide": "echo \"full pipeline not wired yet (see UG-T-7, UG-T-12..14)\" && exit 1"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^20.16.0",
    "dotenv": "^17.4.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
