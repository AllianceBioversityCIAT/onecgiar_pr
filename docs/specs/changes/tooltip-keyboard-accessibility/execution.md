# Tooltip Unification & Keyboard Accessibility — `execution.md`

## 1. Document Control

- **Spec:** `docs/specs/changes/tooltip-keyboard-accessibility/`
- **Approval mode:** gated (no `pre-approved` marker found in requirements.md/design.md Document Control — treat each PASS/HALT gate as a stop-and-report point)
- **Leader:** Claude Code session, 2026-09-04

---

## 2. Task Execution History

### `TIP-T-1` — Rewrite `PrTooltipDirective` for unified click-pin, keyboard, ARIA, and viewport behavior

**Status:** IN PROGRESS (attempt 1 FAIL, attempt 2 pending)

#### Attempt 1 — 2026-09-04

- **Skills assigned:** `angular-developer`
- **Effort:** `high`
- **Files changed:** `onecgiar-pr-client/src/app/shared/directives/pr-tooltip.directive.ts` (only file, in scope)
- **Implementer verification:**
  - `npx ng lint --quiet` → clean.
  - `npm run build` → fails only on 6 pre-existing `[prTooltipPinnable]="true"` bindings in the 5 template files reserved for `TIP-T-2` (not touched this task) — no error originates from the directive file.
  - `npx tsc --noEmit` → clean except `pr-tooltip.directive.spec.ts` (reserved for `TIP-T-3`).
  - **Not Done:** manual smoke check and idle-listener-leak check in a live `npm start` session — no dev server available in the Implementer's environment. Idle-listener claim was code-review-only (teardown array).
- **Implementer self-flagged judgment calls:** (1) `dynamic-panel-menu`'s host is a `<div [routerLink]>` not an `<a>` as design.md states — gets upgraded, Enter/Space now opens tooltip but does not navigate. (2) added a `type="button"|"submit"|"reset"` heuristic beyond the literal tasks.md list to avoid double-firing on `result-framework-reporting-recent-item`. (3) added defensive `hostOwnsAriaExpanded`/`hostOwnsAriaControls` guards for `reporting-entry-hub` even though no current template collides.
- **Reviewer verdict:** `STATUS: FAIL` (4 issues)
  1. **`dynamic-panel-menu` regression** — host is not skipped by the upgrade check despite `tasks.md` TIP-T-1 item 2 and `design.md` DD-3 explicitly naming it. Becomes a `role="button"` tab stop where `Enter` opens the tooltip but does NOT navigate (RouterLink only fires on real `click`, not synthesized). Violates `tasks.md` TIP-T-1 item 2 and `TIP-DD-3`. Remediation: detect via `inject(RouterLink, {optional:true, self:true})` and treat non-null as already-interactive, OR dispatch a real click on activation for router-link hosts (explicit recorded decision either way) — carry a detection test into `TIP-T-3`.
  2. **Dead focus-stops on disabled/empty tooltips** — `ngOnInit` upgrade is one-shot and does not account for `[prTooltipDisabled]` or empty/conditional `[prTooltip]` text, both of which flip at runtime. Confirmed live sites: `rd-partners.component.html:20-24` (+ twins `rd-contributors-and-partners.component.html:148`, `ipsr-contributors.component.html:64`), and 7 more sites binding `''` in a ternary branch (`dynamic-panel-menu.component.html:14`, `results-list.component.html:235`, `results-notifications.component.html:57`, `aow-hlo-table.component.html:129`, `reporting-aow-table.component.html:879`, `program-overview.component.html:835`, `user-roles-info-modal.component.html:2`). Violates `requirements.md` §7 NFR-Accessibility (name/role/value) and `design.md` §2.3 (upgrade scoped to "the trigger" — no-content hosts are not triggers). Remediation: gate upgrade on `!prTooltipDisabled && !!text`, extract `syncHostAffordance()`, call from `ngOnInit` + `ngOnChanges`, track directive-added attributes with a `didUpgradeHost` flag so template-declared attributes are never stripped.
  3. **Focus-steal on outside-click** — `hide()` unconditionally restores focus to `previouslyFocusedEl` on every close path including outside-click, which steals focus from whatever the user just clicked (bubble-phase document click fires after the browser already focused the new target). Concrete regression on `pr-field-header`/`field-card` (adjacent to nearly every Result Detail input): click ⓘ → click into adjacent input → `hide()` fires → focus yanked back to ⓘ. Violates `requirements.md` §8 `TIP-AC-2` (outside click's only specified consequence is "closes") + WCAG 2.4.3. Remediation: gate `restoreTo.focus()` on `focusIsInWidget` (tooltip or host still contains `document.activeElement`) at close time.
  4. **Raw HTML announced to screen readers** — `LiveAnnouncer.announce(this.text)` announces the raw HTML string (tooltip content is set via `innerHTML`), so anchors (CGIAR 2030 Strategy, Impact Area scoring doc links) are spoken as literal markup. Violates `requirements.md` §6 `TIP-R-20` ("announce the tooltip's content"). SHOULD-level — `tasks.md` item 8's deferral clause is an alternative. Remediation: announce `tooltipEl.textContent?.trim()` instead, or remove the call and record the deferral explicitly.
  - **Judgment-call adjudication (Leader, ratifying Reviewer's read):** (1) in scope for `TIP-T-1`, not deferrable to `TIP-T-6` — `tasks.md` names the skip explicitly. (2) `type="button"` heuristic accepted as spec-serving, low false-positive risk — pin it in `TIP-T-3`'s tests. (3) `hostOwnsAriaControls`/`hostOwnsAriaExpanded` guards are inert today and don't break `TIP-DD-2` (`aria-describedby` set unconditionally) — no fix needed for those two, but see ADVISORY below for `aria-describedby` itself.
  - **ADVISORY (4R lens, non-gating):**
    - RELIABILITY: no `hostOwnsAriaDescribedby` guard — a host with its own template `aria-describedby` (help text/error association) loses it permanently after first open/close. Mirror the existing guard pattern.
    - RESILIENCE: `FocusTrapFactory.create()` runs even for plain-text tooltips with no tabbable content; CDK still inserts two tabbable anchor elements around the body-appended tooltip. Consider gating trap creation on `tooltipEl.querySelector('a,button,[tabindex]')`.
    - RISK: `onWindowScrollOrResize` runs `position()` synchronously per scroll event (2 rect reads + 2 style writes) — only while pinned, so the NFR idle-listener target is met, but `requestAnimationFrame` coalescing would remove the layout-thrash pattern at ~4 lines.
    - READABILITY: `tasks.md` item 3 places `aria-controls`/`aria-describedby` wiring in `show()`; diff places it in `pin()`. Practical delta is hover-only tooltips carry no association — near-moot since pin-on-open — but note the deviation.
    - RISK (carry to `TIP-T-6`, not this task): blanket upgrade puts `role="button"`+`tabindex="0"` on a `<span>` nested inside a native `<button>` at `reporting-entry-hub.component.html:331` — interactive-content-inside-button is invalid HTML/nested focus stop. Faithful consequence of `design.md` §2.3's rule, not an implementation defect — flag for the 39-file sweep to decide whether truncation-tooltip hosts inside buttons should be upgraded at all.
  - **Verification-evidence note:** `npm run build` failing only on the 5 reserved `[prTooltipPinnable]` template bindings is structurally expected given the task split (`TIP-T-1` removes the input; `TIP-T-2` removes the bindings) — branch is not mergeable/buildable in isolation until `TIP-T-2` lands. Idle-listener DoD item closed by code review (teardown array + `ngOnDestroy` call `hide()` on every path). Manual smoke-check DoD item remains open and is exactly where Issue 3 lives — validates the "test the assumption" principle in hindsight.

#### Attempt 2 — 2026-09-04

- **Skills assigned:** `angular-developer`
- **Effort:** `xhigh` (bumped one level after attempt 1 FAIL, per rework-loop rule)
- **Files changed:** `onecgiar-pr-client/src/app/shared/directives/pr-tooltip.directive.ts` (same single file, revised in place — not a rewrite from scratch)
- **Fixes applied (all 4 Reviewer issues + both optional advisories):**
  1. `dynamic-panel-menu` regression → `inject(RouterLink, {optional:true, self:true})` folded into `hostIsNativelyInteractive`; a `[routerLink]` host is now permanently skipped by the upgrade (matches Reviewer's suggested remediation exactly).
  2. Dead focus-stops → added `OnChanges` + `syncHostAffordance()` (called from `ngOnInit` and on `text`/`prTooltipDisabled` changes), gated on `!hostIsNativelyInteractive && !prTooltipDisabled && !!text`, tracked via `didUpgradeHost` so only directive-added attributes are ever touched. Closes a pinned tooltip via `hide()` before stripping attributes if it becomes ineligible at runtime. Keydown handlers early-return before `preventDefault()` when disabled/empty.
  3. Focus-steal on outside-click → `hide()` snapshots `document.activeElement` and computes `focusIsInWidget` before any teardown; restores focus only when true.
  4. Raw HTML to `LiveAnnouncer` → now announces `tooltipEl.textContent?.trim()` instead of raw `this.text`.
  - Advisory (both implemented): `hostOwnsAriaDescribedby` guard added; `FocusTrapFactory.create()` gated on the tooltip containing tabbable content.
- **Implementer verification:** `npx ng lint --quiet` clean; `npx tsc --noEmit` clean for this file (only the known out-of-scope `prTooltipPinnable` errors in the spec file and 6 template bindings remain — reserved for `TIP-T-2`/`TIP-T-3`, one extra site found beyond design.md's named 5: `program-overview.component.html`, noted for `TIP-T-2`). Manual smoke check still not performed (same environment constraint).
- **Reviewer verdict:** `STATUS: PASS`. All 4 issues independently re-verified against the actual template/code (not taken on the Implementer's claim) — `dynamic-panel-menu`'s real markup confirmed as `<div [routerLink]>` at line 9-15; the hide-before-strip ordering in `syncHostAffordance`'s de-upgrade branch confirmed correct (reversing it would reintroduce the stale-attribute bug); focus-steal fix confirmed correct given bubble-phase document click timing; LiveAnnouncer fix confirmed (`innerHTML` set before `textContent` read). Both advisory fixes confirmed present and wired correctly (mirrored guard pattern; querySelector gate).
  - **Evidence-gap note (Reviewer, non-blocking):** fixes 1, 3, and 4 land in defect classes `requirements.md` §7.1 already marks as not jsdom-observable (focus movement, screen-reader announcement) — passing the code is not certifying the behavior in a browser. `TIP-T-3` must add a detection test for the RouterLink skip; `TIP-T-4` (Cypress) must carry focus-restore-on-outside-click; `TIP-T-6`'s manual pass must carry the LiveAnnouncer text-content check. Do not let those later tasks close on presence assertions alone for these three.
  - **ADVISORY (4R lens, non-gating):**
    - RELIABILITY: "close when ineligible" only applies inside the `didUpgradeHost` branch — a native `<button prTooltip>` whose `prTooltipDisabled` flips true while pinned stays open indefinitely. Suggested one-line fix: hoist the `if (pinned && !shouldUpgrade) hide()` check above both branches. Not applied this task — carry to `TIP-T-6` sweep or a future pass if it surfaces.
    - RELIABILITY: `hide()` invoked from `syncHostAffordance` (programmatic, not user gesture) with the host focused refocuses the host right before its `tabindex` is stripped — browsers tolerate this today (no visible break) but it's fragile. Suggested fix: skip focus-restore on the programmatic close path.
    - RELIABILITY: `hide()` runs full ARIA teardown even when nothing was ever pinned (e.g. plain `mouseleave`) — every `[prTooltip]` host, including deliberately non-upgraded ones, acquires `aria-expanded="false"` on first mouse-out, an `aria-allowed-attr` violation on a role-less host. Suggested fix: gate the three ARIA lines on `wasPinned`.
    - RESILIENCE: focus-trap gate `querySelector('a,button,[tabindex]')` misses `input`/`select`/`textarea`/`[contenteditable]` and matches `tabindex="-1"`. No current tooltip body has a form control, so not exercised today — latent gap if one is added later.
    - READABILITY: `hostIsNativelyInteractive` now conflates 4 distinct skip reasons (native tag, RouterLink, author role/tabindex, `type=` heuristic) — well-documented in the docblock, but `TIP-T-3` should assert each independently so a future edit can't silently widen the upgrade.
  - None of the above ADVISORY items block PASS or consume a rework attempt, per the Advisory-Never-Gates rule. Not applied in this task; carried forward as recorded findings for `TIP-T-3`/`TIP-T-4`/`TIP-T-6` to pick up, or a future spec if the team wants them addressed sooner.

**Requirements covered:** `TIP-R-1` through `TIP-R-10` (mechanism), `TIP-R-20` (best-effort, implemented not deferred), `TIP-AC-1` through `TIP-AC-8` (implementation half).

**Decisions made:** RouterLink detection via `inject(RouterLink, {optional:true,self:true})` rather than synthesizing a click for router-link hosts (matches spec's literal "skip" instruction). LiveAnnouncer kept (not deferred) since the textContent fix fully resolved the raw-HTML issue without needing the SHOULD's deferral escape hatch.

**Issues encountered:** attempt 1 FAILed on 4 issues (see above); attempt 2 fixed all 4 plus 2 advisories, confirmed by independent Reviewer re-verification against the actual templates and code, not the Implementer's claims alone.

**Final verification result:** `npx ng lint --quiet` clean; `npx tsc --noEmit` clean for the directive file. Full `npm run build` remains red until `TIP-T-2` removes the 6 template `[prTooltipPinnable]` bindings — this is the expected, structural state of the task split, not a defect of this task.

**Final status: PASS (attempt 2/3).**

---

**Next eligible tasks (all depend only on `TIP-T-1`, now `[x]`):** `TIP-T-2` (5-file — now 6-file, see note above — cleanup), `TIP-T-3` (Jest retarget), `TIP-T-4` (Cypress focus/clamp/reposition), `TIP-T-5` (Cypress compound-click sites). All four are parallel-friendly (no shared files) per the dependency graph in `tasks.md` §4.

---

### `TIP-T-2` — Remove the now-redundant `[prTooltipPinnable]="true"` attribute from its call sites

**Status:** DONE

#### Attempt 1 — 2026-09-04

- **Skills assigned:** `angular-developer`
- **Effort:** `low`
- **Files changed** (attribute removal only, one line each):
  1. `onecgiar-pr-client/src/app/custom-fields/pr-field-header/pr-field-header.component.html`
  2. `onecgiar-pr-client/src/app/custom-fields/field-card/field-card.component.html`
  3. `onecgiar-pr-client/src/app/pages/bilateral/components/section-general-info/section-general-info.component.html`
  4. `onecgiar-pr-client/src/app/pages/results/pages/result-detail/pages/rd-general-information/rd-general-information.component.html`
  5. `onecgiar-pr-client/src/app/pages/results/pages/result-detail/result-detail.component.html`
  6. `onecgiar-pr-client/src/app/pages/result-framework-reporting/pages/dashboard-lab/components/program-overview/program-overview.component.html` (6th site, found by `TIP-T-1`'s Implementer during their own grep, not in `tasks.md`'s original 5 — same cleanup, same reasoning)
- **Diff summary:** each site had exactly `[prTooltipPinnable]="true"` removed from the `[prTooltip]`-bearing element (no other attribute, structure, or logic touched).
- **Verification:**
  - `grep -rn "prTooltipPinnable" onecgiar-pr-client/src --include=*.html` → one residual hit: `program-overview.component.html:777`, inside an HTML **comment** (a docblock explaining the P2-3201 rationale, not a live binding) — left untouched as out of the attribute-removal scope; flagged for a future doc pass rather than edited silently.
  - `npm run build` (production) → **succeeds** ("Application bundle generation complete"). Only pre-existing, unrelated warnings remain (bundle-budget, unused `DecimalPipe`/`@let`, CommonJS deps for `web-llm`/`pdfjs-dist`). This was the last thing blocking the build after `TIP-T-1`.
  - `npx ng lint --quiet` → "All files pass linting."
  - Grepped for other `prTooltipPinnable` references: three `.spec.ts` files access it as a real TypeScript property on the directive instance (`directive.prTooltipPinnable`, `fresh.prTooltipPinnable`) — genuine compile-breaking references, not incidental strings: `pr-tooltip.directive.spec.ts` (get/set), `pr-field-header.component.spec.ts`, `field-card.component.spec.ts` (both `expect(...injector.get(PrTooltipDirective).prTooltipPinnable).toBe(true)`). Confirmed but **not fixed** — reserved for `TIP-T-3`.

**Requirements covered:** completes the `TIP-T-1`/`TIP-T-2` split — directive input removed (`TIP-T-1`) and all template call sites cleaned up (`TIP-T-2`); pinning is now unconditional, zero behavior change.

**Decisions made:** left the stale `prTooltipPinnable` mention in `program-overview.component.html`'s comment (line 777) untouched — it's prose describing rationale, not the attribute itself, and the task scope was explicitly "just remove the attribute... do not touch anything else."

**Issues encountered:** none — all 6 sites matched the expected pattern on first read; no build/lint fallout beyond the already-known `TIP-T-3` spec-file items.

**Implementer's self-reported verification result:** `npm run build` green, `npx ng lint --quiet` clean. *(Note: the Implementer's report characterized this as "Final status: PASS" — corrected here. Per `/akili-execute` Step 2.3, PASS is a Reviewer verdict, not an Implementer self-assessment; the independent audit had not run yet when that line was written.)*

**Reviewer verdict:** `STATUS: PASS`. Independently re-verified (not trusting the Implementer's grep claim): all 6 sites confirmed to have lost exactly `[prTooltipPinnable]="true"` and nothing else (`[prTooltip]`/`prTooltipPosition`/`prTooltipStyleClass`/`aria-label` all intact); the one residual `prTooltipPinnable` hit in `program-overview.component.html:777` confirmed to sit inside an HTML comment, not a live binding; the 6th file (beyond `tasks.md`'s hand-enumerated 5) confirmed as legitimate in-scope cleanup, not scope creep — `TIP-AC-9`/`TIP-T-6` already mandate the full 39-file sweep this residue belongs to.

**Reviewer must-carry findings (not gating this task, corrected into `tasks.md` by the Leader):**
1. **Client Jest is currently red** — the Implementer's DoD check silently narrowed `tasks.md`'s original unqualified `grep -rn "prTooltipPinnable" onecgiar-pr-client/src` to `--include=*.html`, which hid 4 live TypeScript compile errors: `field-card.component.spec.ts:79`, `pr-field-header.component.spec.ts:69`, and `pr-tooltip.directive.spec.ts:60,209` all reference the `@Input() prTooltipPinnable` that `TIP-T-1` removed. `npm run build`/`ng lint` don't see `*.spec.ts`, so neither green check could have caught this. Per `design.md` TIP-DD-5 Consequences, this is `TIP-T-3`'s ownership ("any test referencing `prTooltipPinnable` must be updated…, not after") — `tasks.md`'s `TIP-T-3` **Files (expected)** list named only the directive spec, an incomplete list now corrected to include both `custom-fields` specs, and its DoD now requires running them, not just the directive spec pattern.
2. **`tasks.md` TIP-T-2's DoD grep corrected** from unqualified to `--include=*.html`, matching DD-5's actual ownership split — the original wording was unsatisfiable until `TIP-T-3` lands.
3. (Leader's call, deferred as noise-level for a one-line attribute removal) `program-overview.component.html`'s comment at line 777 now describes an attribute that no longer exists, and that folder's own `CLAUDE.md` `Verified:` stamp convention technically applies — not addressed in this task, left for a future doc pass or `TIP-T-6`'s sweep.

**`tasks.md` corrected in place** (Leader, same-day): `TIP-T-2` DoD grep qualified to `--include=*.html`; `TIP-T-3` **Files (expected)** and DoD updated to include `pr-field-header.component.spec.ts` and `field-card.component.spec.ts`.

---

### `TIP-T-3` — Retarget and extend `pr-tooltip.directive.spec.ts` (Jest)

**Status:** IN PROGRESS (attempt 1 FAIL, attempt 2 pending)

#### Attempt 1 — 2026-09-05

- **Skills assigned:** `angular-developer`, `tdd`
- **Effort:** `high`
- **Files changed:**
  - `onecgiar-pr-client/src/app/shared/directives/pr-tooltip.directive.spec.ts` (full rewrite: `TestBed.createComponent`-mounted host components replace the dead bare-constructor harness, since the directive now calls `inject()` in field initializers)
  - `onecgiar-pr-client/src/app/custom-fields/pr-field-header/pr-field-header.component.spec.ts`
  - `onecgiar-pr-client/src/app/custom-fields/field-card/field-card.component.spec.ts`
- **Implementer verification:** `npx jest --testPathPattern="pr-tooltip.directive.spec"` → 44 passed. `npx jest --testPathPattern="pr-field-header.component.spec|field-card.component.spec"` → 25 passed. `npx ng lint --quiet` clean. Scoped coverage on the directive: 95.81%/91.25%/96%/97.5%. No directive bug found, no changes to `pr-tooltip.directive.ts`.
- **Reviewer verdict:** `STATUS: FAIL` (2 issues, both "test that cannot fail" defects — the gating principle here is falsifiability, not pass/fail count)
  1. **RouterLink-detection test is vacuous.** Uses an `<a [routerLink]>` host — `tag === 'A'` already short-circuits `hostIsNativelyInteractive` before `!!this.routerLink` is ever evaluated, so deleting the `inject(RouterLink)` term entirely leaves all 3 tests in that block passing. The actual regression site (`TIP-T-1` attempt-1 issue 1) was `dynamic-panel-menu`'s `<div [routerLink]>`, not an anchor. Violates the explicit carry-forward instruction in this file's own `TIP-T-1` attempt-2 entry ("`TIP-T-3` must add a detection test for the RouterLink skip … do not let those later tasks close on presence assertions alone"). Remediation: rebuild the fixture as `<div [routerLink]="['/foo']" [prTooltip]="...">` — but note Angular's `RouterLink` itself sets `tabindex="0"` on non-anchor hosts at input-set time (before `ngOnInit`), so a `tabindex`-absence assertion won't isolate the `routerLink` term either; assert what's actually falsifiable for that host shape (no `role` added, keydown Enter doesn't open the tooltip, real click still pins) and record in a comment that the `routerLink` term itself isn't cleanly isolable in jsdom.
  2. **"Escape restores focus" test never moves focus away from the trigger first**, so `expect(document.activeElement).toBe(btn)` holds even with the entire `focusIsInWidget`-gated restore block deleted from `hide()`. Violates `requirements.md` `TIP-AC-3` (restore is part of the AC, not incidental) and the reviewer-persona rule that a presence-style assertion is not behavioral proof. Remediation: give the tooltip a focusable link, focus it explicitly (not via CDK's `focusFirstTabbableElement` — jsdom doesn't populate `offsetParent`/`getClientRects` so the trap may silently no-op), dispatch Escape, assert focus returned to the trigger; or if kept as-is, rename to what it actually proves and extend the recorded gap to name focus-restore explicitly as `TIP-T-4`'s.
  - **ADVISORY (non-gating):** coverage note mischaracterized — lines 260-262 are the `case 'bottom':` branch (regression-lost when the old 4-position loop was dropped, not "left/right width calc"); line 338 (`onWindowScrollOrResize` body) is genuinely untested — teardown tests prove listeners are added/removed but never that they do anything; the disabled-host keydown test never asserts `defaultPrevented`/uses non-cancelable events, so the attempt-2 early-return-before-`preventDefault` fix is unverified; `didUpgradeHost` guard test actually exercises the `hostIsNativelyInteractive` skip branch, not the guard it's named for; scroll listener's capture-phase `true` argument is never asserted despite the test title claiming it; RouterLink block's `TestBed.resetTestingModule()` inside a nested `beforeEach` couples awkwardly to the outer fixture; `custom-fields` specs' cleanup relies on `hide()`'s orphan-node guard rather than `fixture.destroy()`.
#### Attempt 2 — 2026-09-05

- **Skills assigned:** `angular-developer`, `tdd`
- **Effort:** `xhigh` (bumped after attempt 1 FAIL)
- **Files changed:** `pr-tooltip.directive.spec.ts` only (the 2 `custom-fields` specs were already correct from attempt 1, confirmed and left untouched); `pr-tooltip.directive.ts` confirmed zero-diff (untouched across both attempts).
- **Fixes applied, with falsifiability explicitly verified by reverting the corresponding directive fix, confirming the test failed, then restoring it and confirming green again:**
  1. RouterLink test rebuilt on a `<div [routerLink]="routerLinkValue()">` bound to a `null` signal — keeps a real `RouterLink` instance attached (Angular instantiates by template presence) while avoiding RouterLink's own `tabindex` side effect (only fires on a non-null value, verified against `@angular/router`'s actual source). Added an in-suite counterfactual `plainDivHost` (identical minus `[routerLink]`) asserted to **receive** the upgrade — the differential that actually isolates the `!!this.routerLink` term. Falsifiability confirmed: deleting `!!this.routerLink` from the directive made `role` appear on `routerHost` and `keydown.enter` open the tooltip — 2 failures, as predicted; restored, green again.
  2. Escape-restores-focus test rebuilt with a real `<a href="#">` inside the tooltip, manually focused (not via CDK `FocusTrap`, correctly avoided since jsdom doesn't populate `offsetParent`/`getClientRects` for the trap's checks), with `document.activeElement === link` asserted **before** Escape fires (so a jsdom focus no-op fails loudly rather than passing for the wrong reason). Falsifiability confirmed: commenting out the `focusIsInWidget`-gated restore line in `hide()` made focus fall to `document.body` instead of the trigger — test failed as predicted; restored, green again.
  - All 5 attempt-1 advisory items applied: bottom-position coverage restored, disabled-host keydown test now asserts `cancelable:true` + `defaultPrevented === false`, scroll capture-phase argument asserted, new test proving scroll-while-pinned actually re-runs `position()`, misnamed `didUpgradeHost` test retitled to accurately describe the `hostIsNativelyInteractive` skip path it exercises.
- **Implementer verification:** `npx jest --testPathPattern="pr-tooltip.directive.spec"` → 47 passed (up from 44). `npx jest --testPathPattern="pr-field-header.component.spec|field-card.component.spec"` → 25 passed. `npx ng lint --quiet` clean.
- **Reviewer verdict:** `STATUS: PASS`. Both fixes independently re-verified as genuinely falsifiable (not just Implementer-claimed): confirmed the RouterLink/`tabindex` side-effect premise directly against the installed `@angular/router` source (`setTabIndexIfNotOnNativeEl`); confirmed the `plainDivHost` counterfactual cleanly isolates the `routerLink` term from `role`/`tabindex`/tag/type; confirmed the focus-restore test's pre-Escape assertion means a jsdom focus quirk would fail loudly rather than silently pass; confirmed all 5 advisory fixes are real code changes; confirmed no new vacuous tests were introduced (scroll-reposition and vertical-clamp tests both independently checked as falsifiable); confirmed `pr-tooltip.directive.ts` is genuinely untouched and matches what `TIP-T-1` attempt 2 landed.
  - **ADVISORY (non-gating):** a code comment states RouterLink's null-branch "never touches tabindex" when it actually removes it (conclusion still correct since input setters run before `ngOnInit`, but the stated mechanism is wrong — fix the comment); the `tabindex`-absence assertion is durable only as long as Angular's input-before-`ngOnInit` ordering holds (the `role` + `plainDivHost` assertions are ordering-independent and are the durable proof); one test title ("does not reposition... after hide()") slightly overclaims what it checks; `tasks.md` DoD's "zero `.pr_label_tooltip` references" is satisfied in substance but not literally (one mention survives in file-header prose explaining the retarget — same call already made for `TIP-T-2`'s residual comment, treated consistently); `TestBed.resetTestingModule()` coupling in the RouterLink nested `beforeEach` (flagged in attempt 1) remains unaddressed — harmless, cosmetic.

**Requirements covered:** `TIP-R-21`, `TIP-AC-1`, `TIP-AC-2`, `TIP-AC-3`, `TIP-AC-4`, `TIP-AC-6` (presence only, explicitly commented as such), `TIP-AC-10`.

**Decisions made:** regression tests added for all 4 of `TIP-T-1`'s attempt-1 FAIL issues (RouterLink skip, `syncHostAffordance` runtime re-sync, outside-click focus-non-steal, LiveAnnouncer via ARIA presence) so none can silently regress. Kept the CDK `FocusTrap`-avoidance pattern (manual `.focus()` calls) as the correct jsdom-compatible technique for any future focus-behavior test in this file.

**Issues encountered:** attempt 1 FAILed on 2 "vacuous test" defects — both fixed and independently re-verified in attempt 2, with the Reviewer explicitly re-deriving the falsifiability rather than trusting the Implementer's report.

**Final verification result:** `npx jest --testPathPattern="pr-tooltip.directive.spec"` → 47 passed; `npx jest --testPathPattern="pr-field-header.component.spec|field-card.component.spec"` → 25 passed; `npx ng lint --quiet` clean.

**Final status: PASS (attempt 2/3).**

**Next eligible tasks:** `TIP-T-4` (Cypress focus/clamp/reposition), `TIP-T-5` (Cypress compound-click sites) — both depend only on `TIP-T-1` (already landed), parallel-friendly with each other (no shared files). `TIP-T-6` still blocked on both.

---

### `TIP-T-5` — Cypress regression coverage for the 5 flagged compound-click sites

**Status:** IN PROGRESS (attempt 1 FAIL, attempt 2 pending)

#### Attempt 1 — 2026-09-07

- **Skills assigned:** `angular-developer`
- **Effort:** `high`
- **Files created (all new `*.cy.ts`, colocated with each site):**
  1. `result-review-drawer.approve-tooltip.cy.ts` (Approve button)
  2. `phase-management-table.edit-action-tooltip.cy.ts` (edit action, tooltip on child `<i>`)
  3. `results-list.menu-item-tooltip.cy.ts` (dropdown menu item)
  4. `user-management.toggle-tooltip.cy.ts` (status toggle)
  5. `reporting-aow-table.achieved-tooltip.cy.ts` (Achieved cell — real component mounted directly)
- **Mount decisions:** sites 1-4 used a "harness" (markup copied from the real template) because each real component pulls in a large DI graph. Site 5 mounted the real component directly (standalone, presentation-only).
- **Implementer verification:** `npx cypress run --component --spec <the 5 files>` → 10/10 passing.
- **Reviewer verdict:** `STATUS: FAIL` (2 gating issues + 1 minor, static read since Reviewer cannot execute Cypress — verified every claim against the real templates/components directly)
  1. **Harness drift means 4 of 5 real sites have ZERO verification.** For sites 1-4, the harness mounts a *copy* of the markup — deleting the real `(click)` binding, flipping the guard, or dropping `[prTooltip]` from any of the 4 real template files leaves all 8 tests green, because nothing reads the real file. This becomes a hole, not just a gap, because `tasks.md` `TIP-T-6`'s manual sweep explicitly EXCLUDES these 5 sites on the assumption `TIP-T-5` already covered them — so `result-review-drawer.component.html`, `phase-management-table.component.html`, `results-list.component.html`, `user-management.component.html` would finish this spec with **no verification of any kind**. Only site 5 (real mount) is actually exercised. Violates `requirements.md` §8 `TIP-AC-9` and `tasks.md` TIP-T-5's own Files field (new *.cy.ts for the component, or an E2E spec if mounting is impractical — a markup replica is neither). Remediation: add a template-drift lock to each of the 4 harness specs (`cy.readFile()` the real `.component.html`, assert the exact binding strings the harness replicates are still present) — cheap (~5 lines/spec), precedented in this repo. Also: the Leader must amend `TIP-T-6`'s scoping sentence to pull these 4 sites back into the manual sweep regardless of whether the drift lock is added.
  2. **Site 3's harness materially diverges from the real template and its own docstring is factually wrong about it.** The real `results-list.component.html` menu item carries `tabindex="0"`, `role="menuitem"`, and its own `(keydown.enter)` handler — none of which the harness has. This flips which directive branch gets exercised: the real site is `hostIsNativelyInteractive === true` (already-interactive, directive leaves it alone) while the harness is `false` (gets the full role/tabindex/keydown upgrade) — the OPPOSITE branch. Untested and unrecorded consequences at the real site: `Enter` fires the command but never opens the tooltip (correct, but unverified), and `pin()` stamps `aria-expanded`/`aria-controls` onto a `role="menuitem"` (invalid ARIA for a non-submenu menuitem — a real accessibility defect, unflagged). Violates `design.md` TIP-DD-3 Consequences (exactly the "already doing their own thing" site class that must be detected and left alone) and `TIP-AC-4`'s negative constraint. Remediation: add the missing `tabindex`/`role`/`keydown.enter` to the harness, correct the docstring, add a keyboard assertion for the real branch, and record the `aria-expanded`-on-`role="menuitem"` finding for `TIP-T-6`/`TIP-DD-2` (not fixed in this task).
  - **Minor (non-gating but noted):** site 4's docstring overclaims — the real `onToggleUserStatus` doesn't flip `isActive` synchronously (it opens a modal or a confirm-gated alert), so the harness's synthetic flip handler doesn't literally verify "the toggle still flips" as `tasks.md` states. Reword the docstring to describe it as a stand-in equivalent to a spy, and record the real confirm-gated flip as `TIP-T-6`'s to verify manually.
  - **ADVISORY (non-gating):** sites 1 and 2's `TIP-DD-1` collision is UNREACHABLE by construction (tooltip text and the click action share the same guard predicate/disabled state) — good news, but means `tasks.md`'s "5 riskiest sites" selection over-counted; the real per-site collision surface is sites 3/4/5. Worth recording back into `design.md` §10.1. Also: site 1's Approve guidance tooltip is very likely unreachable by ANY real user gesture (non-empty only while the button is natively `disabled`, which suppresses click/hover) — a pre-existing product-level issue, not this spec's to fix, but should be escalated as a finding for `TIP-T-6` rather than left as an inline test comment only. Also: two `{force:true}` clicks in the tests exist because the pinned tooltip covers its own trigger — this could mean a real (non-forced) re-click to un-pin is genuinely blocked in some layouts, undercutting `TIP-DD-1`'s "user must dismiss it separately" premise; worth a `TIP-T-4`/`TIP-T-6` assertion. Site 2's spec covers only the edit action, not delete (structurally different shape) — low incremental value since delete's collision is same-element already covered by sites 3/4, but recorded as a known gap.
- **Next:** Attempt 2 — effort bumped to `xhigh`. Structured feedback above passed verbatim. Attempt History note: do not repeat (a) a markup-copy harness with no drift lock back to the real template file, (b) a harness claimed as "verbatim" from a template without actually diffing it against the real file's full attribute list (role/tabindex/keydown handlers included, not just the click/tooltip bindings).

#### Attempt 2 — 2026-09-07

- **Skills assigned:** `angular-developer`
- **Effort:** `xhigh`
- **Files changed:** files 1, 2, 3, 4 edited in place (file 5 untouched, already correct from attempt 1).
- **Fixes applied:** template-drift lock (`cy.readFile()` + exact binding-string assertions) added to files 1/2/3/4; file 3's harness rebuilt to carry the real `tabindex="0"`/`role="menuitem"`/`(keydown.enter)` attributes, docstring corrected, `aria-expanded`-on-`role="menuitem"` finding recorded as a `TIP-T-6`/`TIP-DD-2` follow-up; file 4's docstring reworded to honestly describe its synthetic handler.
- **Falsifiability checks:** drift lock — corrupted the real `user-management.component.html`'s click binding locally, confirmed the lock failed, restored, confirmed green. Site-3 keyboard branch — stripped `tabindex`/`role` from the harness, confirmed 2/3 tests failed as predicted, restored, confirmed 11/11 green.
- **Reviewer verdict:** `STATUS: FAIL` on first read (Issue 1: `pr-tooltip.directive.ts` was mid-swap from `TIP-T-4`'s concurrent falsifiability check at read-time — see Leader intervention above; confirmed stale/not a real defect once the checkout was quiet, Leader independently re-ran all 5 files → 11/11 passing). Issue 2 (genuine, carried to attempt 3): file 3's new keyboard test was misnamed — titled as proving `command()` fires on Enter, but its body used a disabled item and asserted the opposite (`command()` NOT called).
- **Final status this attempt:** 1 gating issue confirmed real (Issue 2) → attempt 3.

#### Attempt 3 — 2026-09-07

- **Skills assigned:** `angular-developer`
- **Effort:** `medium` (single, well-understood fix, not a full rework)
- **Files changed:** `results-list.menu-item-tooltip.cy.ts` only.
- **Fix applied:** added a new test (enabled item, real `keydown` Enter trigger, asserts `command()` AND `closeMenu()` both fire exactly once, no tooltip, role preserved) — the genuine real-world keyboard path `design.md` §10.1 item 3 asks for. Renamed the old disabled-item test to accurately describe what it proves. Tightened the drift lock's `tabindex="0"` check (occurs twice in the real file — an unrelated `<th>` and this menu item) to a count assertion (`=== 2`) plus a contiguous-string assertion for the menu item's specific `tabindex`/`role` pairing.
- **Implementer verification:** `npx cypress run --component --spec ".../results-list.menu-item-tooltip.cy.ts"` → 4 passing. `npx ng lint --quiet` clean.
- **Reviewer verdict:** `STATUS: PASS`. Independently re-read the real template and confirmed: the new test's assertion direction is genuinely falsifiable (starts at 0 calls, can only pass if the real `(keydown.enter)` handler's `command()` branch actually executes); the renamed test's title now matches its body; the tightened drift lock's byte-level assumptions (CRLF line endings, exact indentation, occurrence counts) were independently verified against the real file and do catch removal of the menu item's own `tabindex` specifically (not just the unrelated `<th>`'s).
  - **ADVISORY (non-gating):** the drift lock's literal `\r\n` dependency is checkout-line-ending-sensitive — would fail loudly (not silently) on a clone with different line-ending normalization; a `\r?\n` regex would be more portable. Not urgent since Cypress CT is local-only.

**Requirements covered:** `TIP-DD-1` (verification), `TIP-AC-1`, `TIP-AC-9` (5 of the riskiest 39 files — with 2 of the 5, sites 1/2, recorded as having their compound-click collision unreachable by construction, a real finding worth carrying into `design.md` §10.1 and `TIP-T-6`).

**Decisions made:** harness-based testing (not real-component mounts) for 4 of 5 sites, defended by a template-drift lock rather than a real mount, given the DI-graph cost of mounting the real components. Site 5 mounted the real component directly (no DI graph). The `aria-expanded`-on-`role="menuitem"` ARIA-validity gap found at site 3 was deliberately NOT fixed here (out of this test-only task's scope) — recorded for `TIP-T-6`/`TIP-DD-2`.

**Issues encountered:** 2 gating issues in attempt 1 (harness drift with no lock; site 3's harness exercising the wrong directive branch) — both fixed in attempt 2. 1 residual issue in attempt 2 (misnamed test) — fixed in attempt 3. A concurrency collision with `TIP-T-4`'s parallel rework transiently corrupted the shared directive file and caused one false-alarm Reviewer FAIL — resolved by the Leader without losing any work (see `TIP-T-4`'s attempt 2 entry above for the full incident record).

**Final verification result:** `npx cypress run --component --spec <all 5 files>` → 11/11 passing (Leader-verified fresh against the correct, stable directive after the concurrency incident was resolved); `npx ng lint --quiet` clean throughout.

**Final status: PASS (attempt 3/3 — at the rework ceiling, but the ceiling was reached via 2 legitimately-scoped narrow follow-ups after the attempt-1→2 transition, not 3 full reworks of the same defect class; no HALT triggered since each attempt closed real, different findings).**

---

### `TIP-T-4` — New Cypress component tests: focus order, vertical clamp, scroll/resize reposition

**Status:** IN PROGRESS (attempt 1 FAIL, attempt 2 pending)

#### Attempt 1 — 2026-09-07

- **Skills assigned:** `angular-developer`
- **Effort:** `high`
- **Files created:** `onecgiar-pr-client/src/app/custom-fields/pr-field-header/pr-field-header.tooltip-a11y.cy.ts` (new file, 5 tests: focus-order + negative case, vertical clamp, scroll reposition, resize reposition)
- **Infra changes (needed, in scope):** added `cypress-real-events` devDependency (`package.json`/`package-lock.json`) for genuine CDP-level Tab dispatch (Cypress core has no `{tab}` support); registered in `cypress/support/component.ts`; added to `tsconfig.ct.json` `types`.
- **Genuine bug found and fixed along the way (test infra, in scope):** `.pr-tooltip` is appended to `document.body`, outside the Angular tree Cypress remounts between tests — a pinned tooltip could survive into the next test. Added an `afterEach` cleanup scoped to `.pr-tooltip` nodes only (does not touch the Cypress mount root).
- **Implementer verification:** `npx cypress run --component --spec ".../pr-field-header.tooltip-a11y.cy.ts"` → 5/5 passing. `npx ng lint --quiet` clean. Full batched `test:ct` run found 3 pre-existing failures (`add-button.contract.cy.ts`, `field-card.contract.cy.ts`, `pr-field-header.contract.cy.ts`) — verified unrelated via `git stash` isolation.
- **Reviewer verdict:** `STATUS: FAIL` (1 gating issue — the scroll half of `TIP-AC-8`/`TIP-R-8` is unproven)
  1. **The window-scroll reposition test cannot fail on an implementation with NO scroll listener at all.** The directive positions the tooltip in DOCUMENT coordinates on an absolutely-positioned node (`top = ... + window.scrollY`, `.pr-tooltip { position: absolute }`) — so on a plain window scroll, the tooltip is ALREADY scroll-invariant relative to the trigger even with zero listeners: `style.top` stays at `trigger.bottom_before + gap + 0`, and after the page scrolls, `rect.top` (which subtracts the new `scrollY`) lands at exactly `trigger.bottom_after + gap` regardless of whether `position()` ever re-ran. Both "listener works" and "no listener exists" branches satisfy the test's assertion to sub-pixel precision. The companion "must have changed" guard is equally inert — satisfied by the page scroll alone. The case the directive's `capture: true` was specifically built for (ancestor-scrollable containers — tables, drawers, per `design.md` DD-4) is never exercised; only there does window-level `scrollY` NOT already compensate for the movement. The falsifiability spot-check (pre-`TIP-T-1` directive) didn't catch this because that revert fails for an unrelated reason (no unconditional pin), masking that the scroll assertion itself is inert. **Note the resize test (#5) is genuinely falsifiable and was independently re-verified — only the scroll half is the problem.** Violates `tasks.md` TIP-T-4 DoD's explicit falsifiability requirement, `design.md` §10 row 4, `requirements.md` `TIP-AC-8`.
     * **Remediation:** replace the window-scroll fixture with an `overflow: auto` container whose INNER scroll moves the trigger independently of `window.scrollY` (e.g. a `<div style="height:200px; overflow:auto">` with spacers) — pin the tooltip, `cy.get('#scrollBox').scrollTo(0, 150)`. Without `position()` re-running, the tooltip stays at its stale spot while the trigger visibly moves, so the assertion fails by the scroll delta. This also exercises the `capture: true` path the plain-window fixture never reaches. Re-run the pre-`TIP-T-1` spot-check on this test specifically, AND separately confirm it fails against the CURRENT directive with the scroll/resize registration lines commented out — that second check is the one that would have caught this defect.
  - **What passed (Reviewer's own words, worth preserving):** focus-order test (#1) meets the no-pass bar (exact per-Tab sequence by id, `cy.realPress` uses real CDP dispatch, Enter-activation via stub not "nothing threw", trap wrap-around consistent with CDK mechanics). Vertical clamp test (#3) is genuinely oversized and genuinely falsifiable (correctly avoids the `sgi-dac-tooltip` 320px cap; 30 paragraphs render far past viewport height). Pre-existing-failures claim independently confirmed as unrelated (grepped all 3 failing contract specs for any tooltip/focus/role keyword — zero hits). Cleanup fix confirmed in-scope and correctly scoped. `package-lock.json` inclusion confirmed correct and required (not scope creep).
  - **ADVISORY (non-gating):** test #2 (plain-text tooltip, no dead trap) is currently a tautology — would pass even if the `hasTabbableContent` gate were deleted, since nothing in the fixture could move focus regardless; strengthen by asserting CDK sentinel anchors are absent. `.pr-tooltip`'s CSS pop animation (`translateY` over 0.16s) could make a `getBoundingClientRect()` read up to 4px off if taken in the same tick as a click — harmless today given current tolerances, worth a comment. `cy.realPress` hard-depends on a Chromium-family browser (CDP) — fine since Cypress is local-only, but worth noting in the PR description. Pre-existing stale comment at `pr-field-header.component.html:17-21` still claims the directive's "default (click hides)" applies, contradicting `TIP-DD-1`/`DD-5` — belongs to `TIP-T-2`/`TIP-T-6`, flagged not fixed here.
- **Next:** Attempt 2 — effort bumped to `xhigh`. Structured feedback above passed verbatim. Attempt History note: do not repeat testing "did position change" against a window-level scroll on a directive that stores tooltip position in document coordinates — document-coordinate absolute positioning is inherently scroll-invariant under `window` scroll; the only fixture shape that can discriminate "listener re-ran `position()`" from "no listener exists" is one where the TRIGGER moves independently of `window.scrollY` (an inner scrollable ancestor).

#### Leader intervention — concurrency collision, 2026-09-07

Running `TIP-T-4` attempt 2 and `TIP-T-5` attempt 2 in parallel (both in the same checkout, per the Delegation Thresholds "no shared files" independence test — their *deliverable* files are disjoint) surfaced a shared-file collision neither task's file list predicted: **both tasks' falsifiability checks require temporarily reverting/swapping `pr-tooltip.directive.ts`** (a file neither task owns or edits permanently, but both must transiently mutate to prove their tests can fail). One agent's temporary swap was mid-flight when the other read the file, and/or a restore step left the working tree on the reverted (pre-`TIP-T-1`) state rather than `HEAD`. `TIP-T-5` attempt 2's agent found the file already reverted, ran its own verification against a temporarily-restored copy, then defensively restored to what it found (the reverted state) rather than to `HEAD` — reasonable caution given its brief, but it perpetuated the corruption instead of fixing it. `TIP-T-4` attempt 2's agent correctly detected the anomaly, made zero edits, and stopped to ask rather than guessing (per `.agents/leader.md` "never economize on correcting a delegation you already know is malformed" — the inverse case: an agent that recognizes it's *about* to build on bad state and halts).

**Resolution:** verified no agent was still active (`ListAgents` — both had returned), confirmed `pr-tooltip.directive.spec.ts`'s large diff against `HEAD` was legitimate (it holds `TIP-T-3`'s approved-but-uncommitted attempt-2 rework, never committed since only `TIP-T-1`+`TIP-T-2` landed in `9ac22e425`) and must NOT be touched, then ran `git restore --source=HEAD -- .../pr-tooltip.directive.ts` (safe: `TIP-T-1` is committed and Reviewer-approved, so `HEAD` IS the correct state for this specific file). Confirmed clean: `git diff` on the directive empty, `npx jest --testPathPattern="pr-tooltip.directive.spec|pr-field-header.component.spec|field-card.component.spec"` → 72/72 green. Resumed the waiting `TIP-T-4` agent with the corrected state and an explicit note not to diff the spec file against `HEAD` as a sanity check (it will look "wrong" by design, since it's ahead of `HEAD`, not reverted).

**Lesson for future waves of this spec (and other specs with a similar "revert-to-prove-it-fails" verification pattern):** a task's file-independence check must also ask "does this task's *verification method* require mutating a file it doesn't own, even temporarily?" — `TIP-T-1`/`TIP-T-3`'s rework loops already established that reverting the directive to prove test falsifiability is the correct technique; running two such reverts concurrently in one checkout, on the same shared dependency, is exactly the `.agents/leader.md` Concurrency Protocol's "different files still collide through everything the checkout shares" case, just via a *temporary* mutation instead of a permanent one. No committed work was lost — both `TIP-T-3`'s uncommitted spec and `TIP-T-5`'s completed attempt-2 files were intact throughout.

**Second occurrence:** the `TIP-T-5` attempt-2 Reviewer (spawned after the fix above, in parallel with the just-resumed `TIP-T-4` agent) read `pr-tooltip.directive.ts` mid-way through `TIP-T-4`'s own falsifiability check and reported it as the pre-`TIP-T-1` version (Issue 1 of that review) — a real observation at read-time, not a hallucination, but stale by the time it was reported: `git diff` on the directive is empty again as of this note. This confirms the collision is systemic to running these two tasks' rework concurrently, not a one-off. Decision: do not re-spawn a `TIP-T-5` attempt-3 Reviewer until `TIP-T-4`'s rework fully completes and the checkout is quiet — re-verify `TIP-T-5`'s actual pass/fail state fresh once `TIP-T-4` reports back, folding the Reviewer's still-valid Issue 2 (file 3's new keyboard test is misnamed — asserts `closeMenu()` fires but not `command()`, and titled as if it proves the opposite of what it checks) into that follow-up rather than re-running a full review over a directive-state false alarm.

**Resolved, both threads:** `TIP-T-4` attempt 2's agent finished (with the corrected directive), ran both required falsifiability checks successfully (pre-`TIP-T-1` revert → 4/5 tests fail as expected; current directive with just the scroll listener commented out → the new scroll test fails specifically, `expected 26 to be close to 76`, exactly the defect class attempt 1 missed; both checks fully reverted afterward, `git diff` on the directive confirmed empty twice). Leader independently re-ran `TIP-T-5`'s full 5-file Cypress suite once the tree was quiet and stable: **11/11 passing**, confirming attempt 2's fixes are real and Issue 1 of that review was purely the transient directive-state false alarm, not a genuine regression. Issue 2 (site 3's misnamed keyboard test — asserts `closeMenu()` but not `command()`, titled as if it proves the opposite) remains open and is queued as `TIP-T-5` attempt 3, scoped narrowly to that one fix.

#### Attempt 2 — 2026-09-07

- **Skills assigned:** `angular-developer`
- **Effort:** `xhigh` (bumped after attempt 1 FAIL)
- **Files changed:** `pr-field-header.tooltip-a11y.cy.ts` only — the `TIP-AC-8` scroll describe block replaced; the other 4 tests (focus-order + negative, vertical clamp, resize) untouched, confirmed unchanged by the Reviewer against attempt 1's own description.
- **Fix applied:** replaced the window-scroll fixture with a trigger inside an `overflow: auto` ancestor container (`#scrollBox`), scrolling the BOX (not the window) so the trigger moves independently of `window.scrollY` — the one fixture shape that can discriminate "the capture-phase listener re-ran `position()`" from "no listener exists". Both required falsifiability checks run: (1) pre-`TIP-T-1` directive → 4/5 fail (`.pr-tooltip` never found, expected — old directive needs opt-in pinning); (2) current directive with ONLY the scroll listener + its teardown commented out → 4/5 pass (resize still works, isolating the change) and the new scroll test fails with `expected 26 to be close to 76 +/- 2` (stale pre-scroll position). Both checks fully reverted, `git diff` on the directive confirmed empty.
- **Implementer verification:** `npx cypress run --component --spec ".../pr-field-header.tooltip-a11y.cy.ts"` → 5/5 passing. `npx ng lint --quiet` clean.
- **Reviewer verdict:** `STATUS: PASS`. Independently reconstructed the fixture geometry from the CT viewport (1280×720) and the directive's actual clamp/position formulas to confirm the reported 26/76 pixel values are real and clear of the vertical clamp (8 / ≈660 bounds) — not just accepted on claim. Confirmed the ancestor-scroll fixture genuinely exercises the `capture: true` listener path (`scroll` doesn't bubble but does propagate in capture phase — the only registration that can observe it). Confirmed falsifiability check #2's isolation is coherent (commenting out only the scroll line, not resize, produces exactly "4 of 5 pass" with resize among the passers). Confirmed the other 4 tests are byte-for-byte consistent with what already passed attempt 1's review. Confirmed no directive/template/Jest/TIP-T-5 file touched, and the directive's scroll/resize listeners are live and uncommented with no leftover experiment state.
  - **ADVISORY (non-gating):** the in-test comment's stated arithmetic ("scrollTop 300 puts the trigger ~100px down inside the 200px window") is superseded by Cypress's default click auto-scroll-into-view behavior (which is what actually produces the 26px pre-scroll reading) — the clamp-clearance conclusion the comment supports is still correct, only the stated reasoning is off. Cosmetic; worth a one-line comment fix for the next maintainer, not gating.

**Requirements covered:** `TIP-AC-5`, `TIP-AC-7`, `TIP-AC-8` (both scroll and resize halves, now both genuinely falsifiable).

**Decisions made:** used `cypress-real-events` for genuine CDP-level Tab dispatch (no synthetic-event alternative exists for real focus movement); chose an ancestor-scrollable-container fixture over a window-scroll one specifically because document-coordinate absolute positioning is inherently window-scroll-invariant, which is itself a useful fact now recorded for any future test in this area.

**Issues encountered:** attempt 1's scroll test was a "test that cannot fail" defect (structurally inert against a window-level scroll) — fixed in attempt 2 with an ancestor-container fixture, independently re-verified by the Reviewer via geometric reconstruction, not just re-reading the claim. A concurrency collision with `TIP-T-5`'s parallel rework transiently corrupted the shared directive file mid-task (see Leader intervention above) — resolved without any lost work, required one pause-and-resume cycle.

**Final verification result:** `npx cypress run --component --spec ".../pr-field-header.tooltip-a11y.cy.ts"` → 5/5 passing; `npx ng lint --quiet` clean; `git diff` on `pr-tooltip.directive.ts` empty.

**Final status: PASS (attempt 2/3).**
