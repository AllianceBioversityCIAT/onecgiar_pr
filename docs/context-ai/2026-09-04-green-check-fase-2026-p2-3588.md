# P2-3588 — el sign-off end-to-end del formulario 2026 de Innovation Development

**Verificado:** 2026-09-04, tarde · rama `performance-refactor` · ambiente **prtest** (build v39)
**Tickets:** [P2-3588](https://cgiarmel.atlassian.net/browse/P2-3588) (subtarea de `P2-3513`) ·
`P2-3513` · `P2-3531` (resuelto, Juan David) · épica `P2-3243`

> **Por qué existe este archivo.** `P2-3513` volvió a nosotros hoy a las 11:52 (`Test In Progress` →
> `To Be Improved`, sin comentario). Lo que falta no es lo entregado — Cami lo verificó y no encontró
> defectos — sino el sign-off completo, que estaba **bloqueado por `P2-3531`**. Ese bloqueo cayó el
> 3-sep. Esto es el intento de cerrarlo y lo que apareció por el camino.

## Lo que quedó probado

✅ **La rama 2026 del green check funciona.** Resultado **11033** (fase 2026, `innovation-dev-info`)
da **verde** por la ruta v2, medido hoy a las 19:25. Con la pregunta consolidada de propiedad
intelectual (id **162**) contestada.

✅ **La pregunta consolidada se sirve por FASE, y por `result.id`.** 11033, 11267 y 11513 (fase 2026)
reciben **solo** la 162 con sus tres opciones (163 Yes / 164 Not sure / 165 No).

🛑 **Trampa de medición, cuesta media hora si se cae en ella.** El endpoint
`GET /v2/api/results/questions/innovation-development/:resultId` toma el **`result.id`**, no el
`result_code` — `result-questions.service.ts` resuelve con `WHERE r.id = ?`. Consultado con el code,
`getResultPhaseYear` no encuentra fila, devuelve `null`, y el servicio **cae al formulario pre-2026**:
salen las cuatro preguntas viejas (101/102/103/138) y parece que la consolidación no se aplicó. No es
eso: es el sujeto equivocado. (La descripción de `P2-3513` cita `.../8869`, que induce al error.)

## Lo que NO quedó probado, y por qué

🔴 **No se pudo llevar ningún resultado de fase 2026 a las cinco secciones verdes**, así que los
criterios de aceptación 1 y 2 de `P2-3588` (sección completa → *Submit* habilitado) siguen abiertos.

El intento, en `11143` (`prueba angel 123`, fase 2026, estado `editing`, 4 de 5 secciones ya verdes):

1. Le faltaban exactamente tres respuestas: **150** (stage GESI), **151** (stage de riesgo) y **162**
   (propiedad intelectual consolidada).
2. Se contestaron **en pantalla**, se guardó con *Save draft*, **se recargó** y persistieron.
3. `innovation-dev-info` **sigue en rojo**. `submit` sigue en `false`.

**La diferencia medida contra el único verde**, comparando las 36 respuestas de 11143 y 11033 una a
una: la respuesta marcada de la pregunta **136** ("How have the end users/stakeholders been involved…")
tiene `answer_text` **vacío** en 11143 y **con texto** en 11033. Es el único eje que distingue al
verde del rojo, junto con megatrends (ver abajo).

⚠️ **Y el campo que debería permitir escribir ese texto no se pinta.** El template
`assumptions-examination.component.html` declara `textInputWhenSelectedLabels` con las tres opciones
que llevan un "Why?" — incluida *"Not considered necessary for this innovation"*, que es la marcada.
Medido cinco veces sobre `app-assumptions-examination input[type=text]` (al cargar, a 500 ms, 1,5 s,
3 s, y tras cambiar de opción y volver): **0 campos**.

🛑 **Una medición anterior sí lo encontró**, con un contenedor calculado con *fallback*
(`closest('.radioButtonList') || closest('div')`). No se ha podido reproducir. **Esa contradicción no
está resuelta** y por eso esto se reporta como hallazgo con evidencia, no como defecto confirmado:
un instrumento que dio dos respuestas no cierra nada. Quien lo retome, empiece por ahí.

## El segundo sospechoso, sin descartar

`11143` tiene marcada la respuesta **126** (*Demographic trends*), de **Megatrends** — la sección que
`P2-3465` retiró del formulario 2026. No se puede desmarcar desde la pantalla actual porque ya no se
pinta. Si la regla viva todavía mira ese bloque, cualquier resultado que contestara megatrends en su
día queda atrapado sin forma de salir por la interfaz.

**No se pudo separar de la hipótesis del `answer_text`** con los datos disponibles. El cruce sobre 45
resultados de fase 2026 (sin un solo error de red) da:

```
verde   con megatrends   sin megatrends
True                  0                1     ← 11033
False                 5               39
```

Y **ninguno** de los 44 rojos tiene a la vez la IPR contestada y megatrends vacío, así que no hay
contraejemplo… ni tampoco confirmación: la muestra a favor es **n=1**.

⚠️ **El "1 de 45" no prueba un defecto por sí solo** — la pregunta 162 es nueva y obligatoria, y casi
nadie la ha contestado todavía. Lo que pide explicación es el caso concreto de 11143, no el conteo.

## Lo que hace falta para cerrarlo, y de quién es

**El cuerpo vivo de `validation_innovation_dev_P25`.** Sin él esto es adivinar: la función se aplica
a mano por ambiente, las copias del repo están stale, y `SHOW CREATE FUNCTION` necesita VPN (probada
hoy: `ETIMEDOUT`). El archivo que Juan David anunció el 3-sep,
`src/shared/querys/manual/P2-3531-validation_innovation_dev_P25.sql`, **sigue sin estar en el repo**
— sólo existe el de `P2-3552`.

Por el reparto por módulo, la regla es de **Juan David**; el campo "Why?" que no se pinta, si se
confirma, es **nuestro** (`result-detail` / `custom-fields`).

## Los correos (AC 3), no intentados a propósito

El tercer criterio pide que al enviar salgan `email_template_ip_experts_support_2026` y
`email_template_ip_support_confirmation_2026`. Probarlo exige **un Submit real**, y ese Submit manda
correo a los IP focal points reales — `intellectual_property_experts` tiene 25 personas en 16 centros
y `getIpExpertsEmailsByResultId` resuelve las del lead centre. **No se hace sin OK de Yeck.**
La vía sin correos sería leer las filas de `template` en la base: necesita VPN.

## Datos escritos en prtest (R22 — se declara, no se restaura)

Todo sobre **11143** (`result_code` 8675, `prueba angel 123`, resultado de prueba, fase 2026):

| Qué | Antes | Ahora |
|---|---|---|
| 150 — stage GESI | sin contestar | *Emerging: specific GESI strategies being tested* |
| 151 — stage de riesgo | sin contestar | *Emerging: active mitigation strategies being tested* |
| 162 — propiedad intelectual | sin contestar | **No** (elegida a propósito: "Yes"/"Not sure" dejan armado el envío de correos a los especialistas) |
| 136 — end users | *Not considered necessary*, `answer_text` = `XYA` | *Not considered necessary*, `answer_text` **vacío** |

⚠️ **La última fila es un efecto del guardado, no una edición intencionada.** Durante el diagnóstico
se cambió la respuesta a *"Yes, the following actions have been taken:"* y se devolvió a la original;
el `XYA` se perdió en el guardado y **no se puede reescribir desde la interfaz** mientras el campo
"Why?" no se pinte. Es, en sí mismo, parte de la evidencia.

🛑 **No se tocó 11267** (`8799`): está en estado `submitted`. Se abrió, se vio que tenía *Unsubmit
result*, y se salió sin guardar.

## Cómo re-medir

```bash
TOKEN=$(grep '^USER_TOKEN=' /Users/yeck/Desktop/reporting/.env | cut -d'"' -f2)

# green check — SOLO la ruta v2, y con result.id
curl -s -H "auth: $TOKEN" \
  "https://prtest-back.ciat.cgiar.org/v2/api/results/results-validation/get/green-checks/11143"

# preguntas — con result.id, NUNCA con result_code
curl -s -H "auth: $TOKEN" \
  "https://prtest-back.ciat.cgiar.org/v2/api/results/questions/innovation-development/11143"
```

Ver también [`2026-09-03-green-check-innovation-dev.md`](2026-09-03-green-check-innovation-dev.md)
para la historia de la función y las dos rutas v1/v2.
