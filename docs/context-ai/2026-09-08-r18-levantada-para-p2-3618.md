# 8-sep-2026 — La regla 18 (ToC no se toca) se levantó UNA vez, para P2-3618

**Si estás leyendo esto porque viste un commit nuestro dentro de Theory of Change: no fue una
violación.** Está autorizado, por escrito, por el dueño del módulo. Este archivo existe para que
nadie lo "corrija" ni lo tome como precedente abierto.

## Qué se tocó

Un solo commit, `f57b0ebd5`, sobre
`onecgiar-pr-server/src/api/results/results-toc-results/repositories/results-toc-results.repository.ts`:

- Las dos expresiones gemelas de `saveInditicatorsContributing` (`:1848` y `:1912`) pasan de
  `this.toNumberOrNull(...)` a `this.toContributingIndicator(...)`.
- Un helper privado nuevo, `toContributingIndicator`, que delega en `toNumberOrNull` y solo añade el
  signo.
- Un spec nuevo (`results-toc-results.repository.spec.ts`) — el primero que tiene ese archivo de
  3164 líneas.

🛑 **`toNumberOrNull` NO se tocó.** Lo comparten ~15 campos (`number_target`, `target_date`,
`toc_level_id`…). Por eso la guarda va en un helper aparte y no dentro de él.

## Quién lo autorizó, y con qué palabras

**Juan David Delgado**, dueño del módulo, por DM el 8-sep-2026 a las 11:23, tras revisar el diff
completo que se le envió a las 11:15:

> *"Las dos decisiones van. Clavar a 0, no rechazar: de acuerdo. 0 y no null: de acuerdo. El green
> check de la sección lee `contributing_indicator IS NOT NULL`, así que con 0 el resultado sigue
> completo y enviable; con null se caería justo antes de producción. **Métele tú con ticket nuevo
> enlazado a P2-3608, helper aparte sin tocar `toNumberOrNull`, y rebase antes del commit. Yo no
> estoy tocando ese método.**"*

La cadena fue: Yeck pidió preguntarle → se le mandó el diff → él aprobó y delegó. Las cuatro
condiciones que puso (ticket nuevo enlazado, helper aparte, no tocar `toNumberOrNull`, rebase antes
del commit) se cumplieron todas.

## Alcance del levantamiento

⚠️ **Es puntual, no general.** Cubre ese commit y nada más. Cualquier otro cambio dentro de
`results-toc-results/**` vuelve a caer bajo la regla 18 y hay que volver a preguntar.

## Por qué no había plan B

Se mapearon todos los caminos de escritura del campo. `saveInditicatorsContributing` es el único
punto por el que pasan ToC v1 (P22), ToC v2, IPSR contributors (P22), share-request (crear y
aceptar) y bilateral center. La otra puerta de servidor, `contributors-partners.service.ts`, vive en
`results-framework-reporting/`, que es de JC — y además solo cubriría 2 de los caminos.

Los dos caminos que quedan fuera **no pueden producir un negativo**: la creación desde RFR ya lo
normaliza (`framework-result-toc-indicators.service.ts:145-155`) y la ingesta bilateral escribe el
literal `1` (`bilateral.service.ts:1680`).

## El dato que cerró la decisión

Juan David contó las filas afectadas sobre la copia de producción: **3 en total** — 2 en Reporting
2025 y 1 en Reporting 2026 (la prueba de Cami), todas en resultados vivos. **Sin migración ni ticket
de datos**: la guarda las corrige en el siguiente guardado de esa sección. Misma decisión que se tomó
para `addressing_demands` en P2-3607.

## Precedente

Mismo patrón que el descongelado puntual de Innovation Use (`91002df61`): se pide, se documenta el
permiso con la cita textual, y el levantamiento no se hereda.
