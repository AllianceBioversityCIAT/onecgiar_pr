# Estado al cierre de la sesión nocturna del 28-ago-2026 — para retomar

**Rama:** `performance-refactor` · sincronizada con `origin` · **working tree sin cambios propios, índice vacío**
**Sesión:** autónoma, madrugada del 28-ago. Orden: *"revisa lo asignado a QA, corrige lo que se pueda, deja quieto lo que tenga impedimento, sube a performance-refactor"*.

---

## 1. Lo primero que hay que hacer

> **Comprobar si prtest ya sirve el build con estos tres cambios.**
> ```bash
> curl -s https://prtest.ciat.cgiar.org/main.js | grep -oE 'APP_VERSION *= *"[0-9]+"'
> ```
> Ninguno de los tres tickets de abajo se ha anunciado a QA como probable — los tres comentarios
> dicen explícitamente *"not on the test site yet"*. Cuando aterrice, se puede avisar.

---

## 2. Lo que se subió (3 commits + 1 merge)

| Commit | Ticket | Qué |
|---|---|---|
| `a9e7ae7c4` | **P2-3519** | Section 1 (General Information) del formulario bilateral muestra la nota de full metadata que Section 2 ya mostraba. |
| `c51193944` | **P2-3504** | El formulario bilateral pregunta a las innovaciones la pregunta de impacto geográfico con el texto aprobado (antes usaba la copia legacy "…regions … for this Output?"). Gateado por `result_type_id` 2 / 7. |
| `11ba9ab1c` | **P2-3508** | `Update result` vuelve al menú de tres puntos de la pestaña Results (`programme-results`), reutilizando `api.shouldShowUpdate` / `api.canUpdateBilateral` y el `change-phase-modal` existente. |

Tests de los tres módulos tocados: verdes (42, 44 y 112). `eslint` limpio.
`CLAUDE.md` de `section-geography` creado; el de `programme-results` actualizado y re-sellado (120 líneas exactas).

---

## 3. Lo que quedó BLOQUEADO (no se tocó, a propósito)

### P2-3518 — el proyecto W3/Bilateral no es editable en un borrador ya creado
- Estado: **`To Be Clarified`, asignado a Ángel**, con la pregunta A/B publicada.
- **Doble impedimento:**
  1. **No existe endpoint** que actualice `project_id` de un resultado ya creado. Se escribe sólo en
     `createResultHeader` (`bilateral-center.service.ts:156-159`); los PATCH del centro son
     `submit-for-review`, `planned-result`, `toc-mapping`, `contributors`. Sin él, el desplegable
     cambiaría en pantalla y volvería atrás al recargar.
  2. **Decisión de negocio sin tomar:** el Science Program y el Center vienen del proyecto. Al
     cambiar el proyecto, ¿se limpian (A) o se quedan (B)? B manda el resultado al programa
     equivocado para revisión.
- **No hace falta migración** (la columna existe) → es trabajo de servicio + DTO + controlador, que
  entra en lo que podemos tomar nosotros **cuando llegue la respuesta**.

### P2-3504 — dos cosas del ticket que NO se implementaron (avisadas en el ticket)
1. **El bloque sigue oculto cuando el foco geográfico principal es "Global"** (gate por *scope*, no
   por tipo de resultado). El formulario clásico no tiene esa condición. Pregunta A/B publicada;
   recomendación **A** (dejarlo así: si ya es global no hay "otras" áreas, y B dejaría incompletos
   los resultados globales ya reportados).
2. **`No` NO se pre-selecciona**, aunque el ticket lo pide. En este formulario la respuesta se queda
   `null` hasta que la persona contesta, y hay un "Please answer Yes or No.". Pre-rellenar un campo
   obligatorio graba una respuesta que nadie dio. **Hay un test que fija el `null`** para que nadie
   lo cambie por descuido.

### `Coming soon` de `section-contributors` — dejados quietos
Su propio `CLAUDE.md` (líneas 112-114) ya dice que necesitan **tickets de backend que no existen**:
guardado de contributing science programs, y guardado de enlazado/agrupado + linked results.
No se abrieron tickets: crear actividades es acción externa y **Héctor y Ángel están reorganizando
el tablero justo ahora** (Slack `#dev-prms-pr`, 27-ago 15:49) — meter ruido ahí es contraproducente.

---

## 4. P2-3322 (regresión zoneless) — VERIFICADO ENTERO: está resuelto

Se barrió el inventario completo del ticket contra el código. **Todo arreglado.** Detalle publicado
en el ticket. Lo que importa para no perder tiempo mañana:

- **Tres entradas del inventario NO son bugs**: `rd-partners.component.ts:95`,
  `ipsr-contributors.component.ts:87` y `rd-contributors-and-partners.component.ts:390` viven dentro
  de `deleteContributingCenter(index, updateComponent = false)` y **ningún llamador pasa el segundo
  argumento** (los tres templates llaman `deleteContributingCenter(i)` y aparte
  `setPossibleLeadCenters(true)`). El timer nunca corre. Se escribió un `markForCheck()` ahí y **se
  revirtió**: añadir un `ChangeDetectorRef` para una rama inalcanzable es código muerto.
- ⚠️ **Trampa de verificación:** un `grep` de `markForCheck|detectChanges|viewRefreshSE` da falsos
  positivos de "roto", porque el patrón mayoritario de fix fue **hacer el flag signal-backed**
  (getter/setter sobre un `signal`), que no aparece en ese grep. Hay que mirar el flag, no el timer.

---

## 5. El diseño de Claude cambió el 27-ago (~15:30) — 462 líneas

Snapshot local re-bajado y sobrescrito: `onecgiar_pr/.design-snapshots/PRMS-Reporting.dc.html`.

**Lo que aparece nuevo en el menú de tres puntos de la pestaña Results** (el de `programme-results`):

```
  Download PDF
  Map to ToC          <-- NO existe en el código
  Update result       <-- construido hoy (P2-3508), con el MISMO gate del diseño
  Delete   (danger)   <-- NO existe en el código; el diseño trae el diálogo "Delete this result?"
```

- ✅ **El diseño valida P2-3508 al pie de la letra**: `<sc-if value="{{ r.canUpdate }}">` alrededor de
  `Update result`, que es exactamente el `@if (canUpdateResult(row))` implementado.
- ⚠️ **`View indicator` no aparece en ninguna de las 256 KiB legibles del diseño**, y en el snapshot
  del 26-ago sí estaba. Es la señal de que el diseño lo quitó — y **P2-3395 está en
  `To Be Clarified` preguntándole a Ángel justamente qué hacer con ese control**.
  🛑 **No se escribió en P2-3395 y no se quitó nada del código**, por dos razones: `get_file` corta
  a 256 KiB y el archivo pesa ~265 KB, así que **la cola es inalcanzable y un grep vacío no prueba
  ausencia**; y el ticket ya tiene una pregunta bien formulada esperando respuesta. Escribirle a
  negocio una conclusión basada en una lectura truncada es justo lo que cuesta credibilidad.
  **Decisión de Yeck:** confirmar con el diseño abierto en pantalla y, si se confirma, cerrar P2-3395
  con "el diseño ya no lo incluye" y quitar el control.
- ⚠️ El menú de **Result Detail** (otro artboard) añade `Change result type` y `Delete result`.
  Tampoco existen. No se construyó nada de esto: `Delete result` es destructivo (endpoint, permisos,
  confirmación de negocio) y `Map to ToC` sería ampliar el alcance de P2-3508 sin ticket.

---

## 6. Cosecha de respuestas: VACÍA

`project=P2 AND status="To Be Clarified"` → 19 tickets. **Nadie contestó nada nuevo.** El último
comentario de cada uno es nuestro (el más reciente, P2-3155, del 27-ago 15:22) o un recordatorio
automático de due date del 14-21 de agosto. Los `updated` del 27-ago a las 16:04 son **nuestros
propios toques**, no respuestas.

---

## 7. Estados movidos (y los que NO se movieron)

| Ticket | Antes | Ahora | Por qué |
|---|---|---|---|
| P2-3519 | Open / Cami | **In Progress / Yeck** | desarrollado; hasta el despliegue la pelota es del dev |
| P2-3508 | Open / **sin asignar** | **In Progress / Yeck** | tenía trabajo hecho y ningún dueño |
| P2-3518 | Open / Cami | **To Be Clarified / Ángel** | bloqueado por negocio: la pelota no es nuestra |
| P2-3504 | In Progress / Cami | *sin cambios* | el trabajo está hecho y es testeable; lo pendiente es un aviso, no un bloqueo |
| P2-3322 | Test In Progress / Cami | *sin cambios* | es de QA ahora; sólo se documentó la verificación |

🛑 **No se tocó nada en `Ready For UAT` ni `To Be Deployed`** (54 + 23). Zona intocable: Yeck sube a
producción a mano. Tampoco se reasignó nada a Cami.
🛑 **No se creó ninguna actividad nueva en Jira.**
