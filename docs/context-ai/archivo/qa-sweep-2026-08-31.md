# Exploratory QA sweep — 31 August 2026 · every result-creation pathway

Driven in a real browser against the branch's own code: `localhost:4200` (build stamp **v15**, branch
`performance-refactor`) talking to `prtest-back`. Every finding was checked against the code or the API
before being written down.

**What this file is for:** the 28-Aug sweep covered the sections of a result once it exists. This one
covers **how a result gets created** — six pathways — and whether the form actually persists what the
reporter types. It also records what was exercised and found healthy, so nobody re-tests it from zero.

**Not covered on purpose:** admin and user management (out of scope for this pass), and everything the
28-Aug sweep already cleared (ToC, geographic location, evidence, the type-specific sections, Knowledge
Product sync, the reviewer cycle, filters).

---

## Pathways exercised

| Pathway | Verdict |
|---|---|
| Classic **Report new result** (`/result/result-creator`) | ✅ works |
| **Report emerging result** (Results Framework & Reporting) | 🔴 the innovation-link question never renders |
| **Report** on a ToC indicator | ⚠️ cannot be tested — backend not deployed |
| **Bilateral, manual form** | ✅ works and persists |
| **Bilateral, from an AI draft** | ✅ works, pre-filled |
| **Innovation Package / IPSR step 4** | ✅ today's change works · 🔴 its acceptance criterion cannot be met as written |

---

## Defects found (4)

| # | Severity | What breaks | Owner |
|---|---|---|---|
| H1 | 🔴 | The innovation-link question **never renders** on the emergent pathway: `dashboard-lab.component.html:1643` does not pass `[showInnovationLinkQuestion]`, so it defaults to `false` (`report-result-form.component.ts:93`). Measured live: `result_type_id`=2 ✅, `phaseYear`=2026 ✅, `showInnovationLinkQuestion`=**false**. P2-3421 is not met on the screen its own story names. One-line fix. | frozen — Innovation Use on hold |
| H4 | 🔴 | **Two incompatible catalogues for the same question.** Bilateral uses `getResultsForInnovUse` (`result.repository.ts:2645`), which returns **no `status_id`** — measured against the API: 2 392 rows, **978 Innovation Development, none carrying a status**, so the Quality-Assessed filter cannot bite and the reporter is offered drafts titled "Ttitleee" and "bua". P2-3420/P2-3421 use `getQaEdInnovationDevelopmentResults` (`:2741`), which filters **and** de-duplicates. P2-3424 is already in `Ready For UAT` with the unfiltered list. | frozen — decision pending |
| H5 | 🔴 | **Duplicate-title checking is dead for everyone.** The search call goes to `search-prms-…es.amazonaws.com/result_dev/_search` and fails with **ERR_NAME_NOT_RESOLVED** — the cluster does not exist (same dead cluster as the P2-3527 security finding). Nobody can tell whether their result was already reported, which is exactly what the form's own help text asks them to check. With the field **empty** the screen still claims *"no similarities in the PRMS Reporting Tool"*. | unassigned — check for an existing ticket first |
| H7 | 🔴 | **P2-3426's acceptance criterion cannot be met as written.** It says *"The final question in Step 4 is the one related to estimated $ investments… to be checked explicitly in QA."* On screen the last of the five questions is **"Provide any reference materials…"**. Not caused by our change: the retired question lived *after* the reference-material links, so removing it leaves references last. QA will verify that line, see it fail, and bounce the ticket for a defect that does not exist. | Ángel — correct the criterion |

## Verified healthy — do not re-test from zero

**Bilateral, manual form.** Created result **8967** (project B-A1080, Innovation Use, Reporting 2026).
Project, lead centre and primary Science Program arrive already resolved. `Next` stays disabled until a
result type is chosen. Title and description were typed, saved, **and survived a full page reload** —
completion went 17% → 33%, General Information 0/3 → 2/3. **"Bulk Upload Results" shows visible but
disabled with the COMING SOON tag**, as the mockup rule requires.

**Bilateral, from an AI draft.** The Drafts tab lists the centre's drafts; a confirmation dialog states
Science Program, project, type and centre before creating. Promoted one draft → result **8884** created,
**pre-filled with the AI title and a 667-character description**, at 43% completion, Editing.

**Classic Report new result** (`/result/result-creator`, the target of the "Report new result" button).
Output/Outcome levels, Indicator category, the 30-word title counter, and the notice that Impact/Action
Area Outcome reporting is disabled. This screen is `app-result-creator` — a different component from the
one the new pathways use; no story asks for the innovation-link question here.

**IPSR step 4 (today's P2-3426 change).** Package 8935, phase **IPSR 2026**. With
`has_scaling_studies = false` — the value the platform writes itself, not a person's answer — **the
question and its study-links list are not painted at all**, which is exactly the provisional behaviour
implemented. The rest of step 4 is untouched.

**Bilateral Innovation Use, full metadata.** The scaling-studies question **is still there**, which is
correct: P2-3535 was deliberately scoped to the Innovation Use form and left bilateral alone.
"Estimated total USD-value of investment…" carries the **Coming soon** tag in the MDS view.

**Two of the 28-Aug fixes re-confirmed on screen.** P2-3241 — the page header now follows the title
being typed instead of showing `Bilateral Draft #…`. P2-3526 — when the duplicate check cannot run it
now *says so* instead of claiming there are no similarities; that is the whole point of the fix, and it
is what made H5 visible.

---

## Corrections to my own earlier notes

- I first recorded that `/result-creator` redirected to the revamp. **The route was mistyped by me** —
  the real one is `/result/result-creator`, and it works. Nothing is broken there.
- I first read the bilateral creator screen as blank. **It was not** — my selector looked for text that
  does not exist on that page.

## Environment notes

- `existing-result-contributors` exists in the server code (`results-framework-reporting.module.ts:31`)
  but **prtest-back returns 404** — confirmed calling it directly with a token. The Results Framework
  Reporting front and back **must be deployed together**, or the ToC creation pathway dies on screen
  with no message to the user: the modal simply does not open.
- Test data left behind in prtest: results **8967** and **8884**. Delete if they get in the way.
