# Estado 2026-09-04 — épica P2-3243 (SIDS revision 2026)

**Verified:** 2026-09-04 · branch `performance-refactor`

> 🛑 **Si vienes de otro chat, LEE ESTO PRIMERO.** El 3-sep se cayeron **tres bloqueos** que otras
> sesiones siguen citando como abiertos. Repetirlos hace perder el día:

| Otro chat dice | Realidad al 4-sep |
|---|---|
| *"P2-3272 espera el texto de los dos correos y el enlace de IP · Nicoleta"* | 🛑 **Los correos LLEGARON** (Ángel, 3-sep 09:54, editando la descripción). Construidos, desplegados y en `To Be Reviewed` con Cami. Solo falta la **URL** de "What is Intellectual Property?" |
| *"P2-3531 no se pone verde, falta la función en base de datos, necesitamos acceso"* | 🛑 **RESUELTO.** La función **sí existía**. Arreglado por Juan David, probado por nosotros, `To Be Deployed` a su nombre. **El "0 de 131" era una medición mía por la ruta equivocada** |
| *"P2-3295 punto 3 no se puede probar: no hay Innovation Use de 2025 pasado a 2026"* | 🛑 **YA HAY.** Creados dos el 3-sep con permiso de Yeck. Y P2-3295 está **`Released Into Live`** |
| *"P2-3265 sigue bloqueada: un reporte de Innovation Development 2026 no se puede completar"* | 🛑 **CADUCÓ EL MISMO DÍA QUE SE ESCRIBIÓ.** Ese comentario es del 3-sep 09:08; Juan David reemplazó la función a las **15:27** y **nosotros lo verificamos a las 16:26** (P2-3531). El 4-sep lo repetí a Yeck como estado actual: **mi propio comentario viejo me engañó a mí**. P2-3265 está en `To Be Reviewed` con Cami desde el 4-sep |
| *"el desplegable de merge/split de P2-3292 está entregado"* | ⚠️ Entregado **y roto en pantalla** hasta el 4-sep: `NG0103`, el control se pintaba pero el clic no seleccionaba. Arreglado en `f38c13161`, **pendiente de ver en pantalla** |

## Los dos resultados de prueba que desbloquean todo

Creados con `PATCH /api/versioning/phase-change/process/result/:id` — **existe cambio de fase por
resultado suelto**, no hace falta el cambio de ciclo entero.

```
11494  ← 8693   Innovation development   código 6432   is_replicated: 1
11496  ← 8694   Innovation use           código 6433   con datos de 2025 dentro
```

A 8694 se le cargó antes: **uso actual 1.200** (700 mujeres / 200 jóvenes · 500 hombres / 150
jóvenes) y **proyección 2030 de 5.000**, para que hubiera algo real que comparar.

🥇 **Las URLs van por CÓDIGO, no por id** (perdí media hora con esto):

```
https://prtest.ciat.cgiar.org/result/result-detail/6432/general-information?phase=36
https://prtest.ciat.cgiar.org/result/result-detail/6433/innovation-use-info?phase=36
```

Con el id, `get/transform/:x` responde 404 legítimamente y la pantalla se queda en el skeleton.
**No es defecto**: crear un resultado y pasarlo de fase por la interfaz navegan con el código, los dos.

Verificado en pantalla el 4-sep sobre 6432: el bloque **Annual updating** se pinta con
*"Is this innovation active and receiving investment?"* — es `P2-3292` Step 1, entregado el 31-ago
y **nunca visto hasta ahora**.

## Lo entregado el 3-sep

- **P2-3292 Step 3** — tabla nueva `result_innovation_merge_split` (dónde continuó una innovación
  fusionada o dividida). **Tabla propia y no `linked_result`**: el único escritor de esa tabla
  desactiva TODOS los vínculos del resultado cuando no recibe ninguno, y el propio formulario de
  Innovation Use lo llama así al responder "no hay innovación vinculada" ⇒ la fusión se habría
  borrado con el mismo guardado que la registra. `387320a84`
- **P2-3272 / P2-3513 item 3** — los dos correos de IP con la redacción de Ángel. **Filas nuevas
  `…_2026`, sin tocar la de 2025**: hay UNA fila por nombre de plantilla y el cuerpo viejo describe
  las cuatro preguntas de IPR, que en 2025 sí se responden. `bd8a79a1f`
- **404 de Innovation Use tras cambio de fase** — `JOIN` → `LEFT JOIN` al catálogo de niveles. La
  replicación no copia `innovation_use_level_id`, el INNER no emparejaba, la sección devolvía 404 y
  el cliente pintaba **formulario vacío sin mensaje**. `d246d9afe`
- **P2-3567** — la "a" sobrante antes del enlace del tutorial. `8271d33af`

## 🛑 Incidente: prtest caído 25 min, culpa nuestra

`ResultInnovationMergeSplitRepository` se inyectó en `ResultsService` y se registró **solo** en
`ResultsModule`. `ResultsService` lo declaran también `DeleteRecoverDataModule` y
`ResultsKnowledgeProductsModule` ⇒ `UnknownDependenciesException` al arrancar. Arreglado en
`678d486e5` con el patrón del hermano: un módulo que provee y exporta, importado por los tres.

**Los tres detectores fueron ciegos:** `tsc` no ve la inyección de Nest · 2118 tests en verde
(ningún spec compila el grafo real) · **el build salió SUCCESS**, porque `Deploy Backend` corre
`docker run -d`, que devuelve antes de que la app sirva.

🥇 **Después de cada push se sondea el endpoint, no se mira el color del build.** Un `503`
**instantáneo** (~80 ms) con el HTML de Apache = no hay nada escuchando; si arrancara habría timeouts.

## Entregado el 4-sep — cuatro sesiones en paralelo

**Reparto por archivos disjuntos** (nunca por tema): `satoru` server + `rd-annual-updating` ·
`bruno` `result-detail` + `versioning` · `demon` `pages/ipsr/**` + los gates de fase ·
`jirafun` feedback + los push.

| SHA | Quién | Qué |
|---|---|---|
| `ef1484067` | satoru | P2-3292 Step 3 — el catálogo de innovaciones destino (server) |
| `87f91db2b` | satoru | P2-3292 Steps 3A/3B — los desplegables en pantalla |
| `bb0058bf6` | bruno | P2-3574 — mensaje "no existe en este año" + `GET /api/versioning/result/code/:code` |
| `dff83c7d3` | demon | P2-3573 — los dos umbrales de fase 2026 y sus gates |
| `157d5fc29` | demon | P2-3573 — Step 3: pregunta nueva, se deja de pintar "Potential situation" |
| `4177a6663` | demon | P2-3572 — Step 2: tipos 1, 2, 5 añadidos a los enablers |
| `19cdc1ccb` | jirafun | P2-3472 — Digital Tools como array |

### P2-3292 queda completo salvo verificación en pantalla
El alcance es **todo el portafolio con buscador**, como dice la historia. ⚠️ Yeck había pedido
primero limitarlo al propio programa; al releer los Steps 3A/3B textuales resultó que Ángel ya lo
especifica (*"one or more innovations from the full PRMS portfolio"*, *"searchable multi-select"*),
así que se siguió la historia y el parámetro `ownerInitiativeId` del server queda **sin usar**.
🥇 **La pregunta estaba mal planteada por no releer el requisito completo** (R16).

### 🛑 P2-3572 choca con una frase del PO, y la desviación está justificada
La nota de Ángel del 23-ago dice que `api/ipsr/innovation-pathway/*` *"must not be modified"*, y
**ese requisito es inalcanzable tal cual**: `get/complementary-innovations` es un endpoint único
compartido, sin variante P25 que tocar en su lugar (verificado: el cliente lo llama **sin** flag
`isP25()`, a diferencia de `GETInnovationPathwayStepFourByRiId`). Se fue por la vía aditiva —
el server devuelve más tipos, el cliente filtra por fase — y eso **cumple el punto 1 de esa misma
nota**, que es el que prevalece: *"It overrides any wording in this story…"*. Respaldo:
`brain/devoluciones.md` D8 y D15.

### ✅ Verificado — evidencia en un hijo
- **P2-3573** (demon) — en pantalla, **en las dos fases**, medido contra el DOM. La línea decisiva:
  *5 celdas en el DOM, 3 visibles* ⇒ la columna se oculta y **el dato sigue vivo**.
- **P2-3292 Step 3** (satoru) — el catálogo, por API: las cuatro condiciones de la historia se
  cumplen, y el caso real **8970 "test bilateral JD" (estado 6)** cerró una duda de negocio sin
  preguntar a nadie.

⇒ Evidencia completa, con cifras y cabeceras: [`2026-09-04-verificaciones.md`](2026-09-04-verificaciones.md)

### ⚠️ Lo que todavía NO está verificado en pantalla
- ~~**P2-3572**~~ ✅ **verificado en prtest real** tras el build #2146: el endpoint ofrece 1292
  filas de 5 tipos, el paquete de 2026 muestra los cuatro tipos nuevos y el de 2025 sigue en 510.
  🛑 Y trae un hallazgo de negocio para Yeck: **los *Approved* quedan fuera de los enablers, y es
  regla PREEXISTENTE** (Innovation Development ya excluía sus 14 desde antes). Cifras por tipo en el hijo.
- **P2-3292 Steps 3A/3B** — los desplegables. El resultado para probarlo existe: código **6432**.
- 🛑 R9: los dos ejes de tiempo se leen igual en el código. Nada de esto se cierra por lectura.

### Trampas del entorno descubiertas hoy
- 🛑 **Un build ROJO puede no ser código.** El **#2143** murió en `Build Frontend`, en el
  `COPY --from=build .../dist/onecgiar-pr-client/browser` del Dockerfile con "not found", justo
  después de que el log dijera *"Application bundle generation complete"*. El **#2144**, con la
  misma punta, salió SUCCESS. Es el COPY/caché de Docker: **reintentar basta**, no se busca el bug
  en el cliente.
- ⚠️ **La suite del server muere con exit 137 (OOM)** con cuatro sesiones a la vez, y se lleva por
  delante el `ng serve` de quien la corra. Correrla con `-w 2`.
- ⚠️ **La suite pasó de 92 s a 249 s** por la misma razón. Lenta no es colgada.

----------

## Abierto — al 4-sep 16:10

> Revisado contra Jira leyendo cada ticket, **no** por JQL (el índice va con latencia).

- **P2-3292** — desarrollo completo. Falta **una** cosa: verlo en pantalla sobre el código **6432**
  (marcar inactiva → tildar "merging" → elegir dos → guardar → **RECARGAR**). Hasta eso, está
  *entregado*, no *verificado*.
  ✅ **El alcance ya NO es una decisión pendiente:** es **todo el portafolio con buscador**, como
  especifica la historia (Yeck, 4-sep: *"no, como dijo Ángel no hay problema"*). El parámetro
  `ownerInitiativeId` del server queda deliberadamente sin usar.
- **P2-3569** — con `demon`. Bug de Cami: en el modal *"Report emerging result"* la pregunta
  obligatoria de vinculación **no se pinta nunca**. 🥇 **Causa real, suya:** la funcionalidad quedó
  cableada a `entity-details`, que es **código muerto** — sus rutas existen pero cargan
  `DashboardLabComponent`. Al copiar el modal a la página viva se perdió el atributo que la
  enciende, y el input declara `false` por defecto. Explica los tres síntomas a la vez, incluido
  que el dato llegue correcto a la base.
  🛑 **Decisión de Yeck pendiente:** el arreglo es **un atributo** en `dashboard-lab.component.html`,
  que es **módulo de JC** (R24). ¿Lo hacemos nosotros por ser nuestra propia funcionalidad, o viaja
  a JC con el diagnóstico dentro?
- **P2-3421** — `To Be Improved`. No espera nada nuestro más que P2-3569, que es su único FAIL
  (Cami: 7 PASS / 1 FAIL / 1 BLOCKED, 3-sep).
- **P2-3272** — solo la URL de "What is Intellectual Property?" (Nicoleta). Y ⚠️ **una
  contradicción**: la descripción, editada el 3-sep, dice que los destinatarios salgan de una lista
  de SharePoint, pero `intellectual_property_experts` ya tiene 25 personas en 16 centros y el PO lo
  confirmó el 27-ago. Preguntado en el ticket con opción A/B.
- **P2-3568** (al cambiar de fase, la evidencia pierde en qué sección estaba) y **P2-3560** — Juan
  David. 🛑 **P2-3568 NO es el green check** — ese era P2-3531 y está resuelto. Confundirlos hace
  citar un bloqueo que no existe.
- **W-20260903-13** — hueco propio: `innovation_use_2030_previous` no devuelve `phase_year`,
  mientras `current_use_previous` sí ⇒ la pantalla no puede etiquetar "(FY2025)" en el bloque de 2030.

### En pruebas con Cami (no son nuestras hasta que vuelvan)

`P2-3265` `To Be Reviewed` · `P2-3513` `To Be Reviewed` · `P2-3514` `Ready For UAT` ·
`P2-3515` `To Be Reviewed` · `P2-3535` `Ready For UAT` · `P2-3537` `To Be Reviewed` ·
`P2-3550` `Ready For UAT` · `P2-3572` `Ready For UAT` · `P2-3573` `Ready For UAT`.

⇒ De las 33 hijas de P2-3243: **20 cerradas**, **9 en pruebas**, **2 de Juanda**, **2 nuestras vivas**.

## Falsos defectos cerrados el 3-sep (de QA, con razonamiento dentro)

- **P2-3564** `Closed` — el arreglo visual de P2-3291 aplica también a 2025, cierto; pero lo de antes
  era **CSS roto** (una regla anidada mal que nunca casaba) y gatearlo obligaría a que un componente
  compartido conozca la fase. Decisión de Yeck.
- **P2-3565** `Done` — la pregunta "linked or bundled" **sí está** para Innovation Use 2026, con el
  texto literal de P2-3420/3421 en vez del genérico de P2-3358. Verificado en navegador. Dos
  historias especifican el mismo control con textos distintos y ninguna dice cuál gana.
