# Barrido de verificación pre-producción — 2026-09-02

Épico de defectos: **P2-3558**. Un tipo de indicador por iteración. Ambiente `prtest`, cliente **v21**
(sello en pantalla y sello del artefacto servido coinciden: `main.js` → `APP_VERSION = "21"`).
Navegador headless, una pestaña, cerrado al final de cada tanda.

----------

## Tipo 1 · Capacity sharing for development — ✅ FUNCIONA COMPLETO

**Resultado de prueba:** `8994` / id 11462, fase 2026, W1/W2, Output, *Editing*
(*"Nightly flow check 2026-09-02 synthetic capacity sharing result do not use for QA"*, creado anoche
por la interfaz). Contraste de fase 2025: `5928` (vacío) y `4277` (con datos).

### (b) Las cinco secciones cargan limpias

| Sección | Campos visibles | 4xx/5xx | Errores de consola | Expulsión |
|---|---|---|---|---|
| General information | 3 | — | — | no |
| Contributors & partners | 3 | — | — | no |
| Geographic location | 5 | — | — | no |
| CapSharing info | 11 | — | — | no |
| Evidence | 0 (control propio) | — | — | no |

### (c)(d) Los 11 campos, escritos → guardados → recargados

Women `3` · Men `2` · Non-binary `1` · Unknown `4` · Length of training **Short-term** ·
Delivery Method **Virtual / Online** · *Were the trainees attending on behalf of an organization?*
**No** → guardado `201 PATCH /api/results/summary/capacity-developent/create/result/11462`,
aviso *"saved successfully"*.

Tras **recargar**: los cuatro números, los tres grupos de radio y el estado **Section complete**
persisten idénticos. 🥇 Es el paso que el barrido existe para hacer.

### 🥇 P2-3246 verificado en pantalla, en sus DOS ramas

El ticket (`To Be Deployed`, nadie lo había visto en pantalla) dice que el radio *"attending on behalf
of an organization"* no refleja su valor guardado tras recargar. **Hoy sí lo refleja:**

| Rama | Tras recargar | Green check |
|---|---|---|
| **No** | radio *No* marcado | `Section complete` |
| **Yes** | radio *Yes* marcado + aparece *Select organizations* | `1 field missing` (correcto: falta la organización) |
| **Yes** + organización *(Id:5) IRRI* | `organization(s) selected (1) (Id:5) IRRI …` con su chip | `Section complete` |

### (e) Contraste con fase 2025 — sin regresión

`cap-dev-info` **no tiene ningún gate de fase** (`grep` de `isP25` / `isCP2026` / `ReportingDesignYear`
en `cap-dev-info.component.{ts,html}`: cero coincidencias), así que el formulario es el mismo en las dos
fases y el contraste consiste en que 2025 siga leyendo lo suyo. Lo hace: `4277` (fase 2025) abre con
`25` / `25` y dos radios marcados, sin 4xx.

### 🛑 Lo que NO se verificó, y por qué

- **Subir un fichero en la Evidence de este tipo.** El control *"Add evidence"* está presente
  (comprobado en `8994`, `8559` y `8842`), y la subida completa —adjuntar → guardar → recargar → sigue
  ahí con su enlace al repositorio— **se ejerció anoche** en el mismo componente compartido (`8842`,
  `result-8842-Document-202609020853-498.pdf`). No se repitió aquí.
- **`Submit result`**: prohibido por el comando (dispara notificaciones a personas reales).
- **Abrir el fichero subido**: necesita sesión de SharePoint en el navegador del agente.

### ❌ Falsos positivos descartados en esta tanda (3)

| Sospecha | Veredicto | Instrumento que lo cerró |
|---|---|---|
| La organización elegida no persiste tras recargar | ❌ persiste | El `innerText` recortado no mostraba el valor; el DOM del multiselect sí (`organization(s) selected (1)`), y el green check pasó a `Section complete` |
| La Evidence de Capacity sharing no tiene control para añadir | ❌ existe | Mi filtro de botones cortaba por longitud; *"Add evidence"* está en las tres pruebas |
| Un resultado de fase 2025 no abre (`404` en `get/transform`) | ❌ instrumento | `5916` y `5924` dan 404 en las tres fases probadas: son de los que `get/all/simplified` lista pero no se pueden abrir. `5928`, `4277` y `5921` dan 200 con `phase=34` |

### Datos de prueba escritos (regla 22 — se quedan puestos)

- **8994 / id 11462**, CapSharing info: `3 / 2 / 1 / 4`, Short-term, Virtual/Online,
  *attending on behalf* = **Yes** con la organización **(Id:5) IRRI**. Estado final: `Section complete`.

----------

## Tipo 2 · Innovation development — 🔴 EL FORMULARIO FUNCIONA, PERO LA SECCIÓN NO PUEDE PONERSE VERDE

**Resultado de prueba creado hoy de cero por la interfaz:** `8995` / id 11463, fase 2026, SP08, Output,
Innovation development (*"Barrido 2026-09-02 innovation development synthetic result do not use for QA
sign off"*), `201 POST /api/results/create/header`.

### ✅ Lo que SÍ funciona (y prueba los arreglos de anoche en el escenario que los rompía)

🥇 **La sección abre en un resultado recién creado** — 33 campos, 24 radios, **cero 4xx/5xx**. Ese es
exactamente el caso que **P2-3555** rompía (500 porque la fila de `results_innovations_dev` no existe
hasta el primer guardado). Positivo concluyente: el código anterior no podía producir esta respuesta.

🥇 **El guardado responde `201`** (`PATCH /v2/api/innovation-development/innovation-dev/create/result/11463`),
cuatro veces seguidas y sin un solo 500 — el caso de **P2-3557**, que bloqueaba el 100% de la fase 2026.

**Persistencia tras recargar, campo por campo:** nombre corto · características (*Incremental innovation*) ·
tipología (*Technological innovation*) · las dos preguntas nuevas de 2026 (**GESI** y **evaluación de
riesgo**) · IPR y soporte de experto en PI · *new or improved variety* · **readiness level 4 —
Controlled Testing** · 2 casillas de diversidad del equipo · los 3 textos largos. **Todo vuelve.**

✅ **P2-3550 confirmado también en un resultado nuevo**: el bloque *Innovation reference materials* no
se pinta en 2026 (`hasRefMaterials: false`).

### 🔴 El defecto: `validation_innovation_dev_P25` NO EXISTE — la sección nunca se pone verde

Con **todos** los campos requeridos respondidos y persistidos, y con el panel *STILL MISSING* ya
**vacío**, el pie de la sección dice **"Section incomplete"** y el sidebar **"0 of 5 sections complete"**.

**Medido por API** (`GET /v2/api/results/results-validation/get/green-checks/<id>`):

| Muestra | `innovation-dev-info` |
|---|---|
| **10 de 10** resultados de Innovation development de **fase 2026** | 🔴 `False` en todos |
| **8 de 8** de **fase 2025** (mismo portafolio P25) | 🔴 `False` en todos |
| **6 de 6** de fase 2022 (portafolio P22) | 🔴 `False` en todos |
| Contraste: `cap-dev-info` en `8994` (Capacity sharing completo) | ✅ `True` |

⇒ El mecanismo de green check **funciona sección a sección**; el que nunca pasa es este.

**⚠️ CORRECCIÓN (misma jornada, tras leer `brain/dominio.md` §3):** mi primera redacción daba el
`grep` de migraciones como causa raíz confirmada. **No lo es, y esa inferencia está expresamente
desaconsejada**: las funciones `validation_<sección>_<portafolio>` **no viven en el repo** — se aplican
a mano contra la base, y las migraciones con cuerpo (`1762528725798-createValidtionP25`,
`1761849861521-createValidtionP22`) están **stale**; volver a correrlas revierte en silencio lo que
esté vivo (Juan David Delgado, `P2-3253`, 31-ago). Ya se nos escapó una conclusión falsa así en
`P2-3467`. **La única lectura fiable es `SHOW CREATE FUNCTION` contra el ambiente, o el comportamiento
observado.**

**La evidencia que SÍ sostiene el hallazgo es la medición, no el repo:**

- `innovation-dev-info` = `False` en **24 de 24** resultados (10 de fase 2026, 8 de 2025, 6 de 2022).
- Entre ellos **8995**, donde hoy respondí y persistí **todos** los campos requeridos que el formulario
  pinta, y donde el panel *STILL MISSING* queda **vacío**.
- Contraste que descarta "el mecanismo está roto en general": `cap-dev-info` = `True` en `8994`, e
  `innovation-use-info` = `True` en 3 de 5 resultados 2026. **Donde la validación existe, la sección
  se pone verde.**

**Indicio complementario, no prueba** (a verificar contra el ambiente, no contra el repo): en la
migración P25 falta exactamente `validation_innovation_dev_P25` mientras están las otras once, y el SP
`validate_sections_mapped_batch` inserta `FALSE` cuando la función no existe — comentario literal
*"Función no existe, insertar FALSE"*. El mapeo sección→función está en `insertValidationMaps.ts:23`.

**Lo que significa para la persona que reporta:** rellena la sección entera, guarda, todo se guarda
bien… y la sección **nunca** se marca como completa, así que **el resultado no se puede enviar**
(`submit: False`). Y no hay forma de saber por qué: el desglose de campos faltantes está vacío.

🛑 **No se arregla desde aquí: es green check + migración, dominio de Juan David** (reglas 14 y 18).

📌 **Ya tiene ticket: `P2-3531`**, abierto y a nombre de Juanda desde el 28-ago — comprobado antes de
crear nada, así que no se levantó duplicado. Lo que aporta esta medición **no es el diagnóstico, es la
magnitud**: convierte "hay que crear una función" en "ningún resultado de Innovation Development se
puede enviar, en ninguna fase". El mapeo de la sección al nombre de función está en
`insertValidationMaps.ts:23` (`innovation-dev-info` → `innovation_dev`).

### ⚠️ Sospecha aparte, declarada sin acusar

`contributor-partners` sale `False` en **los 18** resultados P25 medidos, y de todos los tipos —
también en el Capacity sharing que sí completa su propia sección.

📌 **Resuelta el mismo día, y NO era requisito legítimo: es `P2-3528`.** Un barrido paralelo lo cerró
como bloqueante duro — un resultado mapeado a la ToC no puede poner verde Contributors & partners
porque el green check sigue exigiendo la narrativa que el formulario 2026 **oculta**
(`1762866499786-updatepartnersContributors.ts:47-52`; el JOIN de fase está ahí sin usar en `:63`).
Estos 18 resultados en False, de todos los tipos, son la **confirmación independiente** de que no es
un dato que nadie rellenó. También de Juan David.

### ❌ Falso positivo descartado en esta tanda

Creí que el **nombre corto de la innovación** no se guardaba (`short_title: None` en la API). Era el
instrumento: escribí en el primer `input[type=text]` visible, que **no** es ese control. Localizado por
su placeholder real (*"Innovation short name goes here…"*), se escribe, se guarda y **persiste**.
Cuarto falso positivo de la jornada, misma familia: medir por posición en vez de por identidad.

### 🛑 No verificado

- **Subida de fichero en este tipo**: se ejerció anoche de punta a punta en `innovation-dev-info`
  (`8563`, `result-8563-Document-202609020943-499.pdf`). No repetida hoy.
- **`Submit result`**: prohibido — y de hecho está bloqueado por el defecto de arriba.

### Datos de prueba escritos (regla 22 — se quedan puestos)

- **8995 / id 11463**: sección Innovation Dev info completa con valores sintéticos, readiness level 4.

----------

## Tipo 3 · Innovation use — 🟡 FUNCIONA, CON UNA PEGA DE CONFIANZA

**Resultado creado hoy de cero:** `8996` / id 11464, fase 2026, SP08, **Outcome** → Innovation use
(*"Barrido 2026-09-02 innovation use synthetic result do not use for QA sign off"*),
`201 POST /api/results/create/header`.

🧊 **Innovation Use sigue congelado por Ángel** (solo se descongeló la guarda de pérdida de datos del
bilateral, `91002df61`). Por eso esta tanda es **solo verificación en pantalla: no se tocó código.**

### ✅ Las cinco secciones cargan, y la de tipo guarda y persiste

| Sección | Campos | 4xx/5xx | Expulsión |
|---|---|---|---|
| General information | 3 | — | no |
| Contributors & partners | 61 | — | no |
| Geographic location | 5 | — | no |
| **Innovation use info** | 4 + controles propios | `404 GET /v2/api/innovation-use/get/result/11464` | no |
| Evidence | control propio | — | no |

⚠️ **El 404 de la sección de tipo es el patrón "todavía no hay fila", no un fallo** — cuerpo:
`"Innovation Use not found for result 11464"`. **Y lo importante: el botón Save NO queda bloqueado**
(`disabled: false`), que era la casi-regresión temida al añadir la guarda de carga. Comprobado en
pantalla y con el guardado real.

**Ciclo completo verificado:** *Add actor* → tipo **Researchers** → 10 mujeres / 4 jóvenes / 20 hombres
/ 6 jóvenes → nivel de uso **3** → guardar (`201 PATCH /v2/api/innovation-use/create/result/11464`) →
**recargar** → todo vuelve, Total 30. Confirmado también por API:
`actors[0] = { result_actors_id: 1080, women: 10, women_youth: 4, men: 20, men_youth: 6 }`, `level: 3`.

🥇 **Y este tipo SÍ pone verde su sección**: `innovation-use-info` sale `True` en **3 de 5** resultados
2026 medidos (11026, 11064, 11091). Es el contraste que refuerza el hallazgo del tipo 2 — donde la
función de validación existe, la sección se pone verde; en Innovation development **nunca**.

### 🟡 La pega: una fila de actor SIN tipo se descarta en silencio

**Reproducción medida (dos pasadas sobre el mismo resultado):**

1. *Add actor* → escribir **solo los números** (sin elegir *Actor type*) → la pantalla **calcula y
   muestra el Total (25)** → *Save draft* → **`201`**, ningún aviso → **recargar** → la fila ha
   desaparecido; por API `actors: []`.
2. Misma fila **con** *Actor type = Researchers* → `201` → recargar → **persiste**.

⇒ No es pérdida de datos guardados: es **una fila nueva que el servidor descarta por incompleta sin
decírselo a nadie**. La persona ve el total calculado y un guardado correcto, y al volver no está.
Familia conocida: *el guardado que dice sí y descarta*.

🛑 **No se arregla en esta tanda: Innovation Use está congelado.** El arreglo natural es de front
(exigir el tipo antes de aceptar la fila, o avisar de que se descartará) y **es decisión de Yeck**
descongelarlo para esto (regla 19: la experiencia la decide él, no negocio).

### 🛑 No verificado

- **`Submit result`**: prohibido, y de todos modos `submit: False`.
- **Subida de fichero** en este tipo: no ejercida (el componente de Evidence es el compartido, ya
  probado de punta a punta en otros tipos).
- **Contraste de fase 2025**: la sección `innovation-use-info` **no aplica** en los resultados 2025
  medidos (`green-checks` devuelve `None` para ella en `8131`), así que no hay contraste que hacer.

### Datos de prueba escritos (regla 22 — se quedan puestos)

- **8996 / id 11464**: un actor *Researchers* con 10/4/20/6 y nivel de uso 3.

----------

## Tipo 4 · Knowledge product — ✅ CARGA Y VALIDA · 🛑 el guardado quedó SIN EJERCER, a propósito

**No se creó un KP nuevo**: un Knowledge product nace de un registro de CGSpace, así que se usaron
resultados existentes. **8583** (fase 2026, *"Policy lessons for scaling climate-resilience… Nigeria"*)
y **8578** como contraste de fase 2025.

### ✅ Las cinco secciones cargan limpias, y la de tipo ya está verde

| Sección | Campos editables | 4xx/5xx | Estado |
|---|---|---|---|
| General information | 1 | — | 6 campos faltantes |
| Contributors & partners | 64 | — | 12 campos faltantes |
| Geographic location | 5 | — | — |
| **Knowledge product info** | **1** (+ el Handle) | — | ✅ **Section complete** |
| Evidence | control propio | — | ✅ **Section complete** |

**La sección es casi toda metadata de solo lectura traída de CGSpace** (autores, tipo, peer reviewed,
DOI, accesibilidad, licencia, AGROVOC, Altmetric, FAIR score). El único campo que el usuario responde
es **"Is this knowledge product a MELIA Product?"**.

✅ **La cadena condicional se pinta bien**: al responder **Yes** aparece *"Do you have a MELIA study
planned in your TOC?"*, que es la puerta del flujo de **P2-3548**.

✅ **Contraste de fase 2025** (`8578`, `?phase=34`): misma sección, misma pregunta MELIA, FAIR score
presente, **cero 4xx**. Sin regresión.

### 🛑 Lo que NO se ejerció, y por qué — decisión, no olvido

**El ciclo guardar → recargar del campo MELIA no se ejecutó.** `8583` es un resultado **ajeno**, con
título y autores que parecen datos reales, y ejercer ese ciclo obliga a **escribir en él**. La regla 22
autoriza crear datos de prueba, pero *"sobre resultados de prueba, no sobre datos de alguien"*.

Se cambió el radio a *Yes* solo en pantalla y **se descartó recargando sin guardar**: comprobado que
`8583` sigue con la pregunta condicional oculta y *Section complete* — **no quedó nada escrito**.

**Para cerrarlo hace falta un KP de prueba propio, y eso exige un enlace de CGSpace** — pedido a la
otra sesión, sin buscarlo por mi cuenta (instrucción explícita).

📌 **P2-3548 (el estudio MELIA que seguía adjunto) ya está verificado en pantalla desde anoche**, en
tres casos y con positivos concluyentes. Lo de aquí sería una cuarta confirmación, no la única.

### 🥇 Hallazgo lateral que MATIZA `P2-3528` — la sección sí puede ponerse verde

Midiendo green checks salió el dato que faltaba para acotar ese ticket:

| Medición | Resultado |
|---|---|
| `contributor-partners` sobre **40 resultados de fase 2026**, todos los tipos | ✅ `True` en **2** · 🔴 `False` en **38** |
| Los dos verdes | ids **11026** (Innovation use) y **11040** (Capacity sharing) |
| `knowledge-product-info` sobre 4 KP | ✅ `True` en **4 de 4** |

⇒ El bloqueo de Contributors & partners **no es universal**: hay dos resultados que sí la ponen verde.
Encaja con que golpee a un subconjunto (los mapeados a la ToC, según el diagnóstico de `P2-3528`), pero
**la afirmación "nunca se puede poner verde" sería falsa**. Dato pasado a la otra sesión antes de que
suba la medición al ticket.

### ⚠️ Trampa de instrumento nueva, y de las caras

**`get/all/simplified` da un `year` que NO es la fase con la que se abre el resultado.** El código
`8578` viene listado como `year: 2026` y sin embargo **solo abre con `?phase=34`** (2025): con
`?phase=36` responde 404 y **la app expulsa a la lista de resultados sin ningún mensaje**. Con `8649`
pasa lo mismo en las dos fases (de los que se listan pero no se pueden abrir).

⇒ **Antes de dar por roto un resultado, comprobar con qué fase abre**:
`GET /api/results/get/transform/<code>?phase=<id>` sobre 36 y 34. Es la quinta trampa de instrumento
de la jornada y la que más se parece a un defecto real.
